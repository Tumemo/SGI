# Estado da implementação

Atualizado em: 08/09/2026, após a conclusão de T29.

**Estado:** T00 a T29 concluídas com as limitações externas explicitadas abaixo.

**Próxima tarefa:** nenhuma tarefa do plano. Antes de publicar, executar os alvos externos pendentes do CI e revisar o diff local; não houve publicação nesta execução.

Os resultados verdes registrados na auditoria pertencem ao código de referência. Não são evidência de correções ainda não implementadas.

| Tarefa | Estado | Evidência / observação |
| --- | --- | --- |
| T00 | Concluída | Ambiente confirmado; runner HTTP 224/224 e navegador 26/26 aprovados em banco dedicado |
| T01 | Concluída | Fixture A/B com IDs sintéticos, limpeza/restauração e registro no runner |
| T02 | Concluída | Contexto imutável, política pura e adaptador HTTP de sessão |
| T03 | Concluída | Escopo por edição em artilharia e ocorrências, inclusive temporários e PUT |
| T04 | Concluída | Partida persistida autorizada, IDs discordantes rejeitados e finalização legada preservada |
| T05 | Concluída | Resolução segura de resultados temporários e sincronização em lote |
| T06 | Concluída | Falhas internas não expõem SQL e envelopes preservados |
| T07 | Concluída | Regra pura do cronômetro especificada e coberta por testes determinísticos |
| T08 | Concluída | Persistência PHP, snapshot v2, timezone explícito e replay idempotente |
| T09 | Concluída | Cronômetro offline, snapshot v2, projeção local e aceitação no navegador |
| T10 | Concluída | Gravação imediata do placar na fila existente, projeção local e sincronização |
| T11 | Concluída | Ocorrência exata por ID, edição offline, referências temporárias e filtro de status |
| T12 | Concluída | Revalorização da arrecadação preserva pontos esportivos e ajustes, com migração 003 |
| T13 | Concluída | Registro por diferença, estorno sob trava, lote atômico e concorrência determinística |
| T14 | Concluída | Créditos de pódio com fonte única, delta de retificação, diagnóstico e adoção legada idempotente |
| T15 | Concluída | Retificação transacional de pódio mata-mata, invalidação de fases e terceiro lugar |
| T16 | Concluída | Individual validado antes da substituição, créditos idempotentes no lote e reconciliação mata-mata na mesma transação |
| T17 | Concluída | Histórico usa doação atual, fontes de pódio, ajuste residual e líquido comum a admin/aluno |
| T18 | Concluída | Helper determinístico e matriz concorrente completa verdes após T19/T20; 344/344 asserções |
| T19 | Concluída | Trava aluno/edição/turma/modalidade antes do limite; concorrência mantém no máximo três modalidades |
| T20 | Concluída | Limite de equipes aplicado por capacidade numérica, status e escopo transacional; matriz T18 verde |
| T21 | Concluída | Caso único de ativar/criar edição, trava nomeada por banco, rollback e sincronização de alunos |
| T22 | Concluída | Resultado e transação coordenados em Application; pontuação e individual usam contratos |
| T23 | Concluída | Usuários e ativação desacoplados |
| T24 | Concluída | Inscrições e contratos entre módulos |
| T25 | Concluída | Testes arquiteturais |
| T26 | Concluída | Upgrade e falhas de migração |
| T27 | Concluída | Compatibilidade offline antiga, isolamento e retry |
| T28 | Concluída | Matriz local disponível, subdiretório `/SGI` e recuperação sintética aprovados; alvos externos explicitados |
| T29 | Concluída | Checks sequenciais, rastreabilidade, documentação final e encerramento dos processos próprios registrados |

## Registro por tarefa

Ao concluir ou interromper uma tarefa, acrescentar:

```text
Tarefa:
Estado: em andamento / concluída / pendente por motivo específico
Referência do código: commit + arquivos locais relevantes
Defeito reproduzido: cenário e resultado observado antes da correção
Arquivos alterados:
Decisão aplicada:
Comandos executados e resultado de cada um:
Evidência: log/asserção/valor persistido
Dependências ou limitações restantes:
Próximo passo exato:
```

Não registrar segredos, cookies, tokens ou dados pessoais. Não marcar uma etapa inteira concluída porque um subconjunto de testes passou.

## Registro por execução

Tarefa: T00 — Confirmar ambiente, código e resultados anteriores
Estado: pendente por motivo específico; a etapa não foi marcada como concluída porque o banco isolado não está disponível.
Referência do código: `aedbc420ea34aa759db1b14a8307abe4eb478cfa`, branch `codex/refatoracao-arquitetura-limpeza`; apenas `docs/auditoria-arquitetura-2026-09-07.md` e `docs/plano-implementacao-luna/` já estavam não versionados. Nenhum arquivo de aplicação foi alterado.
Defeito reproduzido: não há defeito de aplicação sendo corrigido em T00. O impedimento de ambiente foi reproduzido por `docker compose -f compose.test.yml --profile mariadb up -d --wait` (daemon Docker indisponível), pela ausência de listener nas portas 3306/3307/3308/8110 e por `php tests/run_all.php` (conexão recusada ao `127.0.0.1:3308`). O serviço Docker também permaneceu parado mesmo após tentativa de inicialização, sem permissão para abrir o serviço.
Arquivos alterados: `docs/plano-implementacao-luna/STATUS.md` apenas. Os logs e artefatos de teste ficaram em `test-results/`, fora do controle de versão.
Decisão aplicada: usei `C:\xampp\php\php.exe` (PHP 8.2.12, `mysqli` habilitado) para a verificação que exige MySQLi e coloquei esse diretório no `PATH` do processo para que Composer e subprocessos usassem a mesma versão. Não instalei dependências já presentes, não alterei a política do sistema, não apontei para banco de trabalho e não executei reset sem banco confirmado.
Comandos executados e resultado de cada um:

- `git status --short`: passou; somente a auditoria e o plano estavam não versionados.
- `git rev-parse HEAD`: `aedbc420ea34aa759db1b14a8307abe4eb478cfa`.
- `git branch --show-current`: `codex/refatoracao-arquitetura-limpeza`.
- `php -v`: PHP 8.4.25; `php --ri mysqli`: falhou porque a extensão não está presente.
- `C:\xampp\php\php.exe -v` e `C:\xampp\php\php.exe --ri mysqli`: passaram; PHP 8.2.12 com MySQLi habilitado.
- `node --version`: `v24.14.0`; `composer --version`: Composer 2.10.3; `docker compose version`: v5.1.1, com daemon indisponível.
- `vendor/autoload.php`, `node_modules/` e `tests/browser/node_modules/@playwright/test`: presentes; nenhuma instalação foi executada.
- `docker compose -f compose.test.yml --profile mariadb up -d --wait`: falhou por falta de conexão com o Docker Engine.
- Servidor `tools/start-test-server.ps1 -Database sgi_test_luna -Port 8110 -PhpPath C:\xampp\php\php.exe`, iniciado com `ExecutionPolicy Bypass` somente no processo: passou; `GET /api/v1/health` retornou `success: true`, `status: ok` e `test_environment.database: sgi_test_luna`.
- `C:\xampp\php\php.exe tests/run_all.php` com `SGI_DB_HOST=127.0.0.1`, `SGI_DB_PORT=3308`, `SGI_DB_NAME=sgi_test_luna`, `SGI_TEST_DB_NAME=sgi_test_luna` e `SGI_APP_ENV=test`: falhou antes de resetar qualquer base, com conexão recusada.
- `PATH` iniciado por `C:\xampp\php;` + `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit 116 testes/1.514 asserções, PHPStan 179/179, CS Fixer 230/230 e verificação de sintaxe sem erros.
- `npm run build`: passou, 230 assets preparados. `npm run check`: passou, 39 arquivos JavaScript válidos. `npm test`: passou, 5 testes.
- `npm --prefix tests/browser test` sem `SGI_BASE_URL`: falhou contra o padrão `http://localhost/SGI/` (18 falhas, 8 não executados), portanto não foi usado como evidência funcional. Repetido com `SGI_BASE_URL=http://127.0.0.1:8110/`: os cenários começaram, mas ficaram bloqueados pela conexão ao banco e foram interrompidos após os primeiros timeouts; não há aprovação de navegador.
- `git diff --check`: passou.
Evidência: health check confirmado para `sgi_test_luna`; checks locais verdes nos números acima; runner HTTP sem execução de migrações/fixtures por conexão recusada; `git status --short` final sem alterações em código, recursos, migrações ou assets versionados.
Dependências ou limitações restantes: iniciar o Docker Desktop/daemon ou disponibilizar uma instância MariaDB de testes comprovadamente descartável em `127.0.0.1:3308` com usuário `root`, senha `sgi-test-only` e banco `sgi_test_luna`. Depois disso, o runner HTTP e a suíte de navegador precisam ser executados de forma completa; só então T01 poderá criar e registrar as fixtures.
Próximo passo exato na interrupção inicial: confirmar que `127.0.0.1:3308` aceita a conexão, consultar novamente `/api/v1/health`, executar `C:\xampp\php\php.exe tests/run_all.php` com as variáveis do plano, depois executar `npm --prefix tests/browser test` com `SGI_BASE_URL=http://127.0.0.1:8110/`. Essa retomada foi concluída abaixo.

Retomada de T00: o MariaDB do XAMPP foi usado somente como instância descartável, em uma datadir nova `test-results/mysql-t00`, sem tocar na datadir de trabalho. `C:\xampp\mysql\bin\mysql.exe --protocol=TCP --host=127.0.0.1 --port=3308 --user=root` confirmou MariaDB 10.4.32 em `root@localhost`; o health check continuou confirmando `sgi_test_luna`.
Resultado da retomada: `C:\xampp\php\php.exe tests/run_all.php` passou com 224/224 asserções e 0 falhas. `npm --prefix tests/browser test`, com `SGI_BASE_URL=http://127.0.0.1:8110/` e o mesmo banco isolado, passou com 26/26 testes. A execução inicial bloqueada permanece registrada acima como histórico de ambiente; não foi usada como aprovação.
Próximo passo registrado antes da conclusão de T01: abrir T01, ler `tests/Support/TestClient.php`, `tests/Support/Assertions.php`, `tests/Integration/AuthAndRbacTest.php`, `tests/Integration/AtomicMutationTest.php`, `tests/Integration/InterclasseLifecycleTest.php`, `tests/bootstrap.php`, `phpunit.xml`, `tests/browser/fixtures.cjs` e o runner necessário; criar a fixture sintética e seu teste pelo mecanismo de execução existente, sem iniciar T02. A execução foi concluída abaixo.

Tarefa: T01 — Preparar fixtures e registro das novas regressões
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `tests/Support/AuditFixtures.php`, `tests/Integration/AuditFixturesTest.php`, `tests/run_all.php`.
Defeito reproduzido: primeiro o teste registrado falhou com `Class "SGITests\\Support\\AuditFixtures" not found` e 0 asserções, confirmando que um cenário não executaria sem o helper. Depois do helper inicial, a suíte HTTP passou, mas a suíte browser reproduziu uma regressão de isolamento: 25/26 testes passaram e o RBAC falhou com `Turma não pertence à edição ativa`, porque a fixture apagava linhas mas deixava os contadores `AUTO_INCREMENT` avançados; o teste legado usa o fallback histórico `id_turma=1` quando a rota compatível é chamada sem edição explícita.
Arquivos alterados: `tests/Support/AuditFixtures.php`, `tests/Integration/AuditFixturesTest.php`, `tests/run_all.php` e este `STATUS.md`. Nenhum arquivo de aplicação, recurso, migração, alias, cache ou fila foi alterado.
Decisão aplicada: a fixture usa transação, nomes sintéticos, IDs retornados pelo banco e limpeza em ordem compatível com as chaves estrangeiras. Captura e restaura os `AUTO_INCREMENT` das tabelas que toca, além da edição ativa anterior, para não mudar o significado dos testes existentes nem depender de IDs presumidos nos novos cenários. A senha dos atletas é um hash de fixture e não é dado de usuário real.
Comandos executados e resultado de cada um:

- Registro inicial de `AuditFixturesTest` no runner e `C:\xampp\php\php.exe tests/run_all.php`: falhou pelo símbolo ausente esperado, exit 1.
- Runner após o primeiro helper: 244/244 asserções, 0 falhas; revelou a alteração de sequência pelos IDs 25/26 nos cenários seguintes.
- `npm --prefix tests/browser test` sem reset entre execuções: falhou por estado residual (21/26 aprovados); execução com `SGI_E2E_RESET=1` isolou o defeito da fixture em 25/26, com apenas o RBAC falhando pelo fallback `id_turma=1`.
- Runner após restaurar `AUTO_INCREMENT`: 253/253 asserções, 0 falhas; 20 asserções de estrutura/limpeza e 9 de restauração de sequência da fixture passaram.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify` com `C:\xampp\php` no `PATH`: passou; PHPUnit 116/1.520, PHPStan 179/179, CS Fixer 230/230 e lint de todos os PHP, incluindo os novos arquivos.
- `npm run build`: 230 assets; `npm run check`: 39 arquivos JavaScript; `npm test`: 5/5; nenhum JavaScript foi alterado por T01.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, `SGI_PHP_PATH=C:\xampp\php\php.exe`, servidor `127.0.0.1:8110` e banco `sgi_test_luna`: 26/26 testes aprovados em 3,5 minutos; o global setup também confirmou 253/253 asserções HTTP.
- `git diff --check`: passou.
Evidência: a saída do runner contém a Suite 0 e todos os registros de criação/restauração/remoção; a execução final do navegador passou pelos fluxos de RBAC, pontuação, torneios offline e contratos visuais. O status do Git final contém apenas os novos testes/helper, o runner e a documentação já prevista.
Dependências ou limitações restantes: T02 deve usar a fixture de autorização já disponível e manter o runner HTTP como entrada; o banco descartável continua sendo `test-results/mysql-t00` em `127.0.0.1:3308` durante esta sessão.
Próximo passo exato: abrir `docs/plano-implementacao-luna/01-autorizacao.md`, ler somente T02 e suas fontes necessárias, escrever a regressão de mesário tentando operar recurso da edição B enquanto A está ativa, confirmar a falha, corrigir autorização na menor fronteira indicada e validar banco/HTTP antes de T03.

Tarefa: T02 — Política única de acesso à edição
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Acesso/Domain/ContextoOperador.php`, `src/Modules/Acesso/Application/EdicaoAccessPolicy.php`, `src/Modules/Acesso/Application/AcessoEdicaoNegadoException.php`, `src/Modules/Acesso/Presentation/Http/CompetitionAccess.php` e `tests/Unit/Modules/Acesso/EdicaoAccessPolicyTest.php`.
Defeito reproduzido: o teste novo de T02 foi executado antes dos arquivos de aplicação e falhou em todos os 5 cenários com `Class "App\\Modules\\Acesso\\Application\\EdicaoAccessPolicy" not found`, 0 asserções e exit 1. O teste cobre perfis 0/1/2/3, edições iguais/diferentes/nulas/zero, mesário sem edição ativa, sessão obsoleta e construção do contexto HTTP a partir da sessão e da edição consultada no banco.
Arquivos alterados: `src/Modules/Acesso/Domain/ContextoOperador.php`, `src/Modules/Acesso/Application/EdicaoAccessPolicy.php`, `src/Modules/Acesso/Application/AcessoEdicaoNegadoException.php`, `src/Modules/Acesso/Presentation/Http/CompetitionAccess.php`, `tests/Unit/Modules/Acesso/EdicaoAccessPolicyTest.php` e este `STATUS.md`. Não houve alteração em migrações, banco de trabalho, aliases, filas, cache ou IDs de mutação.
Decisão aplicada: `ContextoOperador` é `readonly` e não lê sessão. `EdicaoAccessPolicy` recebe o contexto e a edição real do recurso; níveis 0/1 acessam edições existentes, nível 2 exige edição ativa não nula e igual, e demais níveis/IDs ausentes são rejeitados. `CompetitionAccess` permanece como adaptador HTTP: monta o contexto usando a sessão autenticada e `findActiveId()`, atualiza o vínculo legado do mesário e converte a exceção para o envelope HTTP 403 existente. A assinatura continua compatível durante a migração; a validação de recurso é opcional no adaptador até os consumidores de T03–T06 passarem a fornecer a edição persistida.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe vendor/bin/phpunit --configuration phpunit.xml tests/Unit/Modules/Acesso/EdicaoAccessPolicyTest.php` antes da correção: 5 erros, 0 asserções, classe ausente.
- O mesmo teste após a implementação, junto com `tests/Unit/Shared/Http/AccessGuardTest.php`: 11 testes e 27 asserções aprovados.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify` com `C:\xampp\php` no `PATH`: passou; PHPUnit 124 testes/1.560 asserções, PHPStan 182/182, CS Fixer 234/234 e lint sem erros.
- `C:\xampp\php\php.exe tests/run_all.php` com o servidor em `127.0.0.1:8110` e MariaDB descartável em `127.0.0.1:3308`: 253/253 asserções aprovadas, 0 falhas.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, `SGI_PHP_PATH=C:\xampp\php\php.exe`, `SGI_BASE_URL=http://127.0.0.1:8110/` e o mesmo banco isolado: 26/26 testes aprovados em 3,5 minutos; o global setup confirmou novamente 253/253 asserções HTTP.
- `git diff --check`: passou.
Evidência: a política autoriza os perfis administrativos em edição histórica, restringe o mesário à edição ativa consultada, rejeita edição inexistente/nula/zero e não depende de `$_SESSION` no núcleo de aplicação. A suíte HTTP e o navegador preservaram login/RBAC, rotas legadas, pontuação, partidas, artilharia, ocorrências, filas/mutações offline, chaveamento e contratos visuais.
Dependências ou limitações restantes: a aplicação gradual da política aos recursos persistidos, incluindo artilharia e ocorrências, permanece em T03; a revalidação transacional do estado da edição permanece nas tarefas posteriores previstas no plano. O banco de testes descartável continua em `test-results/mysql-t00`, fora do banco de trabalho.
Próximo passo exato: abrir `docs/plano-implementacao-luna/01-autorizacao.md`, ler somente T03 e as fontes necessárias, reproduzir primeiro o acesso de artilheiro/ocorrência de uma edição B com o mesário da edição A, então aplicar `EdicaoAccessPolicy` após a resolução do recurso e validar escrita zero, envelopes e sincronização antes de T04.

Tarefa: T03 — Aplicar a política à artilharia e às ocorrências
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Competicoes/Presentation/Http/ArtilheiroController.php`, `src/Modules/Competicoes/Application/ArtilheiroService.php`, `src/Modules/Competicoes/Domain/ArtilheiroRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliArtilheiroRepository.php`, `src/Modules/Disciplina/Presentation/Http/OcorrenciaController.php`, `src/Modules/Disciplina/Application/OcorrenciaService.php`, `src/Modules/Disciplina/Domain/OcorrenciaRepository.php`, `src/Modules/Disciplina/Infrastructure/MysqliOcorrenciaRepository.php`, `src/Modules/Disciplina/Infrastructure/MysqliOcorrenciaQueries.php`, `tests/Integration/MesarioResourceScopeTest.php` e `tests/run_all.php`.
Defeito reproduzido: antes da correção, `MesarioResourceScopeTest` criou A ativa e B inativa, autenticou o mesário e enviou um gol para atleta/jogo de B; o endpoint respondeu sucesso e a contagem de `artilheiros` aumentou em 1. Esse resultado foi registrado pelo runner como `Regressão T03: código anterior grava artilharia da edição B`. O mesmo teste, após a correção, exige 403 e contagem inalterada; também cobre ocorrência estruturada de B e confirma que não há INSERT.
Arquivos alterados: `src/Modules/Competicoes/Presentation/Http/ArtilheiroController.php`, `src/Modules/Competicoes/Application/ArtilheiroService.php`, `src/Modules/Competicoes/Domain/ArtilheiroRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliArtilheiroRepository.php`, `src/Modules/Disciplina/Presentation/Http/OcorrenciaController.php`, `src/Modules/Disciplina/Application/OcorrenciaService.php`, `src/Modules/Disciplina/Domain/OcorrenciaRepository.php`, `src/Modules/Disciplina/Infrastructure/MysqliOcorrenciaRepository.php`, `src/Modules/Disciplina/Infrastructure/MysqliOcorrenciaQueries.php`, `tests/Integration/MesarioResourceScopeTest.php`, `tests/Unit/Modules/Competicoes/ArtilheiroServiceTest.php`, `tests/Unit/Modules/Disciplina/OcorrenciaServiceTest.php`, `tests/run_all.php` e este `STATUS.md`. Não houve migração, alteração de banco de trabalho, mudança de aliases, filas, cache ou identificadores de mutação.
Decisão aplicada: a autorização ocorre depois da resolução de jogo temporário e dentro da transação iniciada por `MutationAction`. Artilharia consulta a edição real do jogo, a edição/papel do atleta e a participação em uma partida; níveis administrativos preservam o acesso histórico legado, enquanto atletas precisam pertencer à mesma edição e jogo. Ocorrências consultam o alvo persistido para PUT, validam usuário, jogo e turma por relações reais, preservam ocorrência sem jogo usando a edição do aluno e mantêm o 409 de jogo temporário não materializado. O PUT passou a usar a transação sem mudar os identificadores lógicos `artilheiro.post` e `ocorrencias.post`; respostas negadas retornam 403 antes de qualquer sucesso idempotente ser persistido.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` com a regressão ainda esperando o comportamento antigo: 254/254 asserções aprovadas; a nova asserção confirmou que o código anterior gravava artilharia em B.
- `C:\xampp\php\php.exe vendor/bin/phpunit --configuration phpunit.xml tests/Unit/Modules/Competicoes/ArtilheiroServiceTest.php tests/Unit/Modules/Disciplina/OcorrenciaServiceTest.php tests/Unit/Modules/Acesso/EdicaoAccessPolicyTest.php`: 12 testes/29 asserções aprovados.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify` com `C:\xampp\php` no `PATH`: passou; PHPUnit 124 testes/1.563 asserções, PHPStan 182/182, CS Fixer 234/234 e lint sem erros.
- `C:\xampp\php\php.exe tests/run_all.php` com a expectativa corrigida, fixture A/B e banco isolado: 255/255 asserções aprovadas, incluindo 403 e contagem inalterada para artilharia e ocorrência.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, `SGI_PHP_PATH=C:\xampp\php\php.exe`, `SGI_BASE_URL=http://127.0.0.1:8110/` e MariaDB descartável em `127.0.0.1:3308`: 26/26 testes aprovados em 3,6 minutos; o global setup confirmou 255/255 asserções HTTP.
- `git diff --check`: passou.
Evidência: o cenário cruzado devolve HTTP 403 para mesário, não aumenta `artilheiros` nem `ocorrencias`, e o runner preserva gols, cartões, reenvios idempotentes, cartão vermelho automático, jogo temporário pendente, torneio offline e rotas antigas. A última execução HTTP ficou em 255/255; a regressão visual/offline ficou em 26/26.
Dependências ou limitações restantes: a autorização de partidas e IDs discordantes está prevista em T04; resolução completa de resultados temporários fica em T05. A política ainda é aplicada gradualmente aos demais recursos conforme a ordem do plano.
Próximo passo exato: abrir `docs/plano-implementacao-luna/01-autorizacao.md`, ler somente T04 e as fontes necessárias, reproduzir primeiro uma partida de B acompanhada por jogo de A e confirmar que o código atual pode mover/atualizar a partida, então autorizar o alvo persistido e rejeitar qualquer combinação discordante antes da mutação.

Tarefa: T04 — Autorizar a partida persistida, sem aceitar IDs discordantes
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Competicoes/Presentation/Http/PartidaController.php`, `src/Modules/Competicoes/Application/PartidaService.php`, `src/Modules/Competicoes/Domain/PartidaRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php` e `tests/Integration/MesarioResourceScopeTest.php`.
Defeito reproduzido: antes da correção, a fixture criou A ativa e B inativa, autenticou o mesário e enviou PUT para uma partida persistida de B com `jogos_id_jogo` de A, equipe de B e placar 7. O runner passou com o comportamento antigo e registrou `Regressão T04: código anterior move partida B para jogo A`; a linha de B ficou reassociada ao jogo A. A regressão também foi preservada no teste de integração para exigir que a linha permaneça idêntica após a rejeição.
Arquivos alterados: `src/Modules/Competicoes/Presentation/Http/PartidaController.php`, `src/Modules/Competicoes/Application/PartidaService.php`, `src/Modules/Competicoes/Domain/PartidaRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `tests/Unit/Modules/Competicoes/PartidaServiceTest.php`, `tests/Integration/MesarioResourceScopeTest.php` e este `STATUS.md`. Não houve alteração em migrações, banco de trabalho, aliases, filas, cache, IDs de mutação ou no contrato de partidas temporárias.
Decisão aplicada: IDs positivos carregam a partida persistida e sua edição antes da autorização. A política usa essa edição real; um mesário não pode autorizar um registro de B apenas enviando um jogo de A. Depois da autorização, jogo/equipe informados são comparados com os vínculos persistidos e qualquer discordância retorna 422 antes da escrita. PUT sem jogo/equipe infere ambos do registro persistido. POST legado de resultado carrega todas as partidas do jogo, substitui somente a equipe alvo e encaminha o conjunto completo ao `launch`, mantendo a validação/transação única de placar, avanço e premiação. O caminho de ID temporário continua devolvendo a resposta offline legada sem transformar `mm_local_*` em ID real.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` com a expectativa de regressão ainda apontando para o comportamento anterior: 256/256 asserções aprovadas; o cenário confirmou que o código antigo movia a partida B para o jogo A.
- `C:\xampp\php\php.exe -l` nos cinco arquivos PHP alterados de aplicação e nos dois testes novos/alterados: nenhuma falha de sintaxe.
- `C:\xampp\php\php.exe tests/run_all.php` após a correção, com servidor em `127.0.0.1:8110` e MariaDB descartável em `127.0.0.1:3308`: 260/260 asserções aprovadas. A Suite 0.1 confirmou HTTP 403 para partida de B, HTTP 422 para equipe discordante em A, HTTP 404 para partida inexistente, PUT inferido com vínculo preservado e POST legado com placar completo/status `Concluido`.
- `composer verify` com PHP 8.2/MySQLi do XAMPP no `PATH`: PHPUnit 124 testes/1.563 asserções, PHPStan 182/182, lint PHP sem erros e CS Fixer 234/234; todos aprovados.
- `npm --prefix tests/browser test` inicialmente sem servidor escutando: falhou antes de executar a aplicação, com 18 erros `ECONNREFUSED` contra o endereço padrão e 8 testes não executados; não foi usado como evidência funcional.
- `tools/start-test-server.ps1 -Database sgi_test_luna -Port 8110 -PhpPath C:\xampp\php\php.exe`, com conexão para a instância descartável: servidor iniciado em `127.0.0.1:8110`.
- `npm --prefix tests/browser test` com `SGI_BASE_URL=http://127.0.0.1:8110/` e o mesmo banco isolado: 26/26 testes aprovados em 3,8 minutos, incluindo os fluxos online/offline do mesário, filas, chaveamento, rotas antigas e contratos visuais.
- `git diff --check`: passou.
Evidência: a partida de B não mudou seus três campos persistidos após a tentativa cruzada; a partida de A rejeitou equipe discordante sem escrita; o PUT mínimo atualizou apenas o resultado mantendo jogo/equipe; o POST finalizou com os dois placares e status `Concluido`. Os cenários offline existentes continuaram aprovados e nenhum ID temporário foi materializado por este endpoint.
Dependências ou limitações restantes: a resolução de jogos temporários e a sincronização entre resultado, artilharia, ocorrência e chaveamento continuam na T05. O servidor e o MariaDB usados nesta validação são descartáveis e permanecem fora do banco de trabalho.
Próximo passo exato: abrir `docs/plano-implementacao-luna/01-autorizacao.md`, ler somente a seção T05 e as fontes nela listadas — `ResultadoController.php`, `MysqliPartidaGateway::resolveGame/launch`, `MysqliArtilheiroQueries::resolveGame`, `MysqliOcorrenciaQueries::resolveGame`, `ChaveamentoSyncController.php`, `MysqliChaveamentoSyncGateway.php` e `ChaveamentoRules.php` —, reproduzir primeiro a resolução/sincronização de um jogo temporário e só então corrigir o defeito mantendo as tags, aliases, filas/cache e IDs antigos.

Tarefa: T05 — Validar resolução de jogos temporários e sincronização
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Competicoes/Application/JogoResolucaoAmbiguaException.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `src/Modules/Competicoes/Presentation/Http/ResultadoController.php`, `src/Modules/Competicoes/Infrastructure/MysqliArtilheiroQueries.php`, `src/Modules/Competicoes/Presentation/Http/ArtilheiroController.php`, `src/Modules/Disciplina/Infrastructure/MysqliOcorrenciaQueries.php`, `src/Modules/Disciplina/Presentation/Http/OcorrenciaController.php`, `src/Modules/Sincronizacao/Infrastructure/MysqliChaveamentoSyncGateway.php`, `src/Modules/Sincronizacao/Presentation/Http/ChaveamentoSyncController.php` e `tests/Integration/TemporaryResolutionScopeTest.php`.
Defeito reproduzido: antes da correção, `C:\xampp\php\php.exe tests/run_all.php` com a regressão ainda expressando o comportamento permissivo passou em 264/264 asserções e confirmou quatro problemas: resultado temporário da edição B alterava jogo autorizado para A; modalidade A aceitava equipes de B no fallback; fallback ambíguo escolhia o jogo mais recente; e sincronização gravava jogo antes de validar equipe fora da modalidade. A nova regressão foi então invertida para exigir rejeição, ausência de escrita e retry.
Arquivos alterados: `src/Modules/Competicoes/Application/JogoResolucaoAmbiguaException.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `src/Modules/Competicoes/Presentation/Http/ResultadoController.php`, `src/Modules/Competicoes/Infrastructure/MysqliArtilheiroQueries.php`, `src/Modules/Competicoes/Presentation/Http/ArtilheiroController.php`, `src/Modules/Disciplina/Infrastructure/MysqliOcorrenciaQueries.php`, `src/Modules/Disciplina/Presentation/Http/OcorrenciaController.php`, `src/Modules/Sincronizacao/Infrastructure/MysqliChaveamentoSyncGateway.php`, `src/Modules/Sincronizacao/Presentation/Http/ChaveamentoSyncController.php`, `tests/Integration/TemporaryResolutionScopeTest.php`, `tests/run_all.php` e este `STATUS.md`.
Decisão aplicada: a resolução separa inspeção do candidato, autorização da edição, validação de modalidade/equipes e materialização. IDs positivos carregam o jogo persistido; IDs negativos aceitam apenas modalidade existente e tag válida, procurando por modalidade e edição implícitas, sem inserir antes da validação. Fallback por equipes é limitado à modalidade/edição e retorna 409 quando ambíguo. Jogos agendados legados sem linhas em `partidas` continuam aceitos, mas as equipes submetidas são validadas antes de `garantirPartidaEquipe`. A sincronização valida todo o lote antes da primeira escrita e preserva tags, IDs temporários, aliases, filas/cache, corpos persistidos e identificadores de mutação. Artilharia e ocorrências temporárias deixam de resolver dados arbitrários e exigem modalidade/tag/edição autorizáveis.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` com a regressão reproduzindo o comportamento antigo: 264/264 asserções, com os quatro defeitos identificados.
- `C:\xampp\php\php.exe tests/run_all.php` após a correção, no MariaDB descartável `sgi_test_luna` em `127.0.0.1:3308`: 265/265 asserções aprovadas, 0 falhas. A Suite 0.2 confirmou HTTP 403 sem escrita para edição B, HTTP 422 sem jogo auxiliar para equipes incompatíveis, HTTP 409 para fallback ambíguo sem alteração e retry posterior bem-sucedido, além de HTTP 400 sem criação parcial na sincronização.
- `composer verify`: passou; PHPUnit 124 testes/1.571 asserções, PHPStan 183/183 sem erros, PHP CS Fixer 235/235 sem alterações e lint PHP sem erros.
- `npm run check`: passou, 39 arquivos JavaScript válidos.
- `npm test`: passou, 5/5 testes JavaScript.
- `npm --prefix tests/browser test` com `SGI_BASE_URL=http://127.0.0.1:8110/`: 26/26 testes aprovados em 3,4 minutos, incluindo fluxos online/offline, filas, torneio e contratos visuais.
- `git diff --check`: passou.
Evidência: o cenário A/B mantém o snapshot do jogo protegido e não cria partidas auxiliares; o cenário ambíguo preserva a mutação para retry e depois grava o jogo correto; a sincronização rejeita equipe fora da modalidade antes de qualquer `INSERT`; os fluxos existentes de artilharia, ocorrências, placar, chaveamento e torneio offline permaneceram verdes.
Dependências ou limitações restantes: T06 deve revisar os mapeamentos de exceções dos controladores e do middleware, sem transformar todo `RuntimeException` em 404 nem expor detalhes de persistência. Nenhuma migração foi criada ou reescrita; o banco de trabalho, aliases, filas/cache e IDs de mutação não foram alterados.
Próximo passo exato: abrir `docs/plano-implementacao-luna/01-autorizacao.md`, ler somente T06 e as fontes listadas — `UsuarioController.php`, `ResultadoController.php`, `HistoricoTurmaController.php`, `ExceptionMiddleware.php` e capturas amplas de `Presentation` —, reproduzir primeiro erro de validação, FK inválida, persistência simulada e autorização negada, então corrigir os envelopes sem prosseguir até o commit e registrar os resultados.

Tarefa: T06 — Padronizar falhas internas sem quebrar envelopes
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Acesso/Presentation/Http/UsuarioController.php`, `src/Modules/Competicoes/Application/JogoNaoEncontradoException.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `src/Modules/Competicoes/Presentation/Http/ResultadoController.php`, `src/Modules/Resultados/Application/TurmaHistoricoNaoEncontradaException.php`, `src/Modules/Resultados/Infrastructure/MysqliHistoricoTurmaRepository.php`, `src/Modules/Resultados/Presentation/Http/HistoricoTurmaController.php`, `src/Modules/Sincronizacao/Infrastructure/MysqliChaveamentoSyncGateway.php`, `src/Modules/Sincronizacao/Presentation/Http/ChaveamentoSyncController.php` e `tests/Integration/ExceptionEnvelopeTest.php`.
Defeito reproduzido: a nova Suite 0.3 foi executada antes da correção contra uma conexão somente de teste em `information_schema`, com as tabelas da aplicação ausentes. O código anterior produziu três falhas: `UsuarioController` devolvia o texto interno da exceção com envelope de erro e status 200; `ResultadoController` convertia `mysqli_sql_exception` em 404 e expunha a tabela ausente; `HistoricoTurmaController` fazia o mesmo. A execução registrou 269 asserções, 266 aprovadas e 3 falhas. O caso de validação 400 já permanecia correto.
Arquivos alterados: `src/Modules/Acesso/Presentation/Http/UsuarioController.php`, `src/Modules/Competicoes/Application/JogoNaoEncontradoException.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `src/Modules/Competicoes/Presentation/Http/ResultadoController.php`, `src/Modules/Resultados/Application/TurmaHistoricoNaoEncontradaException.php`, `src/Modules/Resultados/Infrastructure/MysqliHistoricoTurmaRepository.php`, `src/Modules/Resultados/Presentation/Http/HistoricoTurmaController.php`, `src/Modules/Sincronizacao/Infrastructure/MysqliChaveamentoSyncGateway.php`, `src/Modules/Sincronizacao/Presentation/Http/ChaveamentoSyncController.php`, `tests/Integration/ExceptionEnvelopeTest.php`, `tests/run_all.php` e este `STATUS.md`.
Decisão aplicada: exceções específicas representam jogo/modalidade/turma inexistentes e continuam em 404; `InvalidArgumentException` permanece como validação pública; autorização e conflitos mantêm seus mapeamentos existentes. `mysqli_sql_exception`, falhas genéricas de `RuntimeException` e demais `Throwable` são registrados no log e devolvem 500 com mensagem estável, sem SQL, constraint, caminho ou stack trace. Usuários preservam o envelope legado `status/mensagem`; resultado, histórico e sincronização preservam `success/message`. A sincronização passou a distinguir validação de persistência. `ExceptionMiddleware` já possuía envelope genérico e não precisou de alteração. Nenhuma captura prossegue até commit após falha.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da correção, com a Suite 0.3: 269 asserções, 3 falhas reproduzidas nos envelopes inseguros.
- `C:\xampp\php\php.exe tests/run_all.php` após a correção, no MariaDB descartável `sgi_test_luna` em `127.0.0.1:3308`: 269/269 asserções aprovadas, 0 falhas. A Suite 0.3 confirmou 500 genérico sem texto interno para usuários, resultado e histórico, além de validação 400; as suítes anteriores permaneceram verdes.
- `composer verify`: passou; PHPUnit 124 testes/1.584 asserções, PHPStan 185/185 sem erros, PHP CS Fixer 237/237 sem alterações e lint PHP sem erros.
- `npm run check`: passou, 39 arquivos JavaScript válidos.
- `npm test`: passou, 5/5 testes JavaScript.
- `npm --prefix tests/browser test` com `SGI_BASE_URL=http://127.0.0.1:8110/`: 26/26 testes aprovados em 3,5 minutos, incluindo autenticação, RBAC, mesário online/offline, torneio e contratos visuais.
- `git diff --check`: passou.
Evidência: respostas de persistência simulada têm status 500 e mensagens públicas fixas; o corpo não contém `information_schema`, nomes de tabelas ou a exceção original. O histórico inexistente em banco válido continua 404, enquanto validação continua 400. Os cenários positivos de login, usuários, placar, artilharia, ocorrências, sincronização, filas e torneio permanecem aprovados.
Dependências ou limitações restantes: a regra de cronômetro ainda está espalhada entre PHP/JavaScript e será tratada em T07–T10. Nenhuma migração foi criada ou reescrita; banco de trabalho, aliases, filas/cache e IDs de mutação não foram alterados.
Próximo passo exato: abrir `docs/plano-implementacao-luna/02-placar-offline.md`, ler somente T07 e as fontes necessárias, reproduzir primeiro os casos de transição/limite do cronômetro indicados no plano e só então extrair a regra pura, mantendo o contrato do estado existente.

Tarefa: T08 — Aplicar o contrato no PHP e proteger o replay
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Competicoes/Application/CronometroService.php`, `src/Modules/Competicoes/Domain/CronometroRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliCronometroRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliJogoGateway.php`, `src/Modules/Competicoes/Presentation/Http/JogoController.php`, `config/routes.php`, `tests/Integration/CronometroPersistenceTest.php`, `tests/run_all.php` e este `STATUS.md`.
Defeito reproduzido: antes da correção, `C:\xampp\php\php.exe tests/run_all.php` com a suíte T08 recém-registrada passou nas 269 asserções anteriores, mas falhou nas 7 novas: pausa não persistia 1.170 segundos, snapshot v2 era ignorado, o GET recalculava da duração 1.200 em vez do saldo persistido, retomar/pausar podia recuperar tempo, a mutação não tinha replay idempotente, a mesma chave com corpo diferente não era rejeitada e snapshot negativo era aceito. O primeiro comando de navegador foi interrompido pelo setup por ausência de `SGI_TEST_BASE_URL` antes de executar testes; ele não é evidência funcional nem alterou dados.
Arquivos alterados: `src/Modules/Competicoes/Application/CronometroService.php`, `src/Modules/Competicoes/Domain/CronometroRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliCronometroRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliJogoGateway.php`, `src/Modules/Competicoes/Presentation/Http/JogoController.php`, `config/routes.php`, `tests/Integration/CronometroPersistenceTest.php`, `tests/run_all.php` e este `STATUS.md`. Não houve migração, alteração do banco de trabalho, mudança de aliases, filas, cache ou identificadores de mutação existentes.
Decisão aplicada: o serviço carrega o jogo com `FOR UPDATE`, usa `CronometroRules` com relógio injetável e salva o snapshot completo dentro da transação iniciada por `MutationAction`. O PUT legado que envia somente status continua aceito; campos legados de duração/extra são incorporados antes da primeira transição para suportar jogos antigos sem duração. O bloco opcional `cronometro.versao=2` valida saldo e referência epoch em milissegundos, congela saldo para pausa/conclusão e desconta somente o intervalo entre referência e recepção para jogo iniciado. O adaptador grava a referência com `FROM_UNIXTIME` e lê com `UNIX_TIMESTAMP`, mantendo a convenção de `DATETIME` do banco sem interpretar o horário local do PHP. O GET usa a mesma regra sobre o saldo persistido, expõe `servidor_epoch_ms` e preserva fallback seguro para registros legados sem duração. A nova chave estável `jogos.put` passa pela loja idempotente; reenvio devolve o envelope salvo e corpo diferente retorna 409. IDs negativos continuam resposta offline local sem materialização falsa.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da correção, com a suíte T08 registrada: 276 asserções, 269 aprovadas e 7 falhas reproduzidas nos defeitos acima.
- `C:\xampp\php\php.exe tests/run_all.php` após a correção, no MariaDB descartável `sgi_test_luna` em `127.0.0.1:3308`: 277/277 asserções aprovadas. A Suite 6.1 confirmou pausa/retomada sem recuperação, snapshot atrasado, zero e saldo persistido, replay idempotente, conflito de corpo, rejeição sem escrita e rollback do cronômetro quando a confirmação idempotente falha.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit 133 testes/1.637 asserções, PHPStan 189/189, CS Fixer 242/242 e lint PHP sem erros.
- `npm --prefix tests/browser test` com `SGI_TEST_BASE_URL=http://127.0.0.1:8110/`, `SGI_BASE_URL=http://127.0.0.1:8110/` e MariaDB descartável: 26/26 testes aprovados em 3,8 minutos, incluindo autenticação, RBAC, mesário online/offline, filas, torneio, sincronização e contratos visuais.
- `npm run check`: 39 arquivos JavaScript válidos.
- `npm test`: 5/5 testes JavaScript aprovados.
- `git diff --check`: passou; os avisos restantes são apenas de conversão LF/CRLF do Git em cópias de trabalho.
Evidência: o banco isolado persistiu 1.170 segundos após 30 segundos de execução, calculou 1.160 após mais 10 segundos, manteve o valor ao pausar, gravou uma única mutação `jogos.put` e reverteu a alteração quando a confirmação falhou. O navegador confirmou que os fluxos antigos e offline continuam operando e que jogos temporários permanecem locais até resultado/materialização.
Dependências ou limitações restantes: T08 não altera registros históricos corrompidos pela pausa antiga e não tenta inventar tempo consumido. A projeção local e o snapshot v2 do cliente foram alinhados em T09; aliases, filas/cache e IDs antigos permanecem compatíveis.
Próximo passo exato: abrir `docs/plano-implementacao-luna/02-placar-offline.md`, ler somente T10 e as fontes nela listadas — `placar.js` (`agendarSalvarPartida`, `salvarPartida`, `ajustarGols`, `finalizarJogo` e callbacks de limpeza), `offline-core.js` (`queueMutation`, `syncQueue`) e `page-runtime.js` —, reproduzir primeiro clique + seguido de navegação imediata antes de 400 ms, verificar a fila em ordem e a projeção no IndexedDB, então corrigir a persistência sem criar outro outbox nem alterar corpos antigos.

Tarefa: T12 — Corrigir a revalorização sem apagar esporte ou ajustes
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `database/migrations/003_fix_arrecadacao_revaluation.sql`, `src/Modules/Resultados/Domain/PontuacaoRules.php`, `src/Modules/Resultados/Infrastructure/MysqliArrecadacaoRepository.php`, `tests/Unit/Modules/Resultados/PontuacaoRulesTest.php`, `tests/Integration/PontuacaoReconciliationTest.php`, `tests/run_all.php` e este `STATUS.md`.
Defeito reproduzido: antes da correção, o cenário integrado foi executado contra a implementação vigente com V=2, Q=10, bruto=30 e 10 pontos esportivos; ao alterar V para 3, o resultado encontrado permaneceu 30 em vez de 40. O teste terminou com 279 asserções, 278 aprovadas e 1 falha: `Revalorização preserva pontos esportivos ... -> Pontos encontrados: 30`.
Arquivos alterados: a nova migração `003_fix_arrecadacao_revaluation.sql`, a regra pura `PontuacaoRules.php`, os arredondamentos de adição/histórico em `MysqliArrecadacaoRepository.php`, o teste unitário, o teste integrado, o registro no runner e este `STATUS.md`. As migrações 001 e 002 permaneceram sem alteração; nenhum banco de trabalho foi usado.
Decisão aplicada: a migração substitui apenas `tr_atualiza_pontos_arrecadacao`; a atualização calcula `pontuacao_turma - ROUND(Q*OLD.V, 0) + ROUND(Q*NEW.V, 0)`, portanto preserva pódio, esporte e ajustes existentes, sem atualizar `interclasses` no próprio trigger. A regra pura usa a mesma fórmula de arredondamento inteiro único para revalorização, adição, remoção e histórico, aceita quantidades inteiras ou com até duas casas e rejeita precisão maior. O fluxo legado, aliases, filas/cache, IDs de mutação e MySQL/MariaDB foram preservados.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da implementação da migração/correção: 279 asserções, 278 aprovadas e 1 falha reproduzida.
- `C:\xampp\php\php.exe vendor/bin/phpunit --configuration phpunit.xml tests/Unit/Modules/Resultados/PontuacaoRulesTest.php`: 5 testes/10 asserções aprovados.
- `C:\xampp\php\php.exe tests/run_all.php` após a correção, em MariaDB descartável `127.0.0.1:3308`: 284/284 asserções aprovadas; aplicação e repetição da migração, baseline e checksums passaram.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit 138 testes/1.658 asserções, PHPStan sem erros, PHP CS Fixer sem alterações pendentes e lint PHP sem erros.
- `npm test`: passou; 16/16 testes JavaScript.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm run build`: passou; 231 assets preparados.
- `npm --prefix tests/browser test` com o servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: 29/29 testes de navegador aprovados em 4,1 minutos; o global setup confirmou 284/284 asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: V2→V3 converte 30 em 40, o retorno V3→V2 restaura 30, J5 preserva o ajuste e resulta em 45, Q0 não altera pontos esportivos/ajustes e 1,25 item usa arredondamento único (5→6). A instalação nova, atualização sobre 001/002 e repetição da migração passaram, sem reescrever 001/002.
Dependências ou limitações restantes: T13 deve tratar registro e estorno da arrecadação mantendo a mesma regra de pontuação e os históricos existentes. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/03-pontuacao.md`, ler somente T13 e as fontes necessárias — `MysqliArrecadacaoRepository.php`, `ArrecadacaoService.php`, rotas/controladores de arrecadação e testes HTTP —, reproduzir primeiro o registro e o estorno duplicável/sem histórico descrito na etapa, então corrigir e validar a consistência do bruto, pontos adicionados e histórico antes de seguir para T14.

Tarefa: T13 — Adicionar e estornar por diferença, sob trava
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Resultados/Infrastructure/MysqliArrecadacaoRepository.php`, `src/Modules/Resultados/Application/ArrecadacaoService.php`, `src/Modules/Resultados/Domain/PontuacaoRules.php`, `src/Modules/Resultados/Domain/ArrecadacaoQuantidadeInsuficienteException.php`, `src/Modules/Resultados/Presentation/Http/ArrecadacaoController.php`, `tests/Integration/ArrecadacaoConsistencyTest.php`, `tests/Support/ConcurrentScenarioWorker.php`, `tests/run_all.php` e este `STATUS.md`.
Defeitos reproduzidos: antes da correção, o teste integrado registrou Q=10 a V=2, alterou V para 3 e estornou; a turma terminou com 40 em vez de 30, deixando 10 pontos indevidos. O mesmo teste iniciou dois workers contra o mesmo histórico; ambos retornaram `removed` e o banco terminou com quantidade `-10.00` e pontos 10. A observação de trava foi então incorporada ao helper para comprovar que os dois workers alcançavam a consulta `FOR UPDATE` antes da liberação.
Arquivos alterados: `MysqliArrecadacaoRepository.php`, `ArrecadacaoService.php`, `PontuacaoRules.php`, a nova exceção de quantidade insuficiente, `ArrecadacaoController.php`, o teste integrado, o worker concorrente, o registro no runner e este `STATUS.md`. Não houve migração nova, alteração de 001/002/003, mudança de aliases, filas/cache, IDs de mutação ou banco de trabalho.
Decisão aplicada: adição e remoção usam `Transaction` aninhável e a ordem edição → turmas por ID crescente → histórico. A adição trava e relê Q/V, calcula `D(Q+q,V)-D(Q,V)`, grava Q, bruto e histórico na mesma transação e conserva `pontos_adicionados` como o delta efetivo, inclusive zero. A remoção descobre a turma apenas para localizar a trava, relê histórico ativo com `FOR UPDATE`, valida edição/status/quantidade, calcula `D(Q-q,V)-D(Q,V)` no valor atual, marca o histórico com predicado ativo e só confirma o desconto se a transição for efetiva. Estorno repetido retorna a resposta legada sem novo desconto; lote inválido sofre rollback integral; quantidade insuficiente gera erro de negócio sem `GREATEST`; fracionários usam a regra inteira única.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da correção: 299 asserções, 295 aprovadas e 4 falhas reproduzidas — revalorização/estorno e concorrência.
- `C:\xampp\php\php.exe tests/run_all.php` após a correção, em MariaDB descartável `127.0.0.1:3308`: 299/299 asserções aprovadas, incluindo dois workers concorrentes observados na trava.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit 138 testes/1.669 asserções, PHPStan 191/191, PHP CS Fixer 245/245 e lint PHP sem erros.
- `npm test`: passou; 16/16 testes JavaScript.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm run build`: passou; 231 assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: 29/29 testes de navegador aprovados em 4,0 minutos; o global setup confirmou 299/299 asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: V2→V3→estorno retorna o bruto esportivo, segundo estorno não modifica o banco, lote com turma inválida não deixa registro parcial, o segundo fracionário tem delta/pontos zero sem perder quantidade e dois processos produzem exatamente um `removed` e um `already_removed`.
Dependências ou limitações restantes: T14 deve separar créditos de pódio da adoção histórica e manter o mesmo bruto de arrecadação e os estornos já corrigidos. O helper concorrente mínimo criado aqui deve ser reutilizado/ampliado em T18, sem solução duplicada.
Próximo passo exato: abrir `docs/plano-implementacao-luna/03-pontuacao.md`, ler somente T14 e as fontes necessárias — `ClassificacaoService.php`, repositórios/consultas de classificação, `MysqliHistoricoTurmaRepository.php`, histórico e testes de pódio —, reproduzir primeiro a alteração de pódio que duplica ou apaga créditos históricos, então corrigir a adoção sem recalcular registros antigos nem mexer no bruto de arrecadação.

Tarefa: T14 — Registrar o crédito do pódio e tratar o legado explicitamente
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `database/migrations/004_podio_credit_sources.sql`, `src/Modules/Resultados/Domain/PodioRepository.php`, `src/Modules/Resultados/Domain/PodioRules.php`, `src/Modules/Resultados/Infrastructure/MysqliPodioRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `src/Modules/Competicoes/Infrastructure/MysqliIndividualRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliChaveamentoRepository.php`, `bin/sgi.php`, `tests/Unit/Modules/Resultados/PodioRulesTest.php`, `tests/Integration/PodiumCreditTest.php`, `tests/browser/occurrence-offline-edit.spec.cjs`, `tests/run_all.php` e este `STATUS.md`.
Defeito reproduzido: antes da correção, uma final mata-mata já concluída não tinha linha de origem em `pontuacoes_podio`; sua retificação deixou a pontuação das turmas em 20/12, quando a troca de vencedor deveria resultar em 12/20. A primeira execução HTTP do cenário também expôs o erro de passagem por referência em `chaveamentoRebuildFromRound`; o defeito foi reproduzido antes do ajuste incidental da variável local. Durante a validação final, o navegador reproduziu uma falha de fixture porque a data UTC da ocorrência (08/09) não coincidia com a data local persistida do jogo (07/09); a aplicação estava filtrando corretamente, e o fixture foi corrigido para usar `jogo.data_jogo`.
Arquivos alterados: nova migração `004_podio_credit_sources.sql`; `PodioRepository.php`, `PodioRules.php`, `MysqliPodioRepository.php`; os gateways de partida/individual; correção de variável por referência em `MysqliChaveamentoRepository.php`; comando `pontuacao:diagnosticar` e adoção em `bin/sgi.php`; testes unitário/integrado, runner, fixture de navegador e este `STATUS.md`. As migrações 001, 002 e 003 permaneceram sem alteração; nenhum banco de trabalho foi usado.
Decisão aplicada: cada edição/modalidade/posição tem uma única fonte em `pontuacoes_podio`, inclusive crédito zero, com turma, equipe/usuário/jogo opcionais e origem `novo` ou `legado_conferido`. A primeira conclusão grava o crédito configurado e calcula somente o delta entre posições antigas e novas; uma retificação reutiliza o valor já concedido, sem ler configuração nova nem duplicar pontos. Mata-mata e individual usam a mesma regra pura agregada por turma. O diagnóstico lista pódios concluídos sem origem, referências órfãs e origens incompatíveis sem alterar dados. A adoção recebe arquivo explícito, valida edição/modalidade/turma/relações/jogo, registra o valor já concedido sem somá-lo, aceita repetição idêntica como no-op e rejeita conflito dentro de transação. O comando e a migração preservam MySQL/MariaDB, aliases, filas/cache, IDs de mutação e não removem crédito antes de estorno.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da implementação do crédito de origem: a retificação reproduziu pontuação sem movimentação de crédito e a primeira passagem HTTP do cenário retornou erro 500 no rebuild; a expectativa de reconciliação falhou.
- `C:\xampp\php\php.exe tests/run_all.php` após a implementação em MariaDB descartável `127.0.0.1:3308`: 310/310 asserções aprovadas, incluindo final mata-mata, retificação individual, adoção zero, repetição idempotente e conflito sem escrita parcial.
- `C:\xampp\php\php.exe bin/sgi.php pontuacao:diagnosticar` com `SGI_DB_NAME=sgi_test_luna`: JSON retornado com `podios_sem_origem: []`, `creditos_orfaos: []` e `diferenças: []`; nenhuma alteração foi feita.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit 141 testes/1.691 asserções, PHPStan 194/194, PHP CS Fixer 249/249 e lint PHP sem erros.
- `npm test`: passou; 16/16 testes JavaScript.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm run build`: passou; 231 assets preparados.
- `npm --prefix tests/browser test -- occurrence-offline-edit.spec.cjs` antes do ajuste do fixture: reproduziu lista vazia por divergência de data UTC/local; após o ajuste: 1/1 aprovado com reset isolado e sincronização real.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: 29/29 testes aprovados em 4,1 minutos; o global setup confirmou 310/310 asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: a tabela mantém uma linha única por posição; a retificação troca exatamente os créditos 1º/2º e preserva configurações históricas, enquanto o ranking individual grava três posições e retifica sem duplicação. A adoção de valor zero deixa o bruto inalterado, a repetição conserva uma linha e o conflito não altera pontos nem fonte. A migração foi aplicada e repetida pela suíte sem reescrever as anteriores.
Dependências ou limitações restantes: a correção específica da retificação de pódio mata-mata e suas regras de reconstrução continua sendo a T15; créditos históricos sem arquivo explícito permanecem apenas diagnosticados até adoção autorizada. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/03-pontuacao.md`, ler somente T15 e as fontes nela listadas, reproduzir primeiro a retificação mata-mata indicada, então corrigir sua reconstrução sem recalcular créditos legados nem tocar no bruto da arrecadação.

Tarefa: T15 — Corrigir pódio mata-mata por substituição de crédito
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `src/Modules/Competicoes/Infrastructure/MysqliChaveamentoRepository.php`, `src/Modules/Resultados/Domain/PodioRepository.php`, `src/Modules/Resultados/Infrastructure/MysqliPodioRepository.php`, `tests/Integration/PodiumCreditTest.php`, `tests/run_all.php` e este `STATUS.md`.
Defeito reproduzido: a correção de uma semifinal concluída reconstruía a final, mas deixava ativos os créditos da final/terceiro lugar antigos; a pontuação permaneceu duplicada quando a nova final foi concluída. O cenário inicial falhou em `Correção de semifinal invalida o crédito do pódio antigo` e `Nova final concede o pódio sem duplicar créditos`, com 312 asserções, 310 aprovadas e 2 falhas. A primeira execução também reproduziu dois `bind_param` com expressões não passadas por referência no caminho de reconstrução; as variáveis locais foram corrigidas para permitir que a regressão de negócio fosse alcançada.
Arquivos alterados: `MysqliPartidaGateway.php` agora bloqueia o jogo, trata final e disputa de terceiro na mesma operação, reconcilia créditos após rebuild e preserva savepoint da transação; `MysqliChaveamentoRepository.php` recebeu as variáveis locais necessárias para MariaDB/PHP 8.2; `PodioRepository.php` e `MysqliPodioRepository.php` invalidam fontes sem origem atual e aplicam o delta negativo; `PodiumCreditTest.php` cobre final, semifinal, final recriada, terceiro lugar, configuração alterada, repetição, pódio não conciliado e adoção. Não houve migração nova ou reescrita, nem alteração de banco de trabalho, aliases, filas/cache ou IDs de mutação.
Decisão aplicada: a retificação mantém o fluxo `MutationAction`/`Transaction` e não faz commit interno. O jogo é bloqueado antes da alteração; a substituição de posições usa os créditos existentes, e uma fonte `novo` que passa a apontar para jogo removido/não concluído/incompatível é marcada inativa, subtraindo seus pontos exatamente uma vez. Fontes `legado_conferido` sem jogo permanecem efetivas; quando a nova fase conclui, seus valores já concedidos são reutilizados. Final concede posições 1/2, `POS:3` concede somente 3º, sem substrings aproximadas; repetir a classificação gera delta zero. A correção de semifinal/oitava reconstrói a chave, remove créditos downstream inválidos e a nova final pode ser concluída sem duplicação. A disputa de terceiro é retificada independentemente e pódio não conciliado falha antes de confirmar placar/crédito.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da correção T15, após os ajustes incidentais de referência: 312 asserções, 310 aprovadas e 2 falhas reproduzidas — créditos antigos não invalidados e nova final duplicando o efeito.
- `C:\xampp\php\php.exe tests/run_all.php` após a correção, no MariaDB descartável `127.0.0.1:3308`: 317/317 asserções aprovadas; incluiu configuração alterada após premiação, repetição delta zero, semifinal→final, terceiro lugar e rollback de pódio não conciliado.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit 141 testes/1.691 asserções, PHPStan 194/194, PHP CS Fixer 249/249 e lint PHP sem erros.
- `npm test`: passou; 16/16 testes JavaScript.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm run build`: passou; 231 assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: 29/29 testes aprovados em 4,1 minutos; o global setup confirmou 317/317 asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: a retificação de semifinal retira os créditos ativos e a nova final repõe somente as posições válidas; 10/7 permanece 7/10, terceiro lugar muda sem afetar campeão/vice, configuração posterior não altera valores já concedidos, repetição mantém o bruto e ausência de conciliação reverte placar e crédito. Savepoints e a resposta idempotente continuam dentro da mesma transação externa.
Dependências ou limitações restantes: T16 deve consolidar o fluxo de modalidades individuais e a sincronização desses créditos sem introduzir uma segunda fila ou alterar os contratos de mutação. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/03-pontuacao.md`, ler somente T16 e as fontes nela listadas, reproduzir primeiro a duplicidade/ausência de crédito individual na sincronização indicada e então corrigir usando as fontes de pódio já existentes.

Tarefa: T16 — Aplicar a mesma pontuação ao individual e ao lote offline
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Competicoes/Infrastructure/MysqliIndividualRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `src/Modules/Sincronizacao/Infrastructure/MysqliChaveamentoSyncGateway.php`, `src/Modules/Competicoes/Infrastructure/MysqliChaveamentoManagement.php`, `tests/Integration/IndividualSyncCreditTest.php`, `tests/Integration/PodiumCreditTest.php`, `tests/run_all.php`, `resources/js/pages/eventos/configurar-pontuacao.js`, `resources/views/pages/eventos/configurar-pontuacao.php` e este `STATUS.md`.
Defeito reproduzido: antes da correção, o teste do lote recebeu uma final mata-mata já concluída, desativou os créditos para simular a divergência local e executou a sincronização; o caminho atualizava partidas/avanço, mas não reaplicava a fonte de pódio, deixando a pontuação reduzida e zero créditos ativos. A reprodução ficou em 321 asserções, 320 aprovadas e 1 falha. Na validação de navegador também foi reproduzida uma corrida independente na tela de pontuação: depois de preencher 25/15/10/5, o carregamento assíncrono podia sobrescrever o primeiro campo com 10 antes do POST, e a recarga falhou com `Expected 25, Received 10`; 25 testes passaram e 3 não foram executados por causa dessa falha.
Arquivos alterados: `MysqliIndividualRepository.php` agora resolve e valida os três atletas/equipes antes de apagar partidas, carrega créditos anteriores antes da substituição e reutiliza a fonte para produzir apenas deltas; `MysqliPartidaGateway.php` expõe a reconciliação de um jogo concluído; `MysqliChaveamentoSyncGateway.php` reconcilia cada jogo concluído dentro da mesma transação do lote antes do avanço; `IndividualSyncCreditTest.php` cobre repetição, troca 1º/2º, três atletas distintos, rejeição sem escrita parcial e lote mata-mata; `tests/run_all.php` registra a suíte. A tela `configurar-pontuacao.js`/view foi protegida contra a corrida de carregamento, mantendo controles desabilitados até os valores persistidos chegarem. Não houve migração nova, alteração de banco de trabalho, mudança de aliases, filas/cache ou IDs de mutação.
Decisão aplicada: POST individual e sincronização usam `MysqliIndividualRepository::salvarRanking` e `MysqliPodioRepository`/`PodioRules` como única reconciliação; a origem estável por modalidade/posição faz reenvios produzirem delta zero. Os três atletas precisam ser distintos, inscritos e possuir equipe ativa da modalidade, e dois atletas da mesma turma continuam válidos. O lote `mata_mata` atualiza estado, reconcilia pódio e processa avanço sob a mesma `Transaction`; a operação permanece compatível com envelopes, ações e chaves de idempotência existentes. A tela administrativa não envia valores padrão enquanto a leitura da edição está pendente.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da correção: 321 asserções, 320 aprovadas e 1 falha em `Lote mata-mata reconcilia o pódio sem reaplicar crédito`; a pontuação não era restaurada e não havia fonte ativa.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou após a validação prévia; PHPUnit 141 testes/1.694 asserções, PHPStan 194/194, PHP CS Fixer 249/249 e lint PHP sem erros.
- `npm test`: passou; 16/16 testes JavaScript.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm run build`: passou; 231 assets preparados.
- `C:\xampp\php\php.exe tests/run_all.php` após as correções, com MariaDB descartável em `127.0.0.1:3308`: 321/321 asserções aprovadas, incluindo troca individual, repetição idempotente, rollback do lote inválido e reconciliação mata-mata.
- `npm --prefix tests/browser test` na primeira validação reproduziu a corrida administrativa acima; após desabilitar os controles durante o carregamento, e com `test-results/sessions` preparado para a sessão descartável, a execução final passou com 29/29 testes em 4,1 minutos e o global setup confirmou 321/321 asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: o lote repetido individual mantém três linhas e delta zero; a troca de 1º/2º move os pontos sem duplicá-los; ranking inválido não deixa escrita parcial; a sincronização da final restaura os dois créditos ativos sem reaplicar valores; e o navegador confirma persistência 25/15/10/5 após recarga, além dos fluxos offline de placar, ocorrências e torneio completo.
Dependências ou limitações restantes: T17 deve verificar a composição de histórico/ranking e a separação entre pontos esportivos, arrecadação e ajustes, sem recalcular fontes de pódio já conciliadas. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/03-pontuacao.md`, ler somente T17 e as fontes nela listadas, reproduzir primeiro a divergência de histórico/ranking/ajuste indicada e então corrigir mantendo as fontes de crédito e os valores históricos.

Tarefa: T17 — Reconciliar histórico, ranking e ajustes
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Resultados/Infrastructure/MysqliHistoricoTurmaRepository.php`, `src/Modules/Resultados/Infrastructure/MysqliRankingRepository.php`, `src/Modules/Resultados/Application/RankingService.php`, `src/Modules/Resultados/Domain/PontuacaoRules.php`, `resources/js/pages/resultados/ranking.js`, `resources/js/pages/aluno/ranking.js`, `tests/Integration/HistoryRankingReconciliationTest.php`, `tests/Unit/Modules/Resultados/PontuacaoRulesTest.php`, `tests/run_all.php` e este `STATUS.md`.
Defeito reproduzido: antes da correção, o cenário isolado registrou 10 itens de arrecadação com valor atual 3, crédito esportivo ativo 10, bruto legado 45 e penalidade 3. O histórico/ranking falhou em separar os componentes: 5 asserções de crédito esportivo, modalidade/posição, ajuste residual, líquido e campos do ranking falharam, embora a doação corrente já fosse lida corretamente; o runner ficou em 327 asserções, 322 aprovadas e 5 falhas. O teste também preservou a exigência de não alterar registros históricos nem reparar banco existente automaticamente.
Arquivos alterados: `MysqliHistoricoTurmaRepository.php` passou a calcular D com a quantidade e o valor atuais, manter `pontos_adicionados` histórico, consultar somente fontes de pódio ativas por edição/turma, agrupar modalidades/fontes e expor bruto, esporte, ajuste, penalidade e líquido; `MysqliRankingRepository.php` passou a retornar os mesmos componentes com penalidades escopadas por edição; `PontuacaoRules.php` centralizou ajuste e líquido; `RankingService.php` foi mantido como contrato de aplicação; os dois consumidores JavaScript passaram a exibir pontuação bruta e líquida e a sinalizar saldo legado pendente; os testes de integração/unidade e o runner foram registrados. Não houve migração, alteração do banco de trabalho, publicação, mudança de aliases, filas/cache ou IDs de mutação.
Decisão aplicada: a fonte de verdade do esporte é `pontuacoes_podio`; a arrecadação exibida é D(Q,V) com o valor vigente, enquanto o histórico continua mostrando o valor registrado na época. O saldo J é `bruto - D - S`, sem transformar penalidade em esporte e sem forçar J a zero; o líquido é `bruto - penalidades`. O campo legado `turma.pontuacao_turma` continua sendo bruto para preservar compatibilidade. Quando J é diferente de zero, o histórico o identifica como saldo legado sem origem detalhada e pendente, sem adoção ou reparo automático. Admin e aluno consomem a mesma semântica de bruto/líquido.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da correção: 327 asserções, 322 aprovadas e 5 falhas nos cenários de crédito esportivo, modalidade/posição, ajuste, líquido e ranking.
- `C:\xampp\php\php.exe tests/run_all.php` após as correções, com MariaDB descartável em `127.0.0.1:3308`: 327/327 asserções aprovadas, incluindo a tabela D/S/J/P (80/0/0/10, 30/10/0/3, 30/10/5/3 e 0/17/0/0) e o cenário bruto 45, D30, S10, J5, penalidade 3, líquido 42.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit 142 testes/1.705 asserções, PHPStan 194/194, PHP CS Fixer 249/249 e lint PHP sem erros.
- `npm test`: passou; 16/16 testes JavaScript.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm run build`: passou; 231 assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: 29/29 testes aprovados em 4,1 minutos; o global setup confirmou 327/327 asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: o histórico agora apresenta a modalidade e a posição da fonte ativa, o ranking não confunde bruto com líquido, a penalidade não aumenta o esporte e o saldo legado fica explicitamente pendente. Os consumidores administrativo e do aluno exibem a mesma pontuação líquida e preservam o bruto registrado.
Dependências ou limitações restantes: T18 deve exercitar concorrência determinística sem alterar as fontes de pontuação, o bruto legado, as filas/cache ou os identificadores existentes. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/03-pontuacao.md`, ler somente T18 e as fontes nela listadas, reproduzir primeiro o cenário concorrente indicado com o teste existente, então corrigir a serialização/retentativa na menor fronteira e registrar novamente os resultados neste `STATUS.md`.

Tarefa: T18 — Criar testes concorrentes que realmente concorram
Estado: concluída; infraestrutura e matriz de invariantes aceitas após T19 e T20.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `tests/Support/ConcurrentScenarioWorker.php`, `tests/Integration/ConcurrentInvariantsTest.php`, `tests/Integration/ArrecadacaoConsistencyTest.php`, `tests/Integration/AtomicMutationTest.php`, `tests/Support/MutationWorker.php`, `tests/Support/TestDatabase.php`, `src/Shared/Database/Transaction.php`, `tests/run_all.php` e este `STATUS.md`.
Defeito reproduzido: a primeira execução do novo teste detectou que o diagnóstico não contava o estado `FOR UPDATE` do MariaDB e a leitura final abriu uma conexão sem banco selecionado; esses defeitos do suporte foram corrigidos e reproduzidos novamente. A execução funcional seguinte, com dois processos PHP distintos e fixture sintética, chegou a 336 asserções, 332 aprovadas e 4 falhas: a inscrição não confirmou a espera explícita na trava do aluno, e duas criações aceitaram a última vaga, deixando 3 equipes para `max_equipes=2`. O estorno concorrente já confirmou dois workers esperando, uma remoção efetiva e quantidade/pontos descontados uma vez; a chave de mutação repetida continuou coberta pelo teste existente.
Arquivos alterados: `ConcurrentScenarioWorker.php` preserva a chamada legada de T13 e ganhou cenários coordenados de arrecadação, inscrição e equipe, anúncio por arquivo, liberação por barreira e retorno estruturado; `ConcurrentInvariantsTest.php` cria/limpa fixtures próprias, mantém trava no pai, observa `INNODB_LOCK_WAITS`/`INNODB_TRX`/`PROCESSLIST` e lê o estado final em nova conexão; `tests/run_all.php` registra o cenário. Nenhum hook HTTP, bypass de CSRF, sleep de produção, migração, alias, fila/cache, ID de mutação ou banco de trabalho foi alterado.
Decisão aplicada: cada worker usa `PHP_BINARY` e conexão própria, anuncia pronto antes da liberação e falha por timeout/exit code; uma recusa de regra é resultado estruturado, não aprovação de falha de processo. O pai mantém a trava de linha em transação e só libera depois de observar os dois participantes; o helper sempre tenta liberar/encerrar workers e remover a barreira em `finally`. O cenário de arrecadação reutiliza a técnica já adotada em T13, sem criar segunda implementação.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe -l tests/Support/ConcurrentScenarioWorker.php` e `C:\xampp\php\php.exe -l tests/Integration/ConcurrentInvariantsTest.php`: sem erros de sintaxe.
- `C:\xampp\php\php.exe tests/run_all.php` após registrar o teste, antes das correções de suporte: abortou no cenário novo após 262/263 asserções aprovadas; a trava estava visível em `PROCESSLIST`, mas o helper não a reconheceu e a conexão de leitura não selecionava `sgi_test_luna`.
- `C:\xampp\php\php.exe tests/run_all.php` após corrigir o suporte, com MariaDB descartável em `127.0.0.1:3308`: 336 total, 332 aprovadas e 4 falhas funcionais reproduzidas — espera explícita de inscrição não observada e três falhas do limite de equipes (resultado e contagem final).
- Os testes existentes de T13/T16/T17 executados no mesmo runner permaneceram verdes, incluindo estorno concorrente e lote de sincronização.
Evidência: o cenário de estorno produziu `['already_removed', 'removed']` e `quantidade=0.00`, `pontos=30`; a inscrição terminou com três modalidades; a criação de equipes produziu dois `accepted` e contagem final 3, demonstrando a janela que T19/T20 precisam fechar. A execução não mascarou falha de worker como recusa legítima.
Dependências ou limitações restantes: as correções específicas de T19 e T20 foram aplicadas sem alterar fontes de pontuação, contratos HTTP, filas/cache, aliases ou IDs de mutação. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/04-invariantes.md`, ler somente T21 e as fontes necessárias, reproduzir primeiro a ativação concorrente de duas edições e a criação concorrente de edições, então corrigir a unicidade da edição ativa e validar antes de T22.

Tarefa: T19 — Proteger limite de inscrições por aluno e capacidade
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Participantes/Infrastructure/MysqliInscricaoRepository.php`, `src/Modules/Participantes/Application/InscricaoService.php`, `src/Modules/Participantes/Domain/InscricaoRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliEquipePadraoRepository.php`, `tests/Integration/ConcurrentInvariantsTest.php` e este `STATUS.md`.
Defeito reproduzido: o cenário T18 com dois processos submetendo modalidades C/D para o mesmo aluno mostrou que a leitura de `$already` ocorria antes da transação e sem trava explícita do aluno; a espera não era observada (`lock_waits=0`, `trx_waits=0`) e a operação dependia apenas da ordem incidental de FK para terminar com três modalidades. O defeito foi reproduzido antes da alteração no runner, com a criação concorrente ainda isolada para T20.
Arquivos alterados: `MysqliInscricaoRepository.php` abre a transação antes de todas as leituras, trava a edição, o aluno real, a turma e as modalidades em ordem numérica, valida edição/status, calcula modalidades distintas já inscritas e capacidade sob as mesmas travas e só confirma após os vínculos; `ConcurrentInvariantsTest.php` passou a travar edição e aluno no coordenador para provar a espera prevista. Não houve alteração de contrato HTTP, migração, banco de trabalho, aliases, filas/cache ou IDs de mutação.
Decisão aplicada: o limite continua sendo de modalidades distintas, não de quantidade de equipes; repetição permanece válida e não consome uma vaga. A capacidade mantém a consulta de ocupação, mas a linha estável da modalidade e da turma é travada antes, evitando `COUNT ... FOR UPDATE` sobre conjunto vazio como única proteção. A resolução/criação de equipe padrão continua dentro da transação existente e mantém as validações de turma, edição, status e resposta.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe -l src/Modules/Participantes/Infrastructure/MysqliInscricaoRepository.php`: sem erros de sintaxe.
- `C:\xampp\php\php.exe tests/run_all.php` antes da correção: 336 total, 332 aprovadas e 4 falhas na infraestrutura/limites; a inscrição não provava espera explícita, enquanto o cenário de equipes ainda apresentava as falhas próprias de T20.
- `C:\xampp\php\php.exe tests/run_all.php` após T19, com MariaDB descartável em `127.0.0.1:3308`: 336 total, 333 aprovadas e 3 falhas exclusivamente em criação concorrente de equipes; a T19 passou com os dois workers em espera, uma aceitação, uma rejeição e três modalidades finais.
Evidência: a saída da Suite 2.6 registra `Inscrição concorrente aguarda a trava do aluno antes do limite`, `Duas novas modalidades ... aceitam no máximo uma` e `Inscrição concorrente mantém no máximo três modalidades` como aprovadas. Os testes existentes de inscrição, edição/alteração de equipe padrão e limites permaneceram compatíveis.
Dependências ou limitações restantes: T20 deve serializar a criação/atualização de equipes e aplicar `max_equipes` independentemente de nome; depois, T18 deve ser executada novamente para confirmar a matriz inteira verde. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/04-invariantes.md`, ler somente T20 e as fontes nela listadas, reproduzir primeiro as duas criações para a última vaga já registradas na Suite 2.6, corrigir `create/generateName/update` e a resolução de equipes padrão na menor fronteira e validar antes de T21.

Tarefa: T20 — Proteger limite de equipes
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Competicoes/Infrastructure/MysqliEquipeRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliEquipePadraoRepository.php`, `tests/Support/ConcurrentScenarioWorker.php`, `tests/Integration/ConcurrentInvariantsTest.php`, `tests/run_all.php` e este `STATUS.md`.
Defeito reproduzido: a primeira execução funcional da matriz concorrente, com dois processos PHP distintos e MariaDB descartável, observou os dois workers esperando, mas ambos aceitaram a criação da última vaga e a contagem final chegou a 3 para `max_equipes=2` (`336` asserções, `332` aprovadas e `4` falhas totais; após T19, `336/333/3`, exclusivamente no limite de equipes). Isso reproduziu a janela entre a leitura da capacidade e o `INSERT`. Os novos casos determinísticos de limite manual/automático, `max_equipes=0`, reativação, geração padrão e rollback por vínculo inválido foram adicionados e executados após a correção, sem alegar falha prévia não observada.
Arquivos alterados: `MysqliEquipeRepository.php` trava modalidade/turma em ordem estável antes de contar equipes ativas, aplica `max_equipes` independentemente do nome, trata `NULL`/`0` como ilimitado e valida reativação/mudança de escopo dentro da transação; `MysqliEquipePadraoRepository.php` reutiliza a mesma proteção na equipe padrão e na próxima equipe com vaga; `ConcurrentScenarioWorker.php` e `ConcurrentInvariantsTest.php` preservam a coordenação de processos e cobrem rejeição sem escrita parcial, idempotência da geração padrão e concorrência; `tests/run_all.php` registra o teste. Nenhuma migração, banco de trabalho, alias, fila/cache ou identificador de mutação foi alterado.
Decisão aplicada: a capacidade é uma propriedade numérica do escopo modalidade+turma e considera somente equipes ativas; nomes automáticos não substituem a validação. Criação, atualização, reativação, geração padrão e escolha de equipe secundária usam travas de modalidade/turma e uma única transação, com rollback completo em erro de vínculo. A compatibilidade de `max_equipes` foi preservada: `NULL` e `0` continuam ilimitados.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da correção T20: `336` total, `332` aprovadas e `4` falhas; após T19, `336` total, `333` aprovadas e `3` falhas exclusivamente na criação concorrente de equipes.
- `C:\xampp\php\php.exe tests/run_all.php` após T20, com MariaDB descartável em `127.0.0.1:3308`: `344/344` asserções aprovadas, incluindo toda a matriz T18/T19/T20 e os novos limites determinísticos.
- `C:\xampp\php\php.exe -l` nos quatro arquivos PHP alterados: sem erros de sintaxe.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit `142` testes/`1.708` asserções, PHPStan `194/194`, PHP CS Fixer `249/249` e lint PHP sem erros.
- `npm test`: passou; `16/16` testes JavaScript.
- `npm run check`: passou; `40` arquivos JavaScript válidos.
- `npm run build`: passou; `231` assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: `29/29` testes de navegador aprovados em `4,1` minutos; o global setup confirmou `344/344` asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: a Suite 2.6 registra espera explícita dos dois workers, uma única aceitação para a última vaga e contagem final dentro de `max_equipes`; as rejeições manual e automática não gravam parcialmente, `max_equipes=0` aceita múltiplas equipes, reativação respeita a capacidade do destino, e a geração padrão cria uma única equipe de forma repetível. A suíte HTTP, a verificação PHP, JavaScript e navegador permaneceram verdes.
Dependências ou limitações restantes: nenhuma dentro de T20; a garantia de edição ativa única será tratada em T21. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/04-invariantes.md`, ler somente T21 e as fontes necessárias — `src/Modules/Eventos/Application/EdicaoService.php`, `src/Modules/Eventos/Domain/EdicaoRepository.php`, `src/Modules/Eventos/Infrastructure/MysqliEdicaoRepository.php`, `src/Modules/Acesso/Presentation/Http/UsuarioController.php` e `src/Modules/Acesso/Infrastructure/MysqliUsuarioGateway.php` —, reproduzir primeiro a ativação concorrente de duas edições e a criação concorrente de edições, então corrigir a unicidade da edição ativa antes de T22.

Tarefa: T21 — Um único caso de uso para ativar/criar edição
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Eventos/Application/EdicaoService.php`, `src/Modules/Eventos/Domain/EdicaoRepository.php`, `src/Modules/Eventos/Infrastructure/MysqliEdicaoRepository.php`, `src/Modules/Acesso/Presentation/Http/UsuarioController.php`, `src/Modules/Acesso/Infrastructure/MysqliUsuarioGateway.php`, `config/routes.php`, `tests/Support/ConcurrentScenarioWorker.php`, `tests/Integration/ConcurrentInvariantsTest.php`, `tests/Unit/Modules/Eventos/EdicaoServiceTest.php` e este `STATUS.md`.
Defeito reproduzido: antes da correção, a reprodução com dois processos PHP distintos e barreira de início mostrou que a ativação concorrente não aguardava qualquer trava nomeada (`lock_waits=0`, `trx_waits=0`, nenhum estado `GET_LOCK`). Na criação concorrente, os dois caminhos executaram `INSERT`/desativação independentemente e um worker terminou com `Deadlock found when trying to get lock; try restarting transaction`. O teste encerrou com `283` asserções, `282` aprovadas e a falha de espera antes da correção; a operação de criação confirmou o deadlock no MariaDB descartável.
Arquivos alterados: `EdicaoService.php` ganhou o caso único `alterarStatus`, com validação de ID/status; `MysqliEdicaoRepository.php` usa a trava nomeada por `DATABASE()` antes de `create/update`, transação com `finally`, valida o alvo sob `FOR UPDATE` antes de desativar outras edições e reaproveita a desativação atômica; `UsuarioController.php` passou a delegar a alteração ao serviço; `MysqliUsuarioGateway.php` deixou de conter o `UPDATE` independente; `config/routes.php` injeta o mesmo serviço na rota de usuários; o suporte/teste concorrente cobre ativação, criação, rollback lógico, trigger e fixtures completas. Nenhuma migração, banco de trabalho, alias, fila/cache ou ID de mutação foi alterado.
Decisão aplicada: `GET_LOCK(SHA1(DATABASE() . ':edicao-ativa'), 10)` é obtida antes da transação e liberada explicitamente em `finally` nos três caminhos que criam, ativam ou desativam edição. O alvo é verificado e as linhas envolvidas são travadas em ordem crescente antes de qualquer desativação. `NULL`/nenhuma edição ativa continuam permitidos; a invariante aplicada é no máximo uma ativa. O trigger existente de `interclasses` continua sincronizando `status_usuario`; a autorização não depende de cache de sessão.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe tests/run_all.php` antes da correção: reprodução determinística T21 com `283` total, `282` aprovadas, ausência de espera em ativação e deadlock na criação concorrente.
- `C:\xampp\php\php.exe tests/run_all.php` após a correção, com MariaDB descartável em `127.0.0.1:3308`: `354/354` asserções aprovadas; ativação/criação concorrentes aguardaram a trava, não expuseram SQL, mantiveram no máximo uma edição ativa e preservaram fixtures/alunos.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit `143` testes/`1.709` asserções, PHPStan `194/194`, PHP CS Fixer `249/249` e lint PHP sem erros.
- `npm test`: passou; `16/16` testes JavaScript.
- `npm run check`: passou; `40` arquivos JavaScript válidos.
- `npm run build`: passou; `231` assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: `29/29` testes de navegador aprovados em `4,0` minutos; o global setup confirmou `354/354` asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: a Suite 2.6 registra espera na trava nomeada, duas ativações aceitas com uma única edição ativa, alunos coerentes com o trigger, ID inexistente rejeitado sem desativar a ativa e duas criações aceitas com categorias/turmas/modalidades/equipes das duas fixtures e no máximo uma ativa.
Dependências ou limitações restantes: nenhuma dentro de T21; a transação e a publicação do resultado na camada Application serão tratadas em T22. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/05-fronteiras.md`, ler somente T22 e as fontes nela listadas, reproduzir primeiro o caso de regra/resultado parcial indicado, então mover a fronteira transacional para Application sem alterar contratos, aliases, filas/cache ou IDs de mutação.

Tarefa: T22 — Coordenar resultado e transação em Application
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Shared/Application/TransactionRunner.php`, `src/Shared/Database/MysqliTransactionRunner.php`, `src/Modules/Competicoes/Application/ResultadoService.php`, `src/Modules/Competicoes/Application/IndividualRankingService.php`, `src/Modules/Competicoes/Domain/ResultadoRepository.php`, `src/Modules/Competicoes/Domain/IndividualRankingRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliPartidaGateway.php`, `src/Modules/Competicoes/Infrastructure/MysqliIndividualRankingRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliChaveamentoManagement.php`, `src/Modules/Resultados/Application/PontuacaoService.php`, `src/Modules/Resultados/Domain/PodioRepository.php`, `src/Modules/Resultados/Infrastructure/MysqliPodioRepository.php`, `src/Modules/Sincronizacao/Infrastructure/MysqliChaveamentoSyncGateway.php`, `src/Modules/Competicoes/Presentation/Http/ResultadoController.php`, `config/routes.php`, testes unitários/integrados e este `STATUS.md`.
Defeito reproduzido: antes da correção, o teste unitário novo de resultado não conseguia carregar `App\Modules\Competicoes\Domain\ResultadoRepository` (`Interface ... not found`), e a leitura do fluxo confirmou que `ResultadoController` dependia diretamente de `MysqliPartidaGateway`, que mantinha resolução, validação, gravação, avanço e crédito na mesma orquestração de infraestrutura. O caminho individual e o lote de sincronização também chamavam diretamente os repositórios concretos, impedindo testar a sequência sem banco.
Arquivos alterados: foram criados `TransactionRunner`/`MysqliTransactionRunner`, `ResultadoService`, `PontuacaoService`, `IndividualRankingService` e os contratos/adaptadores de resultado, pódio e ranking individual. `ResultadoController` agora recebe o serviço; `MysqliPartidaGateway` expõe operações SQL por contrato e mantém `launch` legado como encaminhamento; a reconciliação de pódio saiu do gateway para `PontuacaoService`; `MysqliChaveamentoManagement` e `MysqliChaveamentoSyncGateway` usam runners com savepoints e o mesmo serviço de pontuação/individual; a composição foi atualizada. `AtomicMutationTest` passou a exercitar o runner real aninhado. Nenhuma migração, banco de trabalho, alias, fila/cache ou ID de mutação foi alterado.
Decisão aplicada: o Application valida equipes, placar e duplicidade antes da escrita, resolve e bloqueia o jogo, lê o vencedor anterior, persiste o placar, conclui/avança ou retifica, reconstrói a fase e delega o delta de crédito ao contrato público de pontuação. `MysqliTransactionRunner` confirma o callback ou faz rollback em qualquer `Throwable`, usando `Transaction`/savepoint; `MutationAction` continua sendo a transação externa que persiste a resposta. A correção de vencedor usa créditos históricos já concedidos, sem recalcular configuração; a sincronização reaproveita `PontuacaoService`; ranking individual valida posições distintas no Application e mantém o SQL no adaptador legado.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe vendor\bin\phpunit tests/Unit/Modules/Competicoes/ResultadoServiceTest.php` antes da implementação: falhou reproduzindo a interface ausente; depois: `5/5` testes e, após os casos finais, `7/7` testes/`13` asserções aprovados.
- `C:\xampp\php\php.exe vendor\bin\phpunit tests/Unit/Modules/Competicoes/IndividualRankingServiceTest.php`: `2/2` testes aprovados como parte dos `7/7` finais.
- `C:\xampp\php\php.exe tests/run_all.php` com MariaDB descartável em `127.0.0.1:3308` e servidor `http://127.0.0.1:8110/`: `354/354` asserções aprovadas; resultado real, retificação de pódio, individual, sincronização e falha idempotente permaneceram verdes.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit `150` testes/`1.762` asserções, PHPStan `202/202`, PHP CS Fixer `259/259` e lint PHP sem erros.
- `npm test`: passou; `16/16` testes JavaScript.
- `npm run check`: passou; `40` arquivos JavaScript válidos.
- `npm run build`: passou; `231` assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: `29/29` testes de navegador aprovados em `4,0` minutos; o global setup confirmou `354/354` asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: os testes unitários observam a ordem `resolve→lock→persist→conclude/advance→credit`, o delta de troca `[1 => -3, 2 => 3]`, a rejeição de placar antes da transação e a interrupção antes do crédito quando o avanço falha. A suíte real confirmou que a rota modular não importa o gateway no controller, que a final e os jogos offline continuam concluindo, que créditos não duplicam e que o rollback aninhado da resposta idempotente desfaz a escrita interna.
Dependências ou limitações restantes: o método `launch` público do gateway permanece somente como adaptador de compatibilidade para consumidores legados; a remoção de dependências concretas restantes dos controllers e a verificação arquitetural completa pertencem a T23–T25. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/05-fronteiras.md`, ler somente T23 e as fontes nela listadas — `UsuarioController.php`, `MysqliUsuarioGateway.php`, `UsuarioAdministrativoService.php`, `MysqliEdicaoConsulta.php`, os contratos de Acesso e `config/routes.php` —, inventariar as ações/`acao`/permissões/envelopes indicadas, reproduzir primeiro a dependência MySQLi e o fluxo completo de usuários, então mover consultas e ativação para contratos sem alterar aliases, permissões, upload, filas/cache ou IDs de mutação.

Tarefa: T23 — Retirar MySQLi e consultas de outros módulos do controller de usuários
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Acesso/Presentation/Http/UsuarioController.php`, `src/Modules/Acesso/Application/UsuarioService.php`, `src/Modules/Acesso/Application/UsuarioAdministrativoService.php`, `src/Modules/Acesso/Domain/UsuarioConsultaRepository.php`, `src/Modules/Acesso/Domain/UsuarioManagementRepository.php`, `src/Modules/Acesso/Infrastructure/MysqliUsuarioConsultaRepository.php`, `src/Modules/Acesso/Infrastructure/MysqliUsuarioManagementRepository.php`, `src/Modules/Acesso/Infrastructure/MysqliUsuarioGateway.php`, `src/Modules/Eventos/Domain/EdicaoConsulta.php`, `src/Modules/Eventos/Infrastructure/MysqliEdicaoConsultaRepository.php`, `src/Modules/Acesso/Domain/FotoStorage.php`, `src/Modules/Acesso/Infrastructure/LocalFotoStorage.php`, `config/routes.php`, `tests/Integration/ExceptionEnvelopeTest.php`, `tests/Unit/Modules/Acesso/UsuarioControllerArchitectureTest.php`, `tests/Unit/Modules/Acesso/UsuarioServiceTest.php` e este `STATUS.md`.
Defeito reproduzido: antes da alteração, o teste arquitetural novo falhou em `1/1` caso porque o construtor de `UsuarioController` recebia `mysqli`; a leitura do controller também mostrou import direto de `MysqliEdicaoConsulta`, dependência concreta de `MysqliUsuarioGateway` e validação de inscrição/consulta de edição dentro da apresentação. O gateway misturava GET, autenticação por inscrição, criação/edição/atribuição de alunos, cadastro/edição de colaboradores, upload e SQL administrativo. A reprodução do envelope com conexão em `information_schema` também expôs a regressão intermediária: sem a captura específica, o erro MySQLi retornava HTTP 400 com a mensagem interna `Unknown table`; o teste de integração falhou antes do ajuste.
Inventário preservado:

- GET geral — `acao` ausente, autorização `[0,1,2]`, edição ativa obrigatória, envelope `{status, usuarios}`.
- GET `listar_competidores` — autorização `[0,1,2]`, turma/edição/gênero, envelope `{status, competidores}`.
- GET `listar_colaboradores` — autorização `[0,1,2]`, edição ativa, envelope `{status, colaboradores}`.
- PUT de status da edição — autorização `[0,1]`, `EdicaoService::alterarStatus`, sucesso `{status: sucesso, mensagem: Edição atualizada.}`.
- POST `validar_inscricao` — consulta pública mantida pelas regras CSRF existentes, edição ativa por contrato, RM/data normalizados no caso de uso, sessão regenerada e permissões/dados no envelope legado.
- POST `criar_aluno` — autorização `[0,1]`, validação no caso de uso, edição ativa, envelope de criação preservado.
- POST `editar_aluno` — autorização `[0,1]`, edição ativa, mensagem `Aluno atualizado!` preservada.
- Query `id` para atribuição — autorização `[0,1]`, vínculo de turma/edição e mensagem `Aluno atualizado.` preservados.
- POST `cadastrar_usuario` — autorização `[0]`, edição ativa, upload traduzido na apresentação para caminho temporário e persistido via `FotoStorage`; envelope de criação preservado.
- POST `atualizar_colaborador` e `atualizar_dados_colaborador` — autorização `[0]`, edição ativa, mensagens e proteção de contas preservadas.
- POST `excluir_aluno`, `resetar_senha_aluno` e `excluir_colaborador` — autorização `[0]`, `UsuarioAdministrativoService`, senha padrão e proteções existentes preservadas.
Arquivos alterados: `UsuarioController` agora depende de `UsuarioConsultaRepository`, `UsuarioService`, `UsuarioAdministrativoService`, `EdicaoConsulta` e `EdicaoService`; não recebe `mysqli` nem importa infraestrutura. Foram criados os contratos de consulta/mutação de usuários e de consulta de edição ativa, seus adaptadores MySQLi e o `UsuarioService`, que concentra normalização/validação de inscrição e dados, além de coordenar `FotoStorage` sem transportar `$_FILES` para Application. O gateway histórico foi reduzido a adaptador que delega aos repositórios/serviço, preservando consumidores legados. `config/routes.php` compõe os novos contratos; `MysqliEdicaoConsulta.php` está fora da apresentação de Acesso. Nenhuma migração, banco de trabalho, alias, fila/cache ou ID de mutação foi alterado.
Decisão aplicada: consultas de usuários e mutações têm fronteiras separadas; a consulta da edição ativa é pública no contrato de Eventos e a alteração de status continua usando o caso de uso T21. O controller extrai apenas `tmp_name`/erro do upload e o serviço usa o contrato já existente de foto, removendo arquivo salvo quando a persistência falha. A validação de inscrição normaliza RM/data antes da consulta; a apresentação continua responsável por sessão, resposta e permissões. A captura explícita de `mysqli_sql_exception` mantém o envelope HTTP 500 sem revelar SQL, enquanto erros de domínio seguem HTTP 400.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe vendor\bin\phpunit tests\Unit\Modules\Acesso\UsuarioControllerArchitectureTest.php` antes da implementação: falhou `1/1`, reproduzindo a dependência `mysqli` no construtor; após: `1/1` e `4` asserções aprovadas.
- `C:\xampp\php\php.exe vendor\bin\phpunit tests\Unit\Modules\Acesso\UsuarioControllerArchitectureTest.php tests\Unit\Modules\Acesso\UsuarioServiceTest.php`: `5/5` testes e `15` asserções aprovados, cobrindo fronteira, normalização, validação sem escrita, upload por contrato e tradução de dados.
- `C:\xampp\php\php.exe tests\run_all.php` antes do ajuste final do envelope: `353/354`, com a falha reproduzida no GET de usuários; após a captura compatível: `354/354` asserções aprovadas em MariaDB descartável `127.0.0.1:3308` e servidor `http://127.0.0.1:8110/`.
- `composer verify`: passou; PHPUnit `155` testes/`1.812` asserções, PHPStan `209/209`, PHP CS Fixer `268/268` e lint PHP sem erros.
- `npm test`: passou; `16/16` testes JavaScript.
- `npm run check`: passou; `40` arquivos JavaScript válidos.
- `npm run build`: passou; `231` assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: `29/29` testes de navegador aprovados em `4,5` minutos; o global setup confirmou `354/354` asserções HTTP. Os cenários administrativos validaram cadastro/edição/exclusão de usuários, upload, autenticação e RBAC.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: o teste arquitetural confirma que a apresentação não recebe MySQLi nem referencia `MysqliUsuarioGateway`/`MysqliEdicaoConsulta`; os testes de serviço confirmam normalização e bloqueio antes da escrita. A suíte HTTP e o navegador confirmaram os envelopes, permissões, contas protegidas, upload, autenticação por inscrição, atribuição, edição e exclusão reais. O gateway legado continua chamando os novos contratos, e não contém mais consultas ou regras duplicadas.
Dependências ou limitações restantes: `MysqliEdicaoConsulta.php` está mantido como classe estática legada para compatibilidade potencial, mas não é usado pelo controller de Acesso; o adaptador `MysqliUsuarioGateway` também permanece apenas para consumidores antigos. A extração das regras de inscrição/equipes e a formalização das dependências restantes pertencem a T24–T25. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/05-fronteiras.md`, ler somente T24 e as fontes nela listadas, reproduzir primeiro a regra de inscrição/equipe indicada, então extrair as regras para Application/Domain sem alterar aliases, filas/cache, IDs de mutação, MySQL/MariaDB ou permissões.

Tarefa: T24 — Extrair regras de inscrição/equipes e formalizar dependências
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Participantes/Domain/InscricaoRules.php`, `src/Modules/Participantes/Application/InscricaoService.php`, `src/Modules/Competicoes/Domain/EquipeCapacityRules.php`, `src/Modules/Competicoes/Domain/EquipePadraoRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliEquipePadraoRepositoryAdapter.php`, `src/Modules/Competicoes/Infrastructure/MysqliInscricaoRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliEquipeRepository.php`, `src/Modules/Competicoes/Infrastructure/MysqliEquipePadraoRepository.php`, `src/Modules/Eventos/Infrastructure/MysqliEdicaoRepository.php`, `config/routes.php`, `docs/architecture.md`, `tests/Unit/Modules/Participantes/InscricaoRulesTest.php`, `tests/Unit/Modules/Competicoes/EquipeCapacityRulesTest.php`, `tests/Unit/Architecture/CrossModuleContractsTest.php` e este `STATUS.md`.
Defeito reproduzido: antes da implementação, os testes unitários das regras falharam com 4 erros e 1 falha porque `InscricaoRules` e `EquipeCapacityRules` não existiam. O teste de fronteira também falhou ao detectar import e chamada direta de `MysqliEquipePadraoRepository` em `MysqliInscricaoRepository`. Durante a primeira execução real após extrair a regra, a concorrência abortou um worker porque `InvalidArgumentException` atravessou o processo de inscrição; isso foi corrigido no limite da infraestrutura, convertendo a rejeição para o `RuntimeException` de domínio já tratado pelo worker, sem relaxar o limite.
Arquivos alterados: foi criada a regra pura de união/normalização e limite de três modalidades, a regra pura de capacidade de equipes, o contrato de equipes padrão e seu adaptador MySQLi compatível com os helpers estáticos legados. Inscrição e criação de edição passaram a depender do contrato; locks, `FOR UPDATE`, SQL, persistência e transações permaneceram nos repositórios. A capacidade existente foi delegada à regra sem alterar `EquipeLimiteException`, a composição foi ajustada em `config/routes.php`, e `docs/architecture.md` passou a documentar as dependências permitidas. Nenhuma migração, banco de trabalho, alias, fila/cache ou ID de mutação foi alterado.
Decisão aplicada: `InscricaoRules` recebe IDs normalizados e calcula a união depois que o repositório bloqueia o estado relevante na mesma transação; a infraestrutura continua fornecendo estado bloqueado e escrevendo. `EquipeCapacityRules` preserva capacidade nula/zero como ilimitada e a exceção pública antiga. `Participantes` e `Eventos` usam `EquipePadraoRepository`, enquanto o adaptador mantém os métodos estáticos legados e a geração/quantidade de equipes padrão. Não foi feita refatoração ampla de helpers nem criado acesso concreto entre módulos fora da composição.
Comandos executados e resultado de cada um:

- Testes novos antes da implementação: `C:\xampp\php\php.exe vendor\bin\phpunit tests\Unit\Modules\Participantes\InscricaoRulesTest.php tests\Unit\Modules\Competicoes\EquipeCapacityRulesTest.php` falhou com `4` erros e `1` falha, reproduzindo as classes ausentes.
- `C:\xampp\php\php.exe vendor\bin\phpunit tests\Unit\Architecture\CrossModuleContractsTest.php` antes do adapter: falhou ao detectar a dependência concreta em `MysqliInscricaoRepository`.
- Suíte direcionada após a correção: `12/12` testes e `25` asserções aprovados, incluindo `InscricaoService`, `EquipeService` e fronteiras entre módulos.
- Primeira execução intermediária de `C:\xampp\php\php.exe tests\run_all.php`: o cenário concorrente iniciou, mas um worker terminou com a exceção não traduzida; após o ajuste compatível, a execução final passou com `354/354` asserções no MariaDB descartável `127.0.0.1:3308` e servidor `http://127.0.0.1:8110/`.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit `161` testes/`1.853` asserções, PHPStan `213/213`, PHP CS Fixer `275/275` e lint PHP sem erros.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: `29/29` testes de navegador aprovados em `4,2` minutos; o global setup confirmou `354/354` asserções HTTP.
- `npm test`, `npm run check` e `npm run build` permaneceram aprovados na validação anterior de T23 (`16/16`, `40` arquivos e `231` assets); T24 não alterou JavaScript.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: a regra de limite é exercitada sem banco, a matriz HTTP manteve a concorrência de inscrições/equipes e a criação de edição com quantidade padrão, e a suíte de navegador confirmou inscrição de aluno, RBAC, torneios e fluxos offline sem alteração dos contratos existentes.
Dependências ou limitações restantes: os helpers estáticos legados continuam preservados por compatibilidade; a próxima etapa deve reforçar os testes arquiteturais e de composição sem transformar exceções em aprovações. Nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/05-fronteiras.md`, ler somente T25 e as fontes nela listadas — `tests/Unit/Architecture/LayerDependenciesTest.php`, `ModuleLayoutTest.php`, `PresentationBoundaryTest.php`, `phpstan.neon` e `docs/architecture.md` —, reproduzir primeiro a lacuna arquitetural indicada e fortalecer os checks sem proibir a composição concreta em `config/routes.php`/bootstrap.

Tarefa: T25 — Fortalecer testes arquiteturais sem mascarar exceções
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `tests/Unit/Architecture/LayerDependenciesTest.php`, `tests/Unit/Architecture/CrossModuleContractsTest.php`, `tests/Unit/Architecture/PresentationBoundaryTest.php`, `tests/Unit/Modules/Acesso/UsuarioControllerArchitectureTest.php`, `src/Modules/Participantes/Infrastructure/MysqliInscricaoRepository.php`, `src/Modules/Eventos/Infrastructure/MysqliEdicaoRepository.php`, `tests/Support/ConcurrentScenarioWorker.php`, `tests/Integration/ConcurrentInvariantsTest.php`, `tests/Integration/ExceptionEnvelopeTest.php`, `docs/architecture.md` e este `STATUS.md`.
Defeito reproduzido: o novo check de contratos entre módulos foi executado antes da correção e falhou em `1/1` teste, mostrando que `MysqliInscricaoRepository` importava `App\\Modules\\Competicoes\\Infrastructure\\MysqliEquipePadraoRepositoryAdapter`; a dependência equivalente também existia em `MysqliEdicaoRepository`. A verificação anterior não detectava o adapter porque só procurava o helper estático antigo. Nenhum teste foi relaxado e a falha não foi convertida em aprovação.
Arquivos alterados: os repositórios agora recebem `EquipePadraoRepository` obrigatoriamente e não criam adapter concreto de outro módulo; a composição de `config/routes.php` já existente continua fornecendo `MysqliEquipePadraoRepositoryAdapter`, e os workers/testes de integração foram ajustados para compor explicitamente a mesma dependência. `LayerDependenciesTest` passou a analisar tokens, ignorando comentários e usos de closure, para bloquear HTTP, sessão, banco, SQL e transação em Domain/Application. `PresentationBoundaryTest` passou a cobrir os controllers críticos migrados (`ResultadoController` e `UsuarioController`) e a composição das rotas críticas; `docs/architecture.md` lista individualmente as dependências concretas fora desse escopo. Nenhuma migração, banco de trabalho, alias, fila/cache ou ID de mutação foi alterado.
Decisão aplicada: contratos de módulos são exigidos na fronteira dos repositórios; implementações MySQLi permanecem somente na composição ou no próprio módulo autorizado. Os checks usam imports/tokens e reflexão, sem wildcard de exceção, sem proibir a composição concreta nas rotas/bootstrap e sem elevar o nível do PHPStan. As dependências de apresentação ainda fora do escopo crítico ficaram documentadas com arquivo e motivo, enquanto as rotas de resultado e usuários são verificadas contra os casos de uso efetivamente compostos.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe vendor\bin\phpunit --configuration phpunit.xml tests\Unit\Architecture\CrossModuleContractsTest.php` antes da correção: `1` falha, reproduzindo o import do adapter concreto; depois: `1/1` teste e `6` asserções aprovados.
- Checks arquiteturais direcionados depois: `11/11` testes e `1.425` asserções aprovados.
- `C:\xampp\php\php.exe tests\run_all.php` com MariaDB descartável `127.0.0.1:3308` e servidor `http://127.0.0.1:8110/`: `354/354` asserções aprovadas.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit `163` testes/`1.809` asserções, PHPStan `213/213`, PHP CS Fixer `275/275` e lint PHP sem erros.
- `npm test`: `16/16` testes JavaScript aprovados.
- `npm run check`: `40` arquivos JavaScript válidos.
- `npm run build`: `231` assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: `29/29` testes de navegador aprovados em `4,1` minutos; o global setup confirmou `354/354` asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: a análise arquitetural deixa de depender de busca textual que confundia comentários, os controllers críticos não recebem infraestrutura concreta, as rotas de resultados/usuários registram e compõem seus casos de uso, e a matriz real preservou concorrência, criação de equipes padrão, inscrição, filas/cache offline, aliases e envelopes de exceção.
Dependências ou limitações restantes: as dependências concretas explicitamente listadas em `docs/architecture.md` permanecem fora do escopo crítico desta etapa e não foram ampliadas. O próximo trabalho é a validação de upgrade e de falhas de migração; nenhuma publicação ou alteração de banco de trabalho foi feita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/06-validacao-final.md`, ler somente T26 e as fontes nela listadas, reproduzir primeiro o cenário de upgrade/falha de migração indicado e validar checksum, rollback, repetição e preservação de dados antes de T27.

Tarefa: T26 — Ensaiar upgrade real e falhas de migração
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Shared/Database/MigrationRunner.php`, `tests/Integration/LegacyUpgradeTest.php`, `tests/run_all.php`, `database/migrations/001_initial_schema.sql`, `database/migrations/002_mutation_fingerprint.sql`, `database/migrations/003_fix_arrecadacao_revaluation.sql`, `database/migrations/004_podio_credit_sources.sql`, `database/archive/pre-refactor.sql`, `src/Shared/Database/SqlScript.php`, `tests/Integration/MigrationsTest.php`, `docs/deployment.md` e este `STATUS.md`.
Defeito reproduzido: o novo ensaio criou uma base anterior real a partir de `001_initial_schema.sql`, com dados sintéticos e índice `uk_matricula_interclasse` renomeado para a mesma chave, mas não exclusivo. Antes da correção, `MigrationRunner::assertBaseline()` comparava apenas as colunas e aceitou esse esquema incompatível; a execução registrou `1` falha em `374` asserções no cenário “Índice incompatível recusa baseline sem aprovação falsa”. O primeiro ensaio também encontrou uma referência de local ausente na própria fixture; o dado sintético foi corrigido antes de interpretar o resultado do runner, sem alterar o código de produção.
Arquivos alterados: `MigrationRunner` agora ordena as entradas de `SHOW INDEX`, exige a chave esperada e verifica `Non_unique = 0`, recusando baseline não exclusivo. Foi criada `LegacyUpgradeTest`, registrada no runner, com bancos adicionais explicitamente nomeados e limpos ao final: instalação vazia, base anterior sem `--baseline`, baseline compatível, evolução 002–004, segunda execução, índice incompatível, falha após DDL com `dirty` e checksum divergente. A fixture usa duas edições, matrícula repetida em anos distintos, quatro níveis, hashes verificáveis, equipes/jogos, final e individual, arrecadação fracionária/inativa, penalidades individual/turma, ajuste conhecido, idempotência legada/nova e créditos de pódio. Nenhuma migração aplicada foi reescrita, nenhum banco de trabalho foi alterado e aliases, filas/cache e IDs de mutação foram preservados.
Decisão aplicada: o upgrade usa a estrutura da migração 001 sem apagar seus dados, registra a adoção e aplica somente 002–004; a conferência valida `password_verify`, unicidade efetiva, FKs, ausência de órfãos, trigger de revalorização, fingerprint legado/nova e adoção idempotente de pódio. Falhas após DDL deixam a tabela/objeto parcial e o marcador `dirty`, e a nova tentativa é bloqueada; checksum divergente também bloqueia. Diretórios temporários ficam em `test-results/` e são removidos pelo teste; os bancos são nomes de teste aprovados por `TestDatabase`.
Comandos executados e resultado de cada um:

- Primeira execução do novo `LegacyUpgradeTest` pelo runner: a fixture falhou por FK de local ausente; após corrigir a fixture sintética, o ensaio reproduziu a aprovação indevida do índice não exclusivo (`1` falha em `374` asserções).
- `C:\xampp\php\php.exe tests\run_all.php` após a correção, com MariaDB descartável `127.0.0.1:3308` e servidor `http://127.0.0.1:8110/`: `374/374` asserções aprovadas, incluindo os cenários de upgrade, baseline, `dirty`, checksum e preservação.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit `163` testes/`1.812` asserções, PHPStan `213/213`, PHP CS Fixer `275/275` e lint PHP sem erros.
- `npm test`: `16/16` testes JavaScript aprovados.
- `npm run check`: `40` arquivos JavaScript válidos.
- `npm run build`: `231` assets preparados.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: `29/29` testes de navegador aprovados em `4,6` minutos; o global setup confirmou `374/374` asserções HTTP.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: a atualização partiu de uma base 001 sem histórico, preservou contas e matrículas de anos distintos, aplicou as migrações posteriores uma vez, repetiu sem delta, recusou esquema incompatível, manteve `dirty` após DDL e bloqueou checksum alterado. Nenhuma credencial de demonstração foi criada.
Dependências ou limitações restantes: a comparação de baseline continua deliberadamente limitada ao contrato documentado de colunas e da chave de matrícula; tipos, triggers e demais índices devem continuar sendo comparados no procedimento de implantação/backup conforme `docs/deployment.md`. A próxima etapa trata clientes offline antigos e registros de cache/fila.
Próximo passo exato: abrir `docs/plano-implementacao-luna/06-validacao-final.md`, ler somente T27 e as fontes nela listadas, reproduzir primeiro o cliente/cache offline antigo indicado e validar aliases, registros anteriores, IDs de mutação e sincronização sem limpar dados do mesário.

Tarefa: T28 — Executar matriz disponível, subdiretório e recuperação sintética
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `tests/Integration/RecoveryRehearsalTest.php`, `tests/run_all.php`, `docs/testing.md`, `docs/deployment.md` e este `STATUS.md`. A configuração de CI permaneceu intacta e continua declarando PHP 8.2/8.4, MySQL 8.4, MariaDB 10.11 e comparação visual em Windows.
Defeito reproduzido: a primeira execução do novo ensaio de recuperação parou na fixture sintética por uma FK de histórico que apontava para o usuário `3`, embora a fixture tivesse criado somente o usuário `1`. O defeito era do cenário de teste, foi corrigido antes de interpretar o resultado e reproduzido novamente com a fixture válida; não houve alteração de migração nem de banco de trabalho.
Arquivos alterados: `RecoveryRehearsalTest.php` cria backup SQL real de uma base sintética derivada de `001_initial_schema.sql`, registra hash/versão/fronteira em manifesto, atualiza a origem com as migrações existentes, restaura em outra base e compara dados, hashes, histórico e triggers; `tests/run_all.php` registra a Suite 16. A documentação descreve o comando, artefatos e o limite entre o backup pré-upgrade e a base já atualizada. Nenhuma migração aplicada foi reescrita; aliases, filas/cache e IDs de mutação foram preservados.
Decisão aplicada: a recuperação é ensaiada somente em bancos nomeados pelo teste e descartáveis, com `mysqldump`/`mysql` locais, sem tocar a base do servidor de trabalho. O dump permanece em `test-results/` junto do manifesto para auditoria; as bases sintéticas de origem/restauração são removidas pelo `finally`. O retorno para pacote anterior só é considerado dentro da fronteira pré-upgrade registrada; filas IndexedDB não são limpas.
Comandos executados e resultado de cada um:

- `C:\xampp\mysql\bin\mysql.exe --version` e `C:\xampp\mysql\bin\mysqldump.exe --version`: MariaDB 10.4.32, cliente disponível; nenhum Docker Engine estava disponível para os alvos do compose.
- Primeira execução de `C:\xampp\php\php.exe tests\run_all.php`: interrompida somente pela FK incorreta da fixture de recuperação; a fixture foi ajustada.
- `C:\xampp\php\php.exe tests\run_all.php` após o ajuste: `380/380` asserções aprovadas, incluindo backup, upgrade, restauração, triggers e manifesto.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: aprovado; PHPUnit `163` testes/`1.815` asserções, PHPStan `213/213`, PHP CS Fixer `275/275` e lint PHP sem erros.
- `npm run build`: `231` assets preparados; `npm run check`: `40` arquivos JavaScript válidos; `npm test`: `16/16` testes aprovados.
- Servidor separado com `SGI_BASE_PATH=SGI` em `http://127.0.0.1:8111/SGI/`: health confirmou `sgi_test_luna`; `npm --prefix tests/browser test -- deployment-paths.spec.cjs`: `1/1` aprovado.
- Suíte de navegador na raiz `http://127.0.0.1:8110/`, após o build, iniciou a validação final e revelou o defeito registrado em T29 abaixo; os demais cenários até a ocorrência foram executados, portanto o resultado total não foi convertido em aprovação de T28.
Evidência: `test-results/t28-recovery-*.sql` e `*.json` contêm o dump e o hash da execução; a Suite 16 confirma seis comparações verdes para backup, upgrade, restauração e fronteira pré-upgrade. O teste de subdiretório confirmou redirecionamento, assets e APIs sob `/SGI/`.
Dependências ou limitações restantes: não foi possível executar neste host MySQL 8.4, MariaDB 10.11, PHP 8.4 com integração HTTP e o job visual Windows; a configuração de CI continua sendo a evidência operacional desses alvos, mas eles permanecem pendentes até uma execução no ambiente correspondente. Localmente, PHP 8.2 + MariaDB 10.4.32 e PHP 8.4 para `composer verify` foram aprovados.
Próximo passo exato: concluir T29 lendo o diff final, repetir sequencialmente os checks obrigatórios após a correção do modal, atualizar arquitetura/testes/deploy/conclusão e registrar a limitação externa sem publicar.

Tarefa: T29 — Revisar implementação e entregar evidência final
Estado: concluída.
Referência do código: commit de referência `aedbc420ea34aa759db1b14a8307abe4eb478cfa`, branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado nesta execução; arquivos locais relevantes: `resources/js/pages/competicoes/placar.js`, `public/assets/`, `tests/browser/occurrence-offline-edit.spec.cjs`, `tests/Integration/RecoveryRehearsalTest.php`, `docs/architecture.md`, `docs/testing.md`, `docs/deployment.md`, `docs/migration-completion-plan.md` e este `STATUS.md`.
Defeito reproduzido: a suíte completa `npm --prefix tests/browser test` reproduziu `1` falha em `32` testes no cenário `occurrence-offline-edit.spec.cjs`: após editar e sincronizar uma ocorrência, a edição offline de uma ocorrência temporária abria o modal, mas o callback de fechamento atrasado do primeiro salvamento fechava o novo modal; o clique em `#btnSalvarOcorrencia` aguardou 180 segundos e terminou com `Test timeout of 180000ms exceeded`. O screenshot confirmou a tela sem modal no instante da falha.
Arquivos alterados: `placar.js` agora mantém o timer de fechamento do modal, cancela qualquer fechamento pendente ao iniciar nova ocorrência/edição e substitui o timer anterior antes de agendar outro; o callback limpa sua própria referência. `npm run build` regenerou `public/assets/` para que o bundle servido correspondesse à fonte. Nenhuma permissão, alias, fila/cache, ID de mutação, migração ou banco de trabalho foi alterado.
Decisão aplicada: somente o ciclo de vida do modal foi serializado; a ordem e o conteúdo das mutações offline continuam os mesmos. O timer não é removido de forma global, apenas cancelado quando uma nova operação usa o mesmo modal, evitando que uma confirmação anterior feche uma edição posterior.
Comandos executados e resultado de cada um:

- `npm --prefix tests/browser test` antes da correção, com reset HTTP real: `31` aprovados e `1` falha no cenário de ocorrência offline; falha reproduzida no clique do botão após a segunda edição.
- `npm run build`: `231` assets preparados.
- `composer verify`: aprovado com PHP runtime `8.4.25`; PHPUnit `163` testes/`1.815` asserções, PHPStan `213/213`, PHP CS Fixer `275/275` e lint PHP sem erros.
- `npm run check`: `40` arquivos JavaScript válidos.
- `npm test`: `16/16` testes JavaScript aprovados.
- `C:\xampp\php\php.exe tests\run_all.php` com servidor `http://127.0.0.1:8110/` e MariaDB descartável `sgi_test_luna`: `380/380` asserções aprovadas.
- `npm --prefix tests/browser test -- occurrence-offline-edit.spec.cjs` após a correção: `1/1` aprovado e global setup `380/380` asserções HTTP.
- `npm --prefix tests/browser test` após a correção: `32/32` cenários aprovados em `4,8` minutos, incluindo dois contratos visuais; o global setup confirmou `380/380` asserções HTTP.
- `git diff --check`: passou; os únicos avisos foram conversões LF/CRLF normais do working tree.
- `git diff --name-only -- database/migrations database/archive`: vazio; as migrações aditivas `003_fix_arrecadacao_revaluation.sql` e `004_podio_credit_sources.sql` permanecem novas, e as migrações aplicadas existentes não foram reescritas.
Rastreabilidade final: as buscas não encontraram `saveTimers` descartando gravações no código atual; `generateName` só é chamado depois de `assertCapacity` em criação/atualização; a edição de ocorrência seleciona o ID solicitado e não `lista[0]`; o UPDATE de edição passa pelo serviço único de T21. As escritas de `pontuacao_turma` em produção foram conferidas: `MysqliPodioRepository::aplicarDeltas` aplica somente deltas de origem de pódio, `MysqliArrecadacaoRepository` grava o valor absoluto recalculado por `D(Q,V)`, `MysqliRankingRepository::updateTeam` é ajuste administrativo explícito e os triggers/migrações só revalorizam arrecadação. O adaptador individual reutiliza a fonte de crédito de pódio e não soma uma rotina paralela. Aliases antigos e versionados convergem pelos controllers/serviços compostos e foram exercitados na matriz HTTP/browser.
Documentação atualizada: `docs/architecture.md`, `docs/testing.md`, `docs/deployment.md` e `docs/migration-completion-plan.md`; a auditoria original não foi reescrita e o STATUS preserva o defeito reproduzido, a correção e as evidências.
Evidência: o fluxo completo de duas ocorrências, edição offline da ocorrência alvo, criação temporária, edição antes do POST, dependência na fila, reconexão e inativação passou após o cancelamento do timer obsoleto. A recuperação sintética preservou schema, dados, hashes, histórico e triggers e deixou dump/manifesto com hash em `test-results/`; nenhum banco de trabalho foi alterado.
Dependências ou limitações restantes: neste host não foram executados MySQL 8.4, MariaDB 10.11, PHP 8.4 com integração HTTP separada nem o job visual Windows; esses alvos permanecem pendência operacional explícita do CI. Não houve push, deploy, publicação ou alteração de banco de trabalho. O servidor `8111` criado nesta execução foi encerrado após a validação; o servidor preexistente `8110` foi preservado.
Próximo passo exato: nenhum passo restante do plano; para publicação, executar a matriz externa pendente, revisar os artefatos de `test-results/` e o diff local, depois seguir o procedimento de `docs/deployment.md` com backup real e aprovação operacional.

Tarefa: T27 — Validar caches e filas de versões anteriores
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `resources/js/offline/offline-core.js`, `resources/js/offline/mesario-data.js`, `resources/js/offline/mesario-offline.js`, `resources/js/offline/chaveamento-engine.js`, `config/routes/compatibility.php`, `tests/browser/legacy-offline-compat.spec.cjs`, `tests/browser/clock-persistence.spec.cjs`, `tests/browser/occurrence-offline-edit.spec.cjs`, `tests/browser/offline-tournament-bracket.spec.cjs`, `tests/browser/score-persistence.spec.cjs`, `tests/browser/tournament-offline.spec.cjs`, `tests/browser/README.md` e este `STATUS.md`.
Defeito reproduzido: o novo cenário inseriu uma mutação no formato antigo, sem o campo `session`, com `X-SGI-Mutation-Id` pertencente à sessão autenticada. Antes da correção, `getPendingList()` retornou zero e nenhuma requisição saiu da fila (`1` falha em `2` testes; o teste da casca legada já passava). O cliente também foi exercitado com um registro de outro usuário, resposta idempotente sem fingerprint e CSRF antigo. Uma execução intermediária após a correção da fonte ainda não enxergou a mudança porque o navegador servia o asset público anterior; isso foi identificado e corrigido reconstruindo os assets, sem alterar o teste para mascarar o problema.
Arquivos alterados: `offline-core.js` agora adota um registro antigo sem `session` somente quando seu ID de mutação possui o namespace opaco da sessão atual; registros explicitamente de outro usuário permanecem retidos. O body e o ID não são reescritos, e `garantirIdMutacao` continua atualizando apenas o CSRF. Foi criado `legacy-offline-compat.spec.cjs` com fila pelo alias antigo, resposta sem fingerprint, isolamento, casca v2 antiga sem `pageSources`, ausência de Service Worker e falha de rede no primeiro envio com retry e fila preservada. `tests/browser/README.md` documenta os limites reais de refresh/nova aba/cold-open sem casca preparada e a limitação do formato de pausa sem snapshot v2. Nenhuma migração, banco de trabalho, alias, cache ou identificador de mutação existente foi removido.
Decisão aplicada: a compatibilidade de fila sem sessão usa somente evidência segura já presente no identificador da própria mutação; não foi adotada uma fila legada arbitrária por qualquer usuário. A casca continua aceitando registros antigos que possuem apenas `script`, HTML e CSS, executando-os sem exigir `schemaVersion` ou `pageSources`. O teste não declara suporte a cold-open sem shell e não adiciona Service Worker. IDs temporários negativos, ordem de materialização do chaveamento, aliases PHP/versionados, edição de ocorrência por ID e snapshots de cronômetro já cobertos pelas suítes existentes foram preservados.
Comandos executados e resultado de cada um:

- `npm --prefix tests/browser test -- legacy-offline-compat.spec.cjs` antes da correção, com banco/servidor descartáveis: reproduziu `1` falha em `2` testes; a fila antiga não foi enviada.
- `npm run build`: passou; `231` assets preparados. A execução anterior do teste contra o bundle público antigo foi descartada como validação da fonte e não foi convertida em aprovação.
- `npm --prefix tests/browser test -- legacy-offline-compat.spec.cjs` após a correção e o build, com `SGI_E2E_RESET=1`, servidor `http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: `3/3` testes aprovados; o global setup confirmou `374/374` asserções HTTP. O teste de retry terminou com duas tentativas e a fila em zero somente após a segunda confirmação.
- `git diff --check`: sem erros de whitespace; apenas avisos de conversão LF/CRLF do Git.
Evidência: o navegador enviou exatamente uma vez a mutação antiga pelo alias `lancar_resultado.php`, preservou o body e `X-SGI-Mutation-Id`, substituiu o CSRF antigo pelo token da sessão, aceitou resposta idempotente sem fingerprint e não enviou a linha de outro usuário. O registro antigo da casca montou com seu script único; a rede falhando na primeira tentativa deixou a fila visível e o retry posterior removeu somente a mutação confirmada. O cenário de pausa antiga permanece aceito pela regra de saldo legado, enquanto a referência temporal exata é exigida apenas no snapshot v2.
Dependências ou limitações restantes: refresh, nova aba ou abertura a frio sem uma casca autenticada previamente preparada não são garantia deste pacote, porque o código de referência não usa Service Worker; essa limitação está documentada em `tests/browser/README.md`. Registros legados sem sessão e sem um ID de mutação pertencente ao namespace atual não são adotados automaticamente por segurança e ficam fora da fila visível da sessão até revisão explícita.
Próximo passo exato: abrir `docs/plano-implementacao-luna/06-validacao-final.md`, ler somente T28 e as fontes nela listadas — `.github/workflows/ci.yml`, `compose.test.yml`, `docs/testing.md` e `docs/deployment.md` —, executar a matriz disponível, ensaiar subdiretório `/SGI` e recuperação somente em bancos/arquivos sintéticos, e registrar qualquer pendência externa sem alterar o CI para esconder falhas.

Tarefa: T11 — Consultar e editar a ocorrência certa offline
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `resources/js/offline/mesario-data.js`, `resources/js/offline/offline-core.js`, `resources/js/pages/competicoes/placar.js`, `src/Modules/Disciplina/Presentation/Http/OcorrenciaController.php`, `src/Modules/Disciplina/Infrastructure/MysqliOcorrenciaQueries.php`, `src/Shared/Database/SqlFilters.php`, `tests/javascript/mesario-data.test.cjs`, `tests/browser/occurrence-offline-edit.spec.cjs` e este `STATUS.md`.
Defeitos reproduzidos: o cenário com duas ocorrências distintas abriu o modal pelo ID solicitado, mas antes da correção o campo de descrição vinha vazio/da ocorrência errada porque a edição consumia a primeira posição da lista e a leitura offline não filtrava o identificador. Durante a validação da correção, a segunda edição deixou o botão desabilitado após a primeira gravação; a cadeia criação temporária→edição também ficou pendente porque o sincronizador usava o snapshot antigo da fila depois de resolver o POST. Cada falha foi reproduzida no navegador antes do respectivo ajuste; a primeira execução da edição offline falhou com valor diferente do texto alvo, e as execuções intermediárias falharam com botão desabilitado ou `pending=1` e mensagem de dependência não resolvida.
Arquivos alterados: `resources/js/offline/mesario-data.js`, `resources/js/offline/offline-core.js`, `resources/js/pages/competicoes/placar.js`, `src/Modules/Disciplina/Presentation/Http/OcorrenciaController.php`, `src/Modules/Disciplina/Infrastructure/MysqliOcorrenciaQueries.php`, `src/Shared/Database/SqlFilters.php`, `tests/javascript/mesario-data.test.cjs`, `tests/browser/occurrence-offline-edit.spec.cjs` e este `STATUS.md`. Nenhuma migração foi criada ou reescrita, nenhum banco de trabalho foi alterado e aliases, filas/cache anteriores e IDs de mutação foram preservados.
Decisão aplicada: `urlInfo` adapta apenas os aliases PHP antigos e os nomes das rotas versionadas já existentes. Consultas de ocorrência e ocorrência de turma filtram primeiro pelo ID comparado como string, aceitando `temp_*`; uma consulta única ausente retorna `[]` autoritativo e não a lista inteira. A projeção PUT mescla a linha existente, conserva as referências `[JOGO]`/`[TURMA]`, mantém `status_ocorrencia` e grava aliases `id_usuario`, `turmas_id_turma` e `jogos_id_jogo` para ocorrências temporárias. O editor consulta o ID exato e recusa abrir outra ocorrência quando o registro não está disponível. O backend preserva os prefixos de escopo no PUT e expõe o filtro/estado `status_ocorrencia`; a exclusão individual continua sendo PUT de inativação. Criações offline recebem `temp_<mutation_id>`; uma edição dependente registra `dependsOn` como metadado, mantém body e ID de mutação originais, só substitui o ID temporário no corpo da tentativa de rede após a resposta idempotente do mesmo POST e marca dependência não resolvida como 409/revisão. A fila relê cada item antes do envio para não reutilizar snapshots antigos. O escopo da fila continua separado por sessão/operador.
Comandos executados e resultado de cada um:

- `npm --prefix tests/browser test -- occurrence-offline-edit.spec.cjs` antes das correções: reproduziu a abertura da ocorrência errada/vazia; execuções intermediárias reproduziram botão desabilitado e dependência temporária presa em revisão.
- `npm run build`: passou; 231 assets preparados.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm test`: passou; 16/16 testes JavaScript, incluindo `tests/javascript/mesario-data.test.cjs` para URL versionada, ID ausente, filtro de status, ocorrência de turma, preservação de referências e aliases temporários.
- `npm --prefix tests/browser test -- occurrence-offline-edit.spec.cjs` após a correção: passou; 1/1 cenário. Validou duas ocorrências, edição por ID da segunda offline, sincronização real, criação+edição temporária antes do POST, ausência de duplicata e PUT de inativação com filtros `status_ocorrencia=1/0`.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPUnit 133 testes/1.637 asserções, PHPStan 189/189, PHP CS Fixer 242/242 e lint PHP sem erros.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, PHP 8.2 do XAMPP, servidor em `127.0.0.1:8110` e MariaDB descartável em `127.0.0.1:3308`: passou; 29/29 testes de navegador. O global setup confirmou 277/277 asserções HTTP.
- `git diff --check`: sem erros de whitespace; permaneceram somente avisos de conversão LF/CRLF do Git.
Evidência: o navegador consultou e abriu exatamente a segunda ocorrência, atualizou seu texto offline sem modificar a primeira, sincronizou a mesma linha no MySQL, resolveu uma criação temporária seguida de PUT sem duplicação e ocultou uma ocorrência inativada quando solicitado o filtro ativo. As rotas legada e versionada, a ocorrência de turma, os IDs temporários e a sessão do operador continuam compatíveis.
Dependências ou limitações restantes: ocorrências históricas sem prefixos não são reconstruídas por inferência; o backend só preserva referências existentes e continua recusando troca de jogo/turma. Dependências que não recebem resposta de criação permanecem em revisão para ação explícita, sem replay especulativo.
Próximo passo exato: abrir o README do plano, localizar somente a primeira tarefa incompleta T12, ler a etapa e as fontes nela listadas, reproduzir o defeito de arrecadação indicado antes de alterar código e registrar novamente arquivos, comandos, resultados e a próxima tarefa neste `STATUS.md`.

Tarefa: T10 — Persistir o placar antes da desmontagem
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `resources/js/pages/competicoes/placar.js`, `resources/js/offline/offline-core.js`, `resources/js/offline/mesario-data.js` e `tests/browser/score-persistence.spec.cjs`.
Defeito reproduzido: o novo cenário de navegador clicou em `+` e desmontou a tela imediatamente antes da janela antiga de 400 ms; ao voltar, o placar permaneceu `00`. Depois da primeira correção, a sequência offline já chegava à fila como `[2,3,2]`, mas a tela ainda mostrava `01`: a projeção local usava o alias `id_jogo`, enquanto a associação de pendência só comparava `jogos_id_jogo`. A falha simulada do IndexedDB também foi exercitada para garantir que a ação não anunciasse sucesso durável.
Arquivos alterados: `resources/js/pages/competicoes/placar.js`, `resources/js/offline/offline-core.js`, `resources/js/offline/mesario-data.js`, `tests/browser/score-persistence.spec.cjs` e este `STATUS.md`. O build atualizou os assets gerados em `public/assets/`, sem alterar contrato de origem. Não houve migração, alteração do banco de trabalho, publicação, mudança de aliases, filas/cache antigos ou identificadores de mutação.
Decisão aplicada: o atraso foi removido. Cada clique captura imediatamente jogo, partida e placar absoluto e entra numa cadeia por `id_partida`; para ID positivo, a única escrita passa por `SGIOffline.queueMutation('PUT', ...)`, seguida de `syncQueue(false)` apenas quando online. Partidas temporárias continuam usando a store local e não enviam `mm_local_*`/IDs negativos como reais. A finalização aguarda as cadeias pendentes; falhas locais são visíveis por alerta e não viram sucesso fictício. A leitura offline associa uma pendência PUT de partida ao jogo através da partida já projetada, aceitando os aliases legados `jogos_id_jogo` e `id_jogo`, sem mudar o body persistido nem coalescer entradas.
Comandos executados e resultado de cada um:

- `npm run build`: passou; 231 assets preparados.
- `npm --prefix tests/browser test -- score-persistence.spec.cjs --reporter=dot` com o banco/servidor descartáveis e preparação HTTP já concluída: 1/1 passou em 10,9 segundos. O cenário validou desmontagem imediata, sequência offline, fila, projeção local, reconexão e falha simulada do IndexedDB.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm test`: passou; 14/14 testes JavaScript.
- `composer verify`: passou; PHPUnit 133 testes/1.637 asserções, PHPStan 189/189, PHP CS Fixer 242/242 e lint PHP sem erros.
- `npm --prefix tests/browser test` com `SGI_E2E_RESET=1`, PHP 8.2 do XAMPP, servidor em `127.0.0.1:8110` e MariaDB descartável em `127.0.0.1:3308`: 28/28 cenários de navegador aprovados em 3,8 minutos; o global setup confirmou 277/277 asserções HTTP.
- `git diff --check`: sem erros de whitespace; permanecem apenas avisos de conversão LF/CRLF do Git.
Evidência: o primeiro `+` sobrevive à saída e retorno da tela como `01`; após `+ + −` offline, os três bodies absolutos da fila permanecem em ordem `[2,3,2]`, o IndexedDB e a tela mostram `02`, e a reconexão grava `2` no MySQL. A rejeição de IndexedDB exibe erro recuperável sem fila fictícia. Todos os cenários anteriores de cronômetro, placar, torneio, aliases e sincronização continuam verdes.
Dependências ou limitações restantes: a etapa não resolve ainda consulta/edição de ocorrências por ID, referências de criação temporária ou inativação offline; esses pontos pertencem à T11. Nenhuma migração foi criada ou reescrita e nenhum banco de trabalho foi usado.
Próximo passo exato: abrir `docs/plano-implementacao-luna/02-placar-offline.md`, ler somente T11 e as fontes necessárias — `resources/js/offline/mesario-data.js` (`urlInfo`, `localGet`, `project`, `itemAfetaLeitura`, `respostaLocalComDados`) e `resources/js/pages/competicoes/placar.js` (`editarOcorrencia`, `salvarOcorrencia`, `excluirOcorrencia`) —, reproduzir primeiro duas ocorrências distintas e a edição offline por ID da segunda, então corrigir consulta, projeção e resolução sem reescrever POSTs antigos.

Tarefa: T07 — Especificar e testar o cronômetro como regra pura
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `src/Modules/Competicoes/Domain/CronometroRules.php`, `tests/Unit/Modules/Competicoes/CronometroRulesTest.php` e este `STATUS.md`.
Defeito reproduzido: antes da implementação, o teste unitário novo foi executado contra a regra descrita no plano e falhou com `Class "App\\Modules\\Competicoes\\Domain\\CronometroRules" not found`: 9 testes, 0 asserções, 9 erros. A leitura do código vigente também confirmou que `MysqliJogoGateway::list()` recalculava um jogo iniciado sempre a partir de duração/extra, ignorando o saldo persistido, enquanto o JavaScript de pausa não enviava um snapshot explícito; a correção de integração fica deliberadamente para T08.
Arquivos alterados: `src/Modules/Competicoes/Domain/CronometroRules.php`, `tests/Unit/Modules/Competicoes/CronometroRulesTest.php` e este `STATUS.md`. Não houve migração, alteração do banco de trabalho, mudança de aliases, filas, cache ou identificadores de mutação.
Decisão aplicada: `CronometroRules` é uma regra pura, determinística e sem relógio global. Ela valida duração positiva, saldos/extras não negativos, estados permitidos e referências futuras sem aumentar o saldo; conserva `0` como saldo válido; usa `duracao + extra` somente como fallback legado quando o saldo é `NULL`; materializa o saldo antes de pausar, acrescentar tempo, alterar duração, receber saldo explícito ou concluir; não reinicia a referência ao retomar um jogo já iniciado; e aceita `Finalizado` como compatibilidade de estado antigo. O acréscimo e a duração recebidos como valores absolutos aplicam apenas a diferença, sem dupla contagem. A camada pura usa referência epoch inteira; o adaptador PHP/DATETIME e o replay serão tratados em T08.
Comandos executados e resultado de cada um:

- `C:\xampp\php\php.exe vendor/bin/phpunit --configuration phpunit.xml tests/Unit/Modules/Competicoes/CronometroRulesTest.php` antes da classe: 9 testes, 0 asserções, 9 erros reproduzidos.
- `C:\xampp\php\php.exe vendor/bin/phpunit --configuration phpunit.xml tests/Unit/Modules/Competicoes/CronometroRulesTest.php` após a implementação: 9 testes/29 asserções aprovados. Os casos confirmam 1200→1170 após 30 segundos, pausa estável, retomada em 1160 após 10 segundos, ausência de recuperação em pausa/retomada repetidas, extra absoluto 1170→1230 uma única vez, saldo zero preservado até receber extra, fallback legado somente com `NULL` e referência futura sem aumento.
- `C:\xampp\php\php.exe vendor/bin/phpunit --configuration phpunit.xml`: 133 testes/1.621 asserções aprovados.
- `C:\xampp\php\php.exe C:\Users\ferreira-mr\tools\composer\composer.phar verify`: passou; PHPStan 186/186, PHP CS Fixer 239/239 e lint PHP sem erros.
- `C:\xampp\php\php.exe tests/run_all.php` com `SGI_TEST_BASE_URL=http://127.0.0.1:8110/` e MariaDB descartável em `127.0.0.1:3308`: 269/269 asserções aprovadas.
- `git diff --check`: passou.
Evidência: o saldo é calculado exclusivamente do estado recebido e do instante informado; as transições reproduzem os limites de zero, pausa, retomada, extra absoluto, mudança de duração, saldo explícito e conclusão sem tocar persistência. A suíte HTTP completa continuou verde, e nenhum contrato de rota, alias, fila/cache ou ID de mutação foi alterado.
Dependências ou limitações restantes: T08 deve aplicar essa regra em `MysqliJogoGateway`, `JogoController`, `MutationAction`/transação e no replay, convertendo DATETIME sem mudar o contrato legado. O estado atual ainda contém a lógica de cronômetro espalhada no gateway e no JavaScript, portanto T07 não declara a persistência corrigida.
Próximo passo exato: abrir `docs/plano-implementacao-luna/02-placar-offline.md`, ler somente T08 e as fontes nela listadas — `JogoController.php`, `MysqliJogoGateway.php`, rotas de jogos, `MutationAction.php`, `Transaction.php` e testes de replay —, reproduzir primeiro a perda de saldo/dupla aplicação em uma sequência persistida de iniciar, pausar, retomar e sincronizar, então integrar `CronometroRules` sem alterar aliases, filas/cache, IDs de mutação ou migrações aplicadas.

Tarefa: T09 — Aplicar o contrato do cronômetro ao cliente offline e à projeção local
Estado: concluída.
Referência do código: branch `codex/refatoracao-arquitetura-limpeza`, sem commit criado; arquivos locais relevantes: `resources/js/shared/cronometro.js`, `resources/js/pages/competicoes/placar.js`, `resources/js/offline/mesario-data.js`, `resources/views/components/admin-head.php`, `resources/views/pages/competicoes/placar.php`, `src/Modules/Competicoes/Application/CronometroService.php`, `tests/javascript/cronometro.test.cjs`, `tests/browser/clock-persistence.spec.cjs`, `tests/Integration/CronometroPersistenceTest.php` e este `STATUS.md`.
Defeito reproduzido: antes do módulo compartilhado, `node --test tests/javascript/cronometro.test.cjs` falhou com `MODULE_NOT_FOUND` para `resources/js/shared/cronometro.js`. No navegador, o fluxo de jogo legado com `duracao_jogo` nula enviou snapshot v2 válido mas recebeu HTTP 400 (`duracao_jogo deve ser um inteiro válido`) e permaneceu `Agendado`; a preparação da base legada ocorria tarde demais no servidor. Depois da primeira integração, o torneio SPA reproduziu outro defeito: o clique de iniciar não emitia `PUT`, porque o shell capturava o script da página sem carregar o novo módulo compartilhado; o status ficava `Agendado`. A correção foi reproduzida novamente no torneio online/offline e passou.
Arquivos alterados: `resources/js/shared/cronometro.js`, `resources/js/pages/competicoes/placar.js`, `resources/js/offline/mesario-data.js`, `resources/views/components/admin-head.php`, `resources/views/pages/competicoes/placar.php`, `src/Modules/Competicoes/Application/CronometroService.php`, `tests/javascript/cronometro.test.cjs`, `tests/browser/clock-persistence.spec.cjs`, `tests/Integration/CronometroPersistenceTest.php` e este `STATUS.md`. O diagnóstico temporário usado em `tests/browser/tournament-offline.spec.cjs` foi removido e o arquivo ficou sem alteração final. Não houve migração, alteração do banco de trabalho, publicação, mudança de aliases, filas/cache existentes ou identificadores de mutação.
Decisão aplicada: a regra pura JavaScript (`SGICronometro`) calcula o saldo a partir da referência persistida, status, duração e extra, sem decremento cego; o cliente usa o offset de `servidor_epoch_ms`, materializa o saldo ao pausar/retomar/acrescentar tempo e envia snapshot v2 junto dos campos legados. A projeção e a resposta de sincronização atualizam IndexedDB preservando `_pendente`; falhas de gravação local ou rede ficam visíveis e não são declaradas como sucesso. O módulo compartilhado foi carregado pelo cabeçalho comum do Mesário para também existir no shell SPA e em caches antigos. No servidor, a compatibilização de duração/extra legados ocorre antes da aplicação do snapshot v2. A asserção PHP que dependia da fronteira exata de `NOW()` foi tornada determinística, mantendo a exigência de saldo materializado entre 1.169 e 1.170 segundos e igualdade com a resposta persistida.
Comandos executados e resultado de cada um:

- `node --test tests/javascript/cronometro.test.cjs` antes do módulo: falhou com módulo ausente; após a implementação: 9/9 testes aprovados.
- `npm run build`: passou; 231 assets preparados.
- `npm run check`: passou; 40 arquivos JavaScript válidos.
- `npm test`: passou; 14/14 testes JavaScript.
- `composer verify`: passou; PHPUnit 133 testes/1.637 asserções, PHPStan 189/189, PHP CS Fixer 242/242 e lint PHP sem erros.
- `npm --prefix tests/browser test -- clock-persistence.spec.cjs`: passou; aceitação T09 1/1.
- `npm --prefix tests/browser test -- tournament-offline.spec.cjs`: passou após a correção do shell; 2/2 cenários.
- `npm --prefix tests/browser test` com `SGI_BASE_URL=http://127.0.0.1:8110/`, `SGI_TEST_BASE_URL=http://127.0.0.1:8110/`, PHP 8.2 do XAMPP e MariaDB descartável: passou; 27/27 testes. O global setup executou `C:\xampp\php\php.exe tests/run_all.php` e confirmou 277/277 asserções HTTP.
- `git diff --check`: passou; apenas avisos de conversão LF/CRLF do Git, sem erro de whitespace. A busca pelo diagnóstico temporário não encontrou resultados.
Evidência: a aceitação controlou um jogo de 1.200 segundos, consumiu aproximadamente 30 segundos, pausou, navegou para outra tela e voltou sem resetar o saldo; retomou, consumiu aproximadamente mais 10 segundos, pausou offline e avançou 45 segundos virtuais sem reduzir o saldo congelado. O teste inspecionou a linha do jogo no IndexedDB, o corpo v2 na fila, a remoção da pendência após sincronização e a consulta do servidor com saldo/status compatíveis. O torneio comprovou que a carga compartilhada continua funcionando na SPA e preserva o fluxo online/offline.
Dependências ou limitações restantes: T09 não altera registros históricos nem cria outbox novo; IDs temporários negativos, partidas `mm_local_*`, aliases, filas/cache antigos, IDs de mutação, MySQL/MariaDB e migrações aplicadas permanecem compatíveis. A persistência imediata e ordenada do placar antes da desmontagem ainda pertence a T10.
Próximo passo exato: abrir `docs/plano-implementacao-luna/02-placar-offline.md`, ler somente T10 e as fontes nela listadas — `placar.js` (`agendarSalvarPartida`, `salvarPartida`, `ajustarGols`, `finalizarJogo` e callbacks de limpeza), `offline-core.js` (`queueMutation`, `syncQueue`) e `page-runtime.js` —, reproduzir primeiro clique + seguido de navegação imediata antes de 400 ms, verificar a fila em ordem e a projeção no IndexedDB, então corrigir a persistência sem criar outro outbox nem alterar corpos antigos.
