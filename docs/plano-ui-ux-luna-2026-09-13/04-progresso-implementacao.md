# Progresso da implementação UI/UX

Checkout de referência e atual: `914ff13cec97b3ab79a3420dedb8753b5e940cb9`.
Registro iniciado em 13/09/2026. O plano e as evidências originais foram preservados.

## Linha de base e estado inicial

- `git status --short`: somente `?? docs/plano-ui-ux-luna-2026-09-13/` antes deste registro.
- A pasta da auditoria é material fornecido pelo usuário e permanece preservada.
- Primeira tentativa: `powershell -ExecutionPolicy Bypass -File tools/test-local.ps1 -Suite all -DatabaseBackend local -DatabaseHost 127.0.0.1 -DatabasePort 3306 -DatabaseUser root -DatabasePassword '' -IncludeVisual`. Resultado **código 1**: qualidade PHP (**366 testes, 2.535 asserções**), PHPStan (**235/235 sem erros**), build, checks JS e **101 testes JS** passaram; a etapa HTTP/banco/recuperação parou antes dos cenários porque `127.0.0.1:3306` recusou a conexão. Um segundo perfil local com backend Docker teve falha de inicialização (`MySQL server has gone away`).
- Linha de base completa aprovada pelo executor Compose oficial: `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual`, **código 0**. Incluiu qualidade, integração, navegador e visual: PHPUnit **366 testes/2.535 asserções**, PHPStan **235/235**, JS **101/101**, Playwright **64 testes** e contrato visual **2 testes**. Saída em `test-results/ui-ux-audit-20260913/baseline-docker-E00.log`.
- Reprodução dinâmica E00 aprovada no Compose descartável `sgi-test-uiux-e00`: integração **987/987 asserções**, depois Playwright filtrado **1/1**. A caracterização reproduziu U01/U02: fundo transparente na navegação do aluno e conteúdo principal começando sob a sidebar nas telas de perfil e modalidades; reproduziu U03: `scrollWidth` maior que 320px nas três telas administrativas medidas a 320px. Inspecionei no relatório HTML as capturas de perfil, inscrições, painel, modalidades e colaboradores: o perfil começa à esquerda do limite da sidebar; inscrições ficam atrás dos ícones do menu; painel/modalidades mostram conteúdo maior que o viewport em retrato, e a barra compacta compete com o cabeçalho. As medições foram anexadas como JSON ao relatório Playwright. A imagem de navegador foi reconstruída para conter a versão final do teste de caracterização.
- O Compose isolado foi removido ao terminar (`down --volumes --remove-orphans` somente para `sgi-test-uiux-e00`). As primeiras tentativas filtradas falharam por configuração do harness (imagem desatualizada e troca de persona sem logout); corrigidas sem alterações de produto, a repetição aprovada confirmou a reprodução.
- Evidência estática anterior da auditoria: U01–U04 em `03-validacao.md` e capturas em `evidencias/`. A reprodução autenticada acima confirma U01–U03 no navegador em execução.

## Etapas

| Etapa | Estado | Achados atendidos | Arquivos | Testes e evidências | Pendências |
| --- | --- | --- | --- | --- | --- |
| E00 | validado (linha de base e caracterização pré-correção) | U01, U02, U03 | `tests/browser/frontend-regression.spec.cjs`; logs em `test-results/ui-ux-audit-20260913/` | `test-docker.ps1 -Database mariadb -IncludeVisual` código 0; integração Compose filtrada 987/987; `frontend-regression.spec.cjs --grep E00` 1/1. Capturas e medições anexadas ao relatório Playwright. | A caracterização de overflow U03 continua deliberadamente ativa até E02; as capturas pré-correção permanecem no relatório de E00. |
| E01 | validado | U01, U02, U13 | componentes `admin-nav`, `aluno-nav`, `admin-header`, `admin-head`, `aluno-head`, `page-title`; `shared.scss`, `admin.css`; perfil/modalidade/dashboard/agenda; `page-runtime.js`, `mesario-offline.js`; testes `PageTitleTest.php`, `frontend-regression.spec.cjs`, `mesario-offline.spec.cjs` | `frontend-regression.spec.cjs --grep "E01 corrige a navegação contextual"` 1/1; `mesario-offline.spec.cjs` 1/1; `PageTitleTest.php` 4/4 (117 asserções); `test-docker.ps1 -Database mariadb -IncludeVisual` código 0 (366 PHPUnit/2.535 asserções, PHPStan 235/235, JS 101/101, integração 987/987, navegador 65/65, visual 2/2). Log completo: `test-results/ui-ux-audit-20260913/E01-all-IncludeVisual.log`; capturas e geometria no relatório Playwright. | Nenhuma para E01. Overflow U03 permanece como baseline até E02. |
| E02 | validado | U03, U19 | `resources/css/source/{admin,aluno-shared,utilities}.css`; `resources/scss/shared.scss`; `resources/views/components/admin-header.php`; `resources/views/pages/{aluno/home,competicoes/modalidades,eventos/categorias,eventos/configurar-categorias,eventos/configurar-modalidades,eventos/configurar-resumo}.php`; `resources/js/pages/eventos/configurar-resumo.js`; `tests/browser/frontend-regression.spec.cjs`; `tests/javascript/{responsive-regression,css-bundles}.test.cjs` | `npm run build`; `node --check tests/browser/frontend-regression.spec.cjs`; JS focados 59/59; Compose isolado `e02b`: integração 987/987 e browser grep E02 3/3; Compose `/SGI/`: integração 987/987, deployment-paths + E02 4/4; `test-docker.ps1 -Database mariadb -IncludeVisual` código 0, 366 PHPUnit/2.535 asserções, PHPStan 235/235, JS 101/101, integração 987/987, browser 67/67, visual 2/2. Logs: `test-results/ui-ux-audit-20260913/E02-all-IncludeVisual-final.log`, `E02-subdir.log`; capturas inspecionadas no relatório da execução. | Nenhuma em E02. A ação “Adicionar categoria” do resumo é defeito separado U10 e permanece para correção E08; E02 moveu sua região para o fluxo sem afirmar que o destino existe. |
| E03 | validado | U06, U08 (controles do placar e senha), U13, U14 | `resources/js/shared/page-runtime.js`, `resources/scss/shared.scss`, `resources/css/source/{admin,aluno-shared}.css`; componentes `admin-header`, `admin-nav`, `aluno-nav`, `page-title`; templates e scripts de acesso, aluno, eventos, competições, participantes, disciplina e rankings; `tests/browser/{aluno-portal,frontend-regression,e03-*-accessibility}.spec.cjs` | Regressão específica E03 13/13; executor completo MariaDB `test-docker.ps1 -Database mariadb -IncludeVisual`, código 0: PHPUnit 366/366 (2.535 asserções), PHPStan 235/235, JS 101/101, integração 987/987, Playwright 80/80, visual 2/2. Logs: `E03-final-targeted.log` e `E03-all-IncludeVisual-final2.log`. | Nenhuma. Revisão posterior à primeira validação reabriu a etapa: caracterização Playwright reprovou pelos ícones decorativos ainda expostos; a regressão do tempo extra também encontrou ausência de mensagem associada e falta de validação do limite superior. O seletor dinâmico foi restringido ao controle do acréscimo; os 13 casos e a repetição completa passaram. Snapshots visuais não foram alterados. |
| E04 | validado | U04, U05, U08, U17 | login e perfil/primeiro acesso/foto, navegação, `Kernel.php`, `AssetResponder.php`; regressões E04 de login, perfil e primeiro acesso | Regressões focadas 10/10 e execução final `test-docker.ps1 -Database mariadb -IncludeVisual` aprovada: PHPUnit 366 testes/2.536 asserções, PHPStan 235/235, JS 102/102, integração 987/987, Playwright 95/95 sem retry e visual 2/2. Log final: `test-results/ui-ux-audit-20260913/E04-all-IncludeVisual-final4.log`. Teste dedicado cobriu 1024, 1199 e 1200px; revisão visual do login e snapshots Linux confirmados; implantação `/SGI` validada em execução isolada anterior. | Nenhuma em E04. |
| E05 | validado | U06–U09, U12, U21 | `eventos/configurar-turmas`, `eventos/{categorias,configurar-categorias,configurar-modalidades,configurar-locais}`, `participantes/turma-alunos`, `aluno/modalidade`, `competicoes/equipe-alunos`, `acesso/colaboradores`; testes `e03-configurar-turmas-accessibility`, `e05-category-selection`, `e05-pdf-import-accessibility`, `e05-selection-accessibility`, `frontend-regression`, `css-bundles` | Red E05 13 falhas reproduzidas; regressões focadas 40/40; `test-docker.ps1 -Database mariadb -IncludeVisual` código 0: PHPUnit 366/2.536, PHPStan 235/235, JS 102/102, integração 987/987, Playwright 109/109, visual 2/2. Capturas E05 revisadas em `test-results/ui-ux-audit-20260913/E05-*.png`; logs `E05-red-complete.log`, `E05-focused-browser.log`, `E05-all-IncludeVisual-final.log`. | Nenhuma. Sem snapshots alterados; não foram adicionadas dependências, endpoints, migrações ou regras de negócio. |
| E06 | validado | U11, U18, U20 | `resources/js/pages/{eventos/configurar-agenda,competicoes/chaveamento,aluno/jogos}.js`; views de agenda, chaveamento e aluno; `resources/css/source/{admin,aluno-shared}.css`; regressões `e06-{agenda-accessibility,bracket-accessibility,student-game-details}.spec.cjs` e `responsive-regression.test.cjs` | Red antes da correção: 10 falhas/1 aprovado. Regressão focada E06: 15/15. `test-docker.ps1 -Database mariadb -IncludeVisual`, código 0: PHPUnit 366/2.536, PHPStan 235/235, JS 102/102, integração 987/987, navegador 124/124, visual 2/2. Capturas E06 de agenda 640×360, KVS aberto e detalhe de partida inspecionadas; sem atualização de snapshot. | Nenhuma. |
| E07 | validado | U08, U13, U22 (placar, tempo, ocorrências e estado offline) | `resources/views/pages/competicoes/placar.php`; `resources/js/pages/competicoes/placar.js`; `resources/js/offline/{offline-core,mesario-offline}.js`; `resources/scss/shared.scss`; `resources/css/source/admin.css`; `tests/browser/{clock-persistence,mesario-offline,e07-offline-feedback}.spec.cjs`; testes JavaScript de CSS | `npm run build`; `npm run check`; `npm test` 102/102; regressões JavaScript focadas 59/59; navegador focado 2/2; execução integral `test-docker.ps1 -Database mariadb -IncludeVisual`, código 0: PHPUnit 366/2.536, PHPStan 235/235, JS 102/102, integração 987/987, Playwright 127/127, visual 2/2. Captura 320px do banner revisada. | Nenhuma. |
| E08 | validado | U10, U21 | templates/scripts `eventos/{configurar-resumo,configurar-pontuacao,configurar-modalidades}`; `tests/browser/{e08-edition-setup,frontend-regression}.spec.cjs` | Red E08: 8/8 cenários antes da correção; focados E08 + ciclo administrativo 14/14 e pós-ajuste H1/foco 2/2. `test-docker.ps1 -Database mariadb -IncludeVisual`, código 0: PHPUnit 366/2.536, PHPStan 235/235, JS 102/102, integração 987/987, Playwright 134 aprovados/1 flaky E05, visual 2/2. Depois, o teste de upload E05 foi sincronizado com a escolha do arquivo/lista e recebeu timeout de 15s e contagem de chamadas; o caso passou 3/3 com `--repeat-each=3`. Capturas de resumo, ativação e alterações não salvas inspecionadas. | Nenhuma; confirmar novamente o perfil oficial completo ao concluir E09. Nenhuma referência visual alterada por E08. |
| E09 | implementado; validação final de navegador impedida por requisito identificado | U18, U21; estados de erro, vazio válido e retry nos fluxos cobertos | scripts de listas/resumos/rankings e `aluno/jogos`; specs `tests/browser/e09-*.spec.cjs` (incluindo `e09-participant-turmas-recovery.spec.cjs`) | Reprodução red: HTTP 500 em turmas virou “Nenhuma turma cadastrada”; resposta 503 em jogos também virou vazio. Após as correções, o foco E05/E09 passou **25/26** antes do último ajuste de reconexão do acordeão; a alteração final força `SGIOffline.checkServer(true)` após a primeira falha e libera o estado de carregamento. O teste novo de turmas passou no foco. O executor oficial repetido depois das alterações finais (`test-docker.ps1 -Database mariadb -SkipBrowser -Keep`) passou com código 0: PHPUnit 366/2.536, PHPStan 235/235, JS **103/103**, integração 987/987. A repetição Playwright posterior ao último ajuste foi bloqueada pelo revisor automático por limite de uso; portanto E09 não é marcado como validado. | Reexecutar foco E05/E09 e `all -IncludeVisual` quando o executor de navegador puder ser autorizado; confirmar captura do acordeão após reconexão. |
| E10 | implementado; validação visual de navegador pendente | U19, U22, U24; consolidação de CSS, fontes locais, assets do login e impressão | `resources/scss/{_theme,bootstrap-theme,shared}.scss`; `resources/css/source/{admin,aluno-home,aluno-pages,login}.css`; heads, login e rankings; `tests/javascript/css-bundles.test.cjs` | `npm run build` código 0; `node --test tests/javascript/css-bundles.test.cjs` **53/53**; `node --check tests/browser/accessibility.spec.cjs`; `npm run check` **43/43**; `npm test` **103/103**; executor `test-docker.ps1 -Database mariadb -SkipBrowser -Keep` código 0: PHPUnit **366/2.536**, PHPStan **235/235**, integração **987/987**. `git diff --check` sem erro após limpeza do EOF. Removida importação Google Fonts; overrides `.d-md-*` agora são escopados a `.sgi-app-shell`; offsets `.mobile-nav`/token obsoletos removidos após busca dos consumidores; impressão de ranking consolidada no bundle compartilhado; banners do login passaram a backgrounds locais por composição visível. | Inspecionar capturas/requests e impressão em navegador; não atualizar snapshots sem revisão visual. |
| E11 | implementado; execução completa pendente | U23 e critérios transversais de teclado/nome/foco/impressão | novo `tests/browser/accessibility.spec.cjs`; cobertura E03/E04/E06/E07/E09 preservada | O novo spec cobre labels e envio por teclado no login, menu compacto com retorno de foco e ranking impresso com uma variante. Sintaxe e checks estáticos passam. A execução Playwright completa e a inspeção comparativa permanecem pendentes pelo mesmo bloqueio de uso do revisor automático. | Rodar `all -IncludeVisual`, revisar capturas e fechar o registro final sem declarar aprovação parcial. |

## Registro de execução por etapa

### E01 — validado

- **Reprodução anterior:** a sessão autenticada E00 mostrou sidebar do aluno transparente, mains cobertos pela navegação em 1440px e ausência de links contextuais para inscrições; o menu administrativo ainda identificava incorretamente telas sem destino correspondente. As capturas E00 continuam como referência pré-correção.
- **Mudança:** fundo e rótulos visíveis na sidebar dos dois portais, estado ativo sem depender de ícone `-fill`, páginas de inscrições e retornos preservando a edição, offsets corretos do perfil/inscrições, link de salto para o main, título específico e foco em `h1`/`h2` nas trocas SPA do mesário.
- **Testes:** regressão E01 no navegador autenticado (primeiro acesso, termos, menu desktop/móvel, contraste, skip link, destino/contexto e larguras); fluxo offline com título/foco em agenda e placar; `PageTitleTest` confere renderização escapada. O executor completo E00–E01 passou com Compose MariaDB.
- **Evidência visual:** inspecionei capturas de perfil e inscrições após a correção; a sidebar tem fundo SESI, texto legível e o conteúdo começa depois de x=80px. Não houve atualização de snapshots; o contrato visual existente de login passou sem mudança de referência.
- **Pendências:** nenhuma em E01. O teste continua registrando overflow administrativo a 320px para a correção E02.

Para cada etapa, atualizar o estado para `implementado`, `validado` ou `impedido por requisito identificado`. Registrar reprodução antes/depois, IDs U atendidos, arquivos alterados, comandos e códigos de saída, evidência visual inspecionada e limitações. Não marcar `validado` com apenas build/lint.

### E02 — validado

- **Reprodução anterior:** a linha de base E00 registrou U03 em 320px nas telas de dashboard, modalidades e colaboradores (documentos de 414px, 416px e 350px) e ações de categoria/modalidades fixadas sobre a área útil. A navegação compacta ficava no canto superior direito sem medição de colisão com título, banner offline ou foco.
- **Mudança:** o dashboard agora usa uma coluna abaixo de 576px e preserva duas colunas em 640px; os cards compactos de colaboradores empilham conteúdo e acomodam quatro dígitos/nome; grupos de modalidade quebram sem encolher controles. As ações que usavam `top:85%` ou rodapé fixo foram removidas do overlay e colocadas no fluxo de categorias, configuração de categorias, resumo e modalidades de competição, mantendo edição, modal e links existentes. O cabeçalho móvel reserva espaço ao menu fixo e a home do aluno posiciona saudação abaixo do botão; rolagem por foco respeita o menu e o banner offline. Os links e consultas de resumo usam `SGI_BASE_PATH`/`SGI_API_BASE`.
- **Testes específicos:** `npm run build` (código 0); `node --check tests/browser/frontend-regression.spec.cjs` (código 0); `node --test tests/javascript/responsive-regression.test.cjs tests/javascript/css-bundles.test.cjs` (59/59). Compose MariaDB descartável `sgi-test-uiux-e02b`: integração 987/987 e E01+E02 no `frontend-regression.spec.cjs` (3/3). O teste mede documentos sem overflow em 320/390/640, cartões em 1/2 colunas, foco em linhas e ações, 11 modalidades, Escape/foco de retorno do modal e colisão da saudação longa.
- **Raiz e subdiretório:** Compose descartável `/SGI/` com override temporário em `test-results/ui-ux-audit-20260913/compose-subdir.yml`: integração 987/987; `deployment-paths.spec.cjs` e os cenários E02 (4/4). O resumo recebeu HTTP 200 em `/SGI/api/v1/modalidades`, `/categorias` e `/turmas`; os links preservaram `/SGI`.
- **Execução completa final:** `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual` (código 0): PHPUnit 366/366, 2.535 asserções; PHPStan 235/235; JavaScript 101/101; integração 987/987; Playwright 67/67; visual 2/2. Log: `test-results/ui-ux-audit-20260913/E02-all-IncludeVisual-final.log`. A primeira execução completa antes do ajuste de menu também passou, mas a evidência válida da etapa é a repetição final.
- **Evidência visual:** inspecionei capturas de home do aluno em 320/390px com saudação longa e do resumo em 320px; o menu não cobre títulos e o resumo mantém os cards/ações visíveis. Capturas do relatório anexadas às execuções; nenhuma referência visual foi atualizada.
- **Pendências:** nenhuma em E02. “Adicionar categoria” no resumo continua apontando para alvo inexistente e será corrigido em E08 (U10); esse contrato foi mantido explícito, sem declarar o botão funcional nesta etapa.

### E03 — validado

- **Reprodução anterior:** U06, U08, U13 e U14 foram confirmados no inventário; templates renderizados tinham campos/ações sem nome programático, modais sem associação ao título, hierarquia de headings insuficiente e componentes operáveis fora da ordem de teclado. E00 forneceu linha de base completa aprovada antes das refatorações.
- **Mudança:** associei rótulos e IDs únicos aos campos; dei nomes específicos a ações só com ícone; associei modais ao título visível; organizei headings e grupos; acrescentei foco visível, atalho para o conteúdo e atualização de heading/foco nas reentradas do runtime. Corrigi o retorno de foco ao acionador no modal de detalhes do aluno. Bootstrap continua responsável pela abertura, Escape e contenção de foco; os papéis, opções, vínculos e contratos offline permanecem cobertos pelos testes existentes.
- **Arquivos:** `resources/js/shared/page-runtime.js`, `resources/scss/shared.scss`, `resources/css/source/admin.css`, `resources/css/source/aluno-shared.css`; `resources/views/components/{admin-header,admin-nav,aluno-nav,page-title}.php`; páginas e scripts de `acesso/colaboradores`, ambos os perfis, `aluno/{home,modalidade,ranking}`, `competicoes/{chaveamento,equipe-alunos,jogos,modalidade-detalhes,modalidades,placar}`, `disciplina/ocorrencias`, `eventos/{categorias,configurar-agenda,configurar-arrecadacao,configurar-categorias,configurar-equipes,configurar-locais,configurar-modalidades,configurar-pontuacao,configurar-resumo,configurar-turmas,dashboard,lista}`, `participantes/{turmas,turma-alunos}` e `resultados/ranking`; testes `tests/browser/{aluno-portal,frontend-regression,e03-admin-roster-accessibility,e03-configurar-turmas-accessibility,e03-edition-accessibility,e03-other-modals-accessibility,e03-score-occurrence-accessibility}.spec.cjs`.
- **Testes específicos:** `npm run build` e verificações de sintaxe JS passaram; Compose descartável E03: integração 987/987 e testes E03 13/13. Testes exercitam `getByLabel`/`getByRole`, labels que focam controles, IDs únicos, Escape, retorno de foco, abertura/fechamento repetidos, foco responsivo, nomes de ícones decorativos e limite de 1–30 minutos com erro ligado por `aria-describedby`. O caso modal de perfil espera `shown.bs.modal` durante a transição de 300 ms.
- **Revisão e reprodução complementar:** a revisão final encontrou chevrons da agenda, ícone “adicionar” mobile e ícones do painel de mesário sem `aria-hidden`; a caracterização Playwright falhou nesses pontos antes da correção. No acréscimo de tempo, o código não mostrava erro junto ao campo e não checava o máximo; o novo teste cobre 0 e 31. A primeira chamada do teste usou nome exato incompatível com o glifo do botão; o seletor foi restrito ao container do acréscimo e então passou.
- **Execução completa final:** `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual` (código 0): PHPUnit 366/366 com 2.535 asserções, PHPStan 235/235, JavaScript 101/101, integração 987/987, Playwright 80/80 e contrato visual 2/2. Logs: `test-results/ui-ux-audit-20260913/E03-final-targeted.log` e `E03-all-IncludeVisual-final2.log`.
- **Evidência visual:** inspecionei após a mudança capturas de categorias, formulários de modalidade e jogo, ranking e gestão de turmas. As referências visuais do login passaram sem diferenças; não atualizei snapshots, pois não eram necessárias mudanças visuais nessas referências.
- **Pendências:** nenhuma em E03. Retrato/paisagem em páginas de outras etapas e critérios de zoom/reflow permanecem nos respectivos itens do plano.

### E04 — validado

- **Linha de base antes da edição:** o executor completo após E03 passou com MariaDB descartável: PHPUnit 366/366 (2.535 asserções), PHPStan 235/235, JavaScript 101/101, integração 987/987, navegador 80/80 e visual de login 2/2. `node --test tests/javascript/first-login-password.test.cjs` passou 1/1 antes de E03. O código atual ainda confirma banner alto/campos de 32px em retrato, ajuda estática sem ação, ausência de trava de envio, campos de perfil com rótulos incorretos, toggles fora da sequência Tab e verificação de foto em caminho legado.
- **Login (itens 1–4):** telas mobile e desktop agora têm título e labels visíveis, campos/botão de 48px, banner proporcional ao viewport, ajuda local expansível e estado de envio “Entrando…” com trava/`aria-busy`; falha de rede restaura o botão e preserva a matrícula para retry. Não mudou a matrícula alfanumérica, `autocomplete`, payload, redirect nem resposta genérica de credenciais inválidas.
- **Teste login:** `e04-login.spec.cjs` passou 3/3 em Compose MariaDB isolado após setup de qualidade (366 PHPUnit, PHPStan 235/235, JS 101/101) e integração 987/987. Inclui 320×568, 390×844, 640×360, 1440×900, ajuda nas duas composições, request único, erro de rede e retry. A primeira tentativa falhou por nome transitório do botão no teste; locator corrigido, repetição verde. Capturas `E04-login-mobile-{320,390}.png` inspecionadas, sem alterar snapshots.
- **Reprodução red de perfil:** antes de editar, testes independentes confirmaram a matrícula sob o rótulo duplicado “E-mail”, toggles do perfil com `tabindex=-1` e ausência de botões de mostrar senha no primeiro acesso. Falhas de setup de testes foram corrigidas isolando alunos e aguardando a transição `shown.bs.modal`; os asserts de produto foram preservados.
- **Perfil/primeiro acesso/foto:** rótulos agora identificam matrícula e papel nível 3 como “Aluno”; informações estáticas “Online” e “Senha criptografada” foram removidas. Os toggles do perfil e da senha obrigatória são acionáveis por Tab/Enter/Espaço, têm nome/estado ARIA e preservam foco ao alternar. O listener do perfil administrativo é registrado antes da consulta assíncrona de interclasse. Os templates não embutem `src`/`onerror` nos quatro avatares do perfil: os handlers são instalados antes da URL da foto e exibem fallback para ausência, falha da consulta ou arquivo inacessível. Upload só conclui quando HTTP e payload confirmam sucesso; falhas 400/500 mantêm a edição recuperável. Remoção offline sinaliza pendência sem recarregar nem ocultar a foto.
- **Privacidade e rotas da foto:** os menus consultam `StoragePaths::fotosUsuarios()` para verificar o arquivo e geram somente a URL lógica via `Url`; bytes de `/uploads/fotosUsuarios/...` passam pela sessão e pelas pendências de senha/termos, com cache `private, no-store`. O endpoint `/api/v1/foto` e a autorização existente foram preservados. Testes confirmam GET anônimo redirecionado ao login e acesso autenticado privado.
- **Teste focal perfil/primeiro acesso:** Compose MariaDB green15 passou qualidade e integração (**987/987**) e `e04-profile-accessibility.spec.cjs` **11/11**. Cobriu primeiro acesso e termos, toggles por teclado nos dois perfis, labels, upload inválido e HTTP 500 com payload de sucesso falso, upload real com CSRF, recarga/navegação/avatar/fallback, acesso privado à foto e remoção offline do mesário observada na fila IndexedDB real (incluindo CSRF, sem reload e mantendo a foto pendente). Log: `test-results/ui-ux-audit-20260913/E04-profile-green15.log`. `UrlTest.php` passou 1/1 (4 asserções) incluindo `/SGI/uploads/fotosUsuarios/...`; `first-login-password.test.cjs` passou 2/2. A rota `/SGI` também foi servida e testada em Compose isolado: integração 987/987 e teste do retorno de perfil 1/1.
- **Sincronização e breakpoints:** a revisão do Playwright encontrou duas esperas frágeis no próprio teste: logout aguardava o redirect só depois do clique e o fechamento de modal não esperava remover o backdrop do Bootstrap. O runner focado passou 10/10 após sincronização por evento/estado. `e04-login.spec.cjs` agora verifica também 1024, 1199 e 1200px para resguardar a composição administrativa no limite de 1200px. Sintaxe, build e `git diff --check` passaram.
- **Execução completa final:** `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual` (código 0): PHPUnit **366 testes/2.536 asserções**, PHPStan **235/235 sem erros**, checks JS para 43 arquivos, testes JavaScript **102/102**, integração HTTP **987/987**, Playwright **95/95 sem retry** e contrato visual **2/2**. Log: `test-results/ui-ux-audit-20260913/E04-all-IncludeVisual-final4.log`. O ambiente temporário foi removido pelo executor.
- **Evidência visual:** inspecionei login normal e com erro em mobile e desktop; banners, labels, alvos de 48px, ajuda e estado de erro ficam legíveis. Atualizei somente os quatro snapshots Linux afetados do login e o teste visual passou 2/2; não copiei referências entre plataformas.
- **Pendências E04:** nenhuma.

### E05 — validado

- **Reprodução antes da correção:** `E05-red-complete.log` registra 13 falhas de comportamento no fluxo de cadastro/seleção/importação. Em revisão complementar, testes red separados reproduziram HTML inválido dentro dos botões de seleção de categoria/modalidade e lista de alunos desatualizada/botão preso após importação confirmada (`E05-review-html-red.log`, `E05-review-mobile-html-red.log`, `E05-review-success-red.log`).
- **Turmas e categorias:** seleção por categoria está disponível também no layout compacto; “Adicionar turma” só habilita depois de uma escolha válida e usa essa mesma categoria ao listar/criar, inclusive quando o parâmetro recebido é inválido. Os cartões de seleção e os links de detalhes são controles distintos, operáveis por teclado, sem botões ou headings aninhados. O limite de PDF informa 20 MB e deixa claro que os limites reais do PHP/servidor também se aplicam.
- **Importação PDF:** os inputs nativos estão visíveis e nomeados. Um upload só confirma sucesso com HTTP bem-sucedido e JSON `{success: true}`; HTML, JSON inválido, status 500 ou `success: false` mantêm a turma/arquivo recuperáveis para retry sem duplicar a turma. Após sucesso, a lista de alunos é recarregada, a busca/contexto são mantidos e o formulário libera o próximo arquivo. A barra de progresso anuncia o estado indeterminado sem inventar percentual e expõe valores ARIA quando há total conhecido.
- **Inscrição e equipe:** cartões anunciam elegibilidade e motivo de indisponibilidade sem mudar a regra de até três modalidades, gênero, capacidade ou vínculo. A seleção de equipe descreve cada aluno e a matrícula, conta apenas novos selecionados, mantém os membros existentes selecionados e impedidos de remoção, e atualiza o rótulo do botão com a quantidade. A revisão visual cobriu a lista vazia real e, no cenário sintético do teste, alunos selecionados e já vinculados.
- **Colaboradores:** senhas são mascaradas com alternância acessível; edição com campo vazio preserva a senha. O teste usa matrícula numérica, em conformidade com a normalização digits-only já existente, e verifica login após a edição.
- **Arquivos e testes:** templates/scripts de `eventos/configurar-turmas`, `eventos/{categorias,configurar-categorias,configurar-modalidades,configurar-locais}`, `participantes/turma-alunos`, `aluno/modalidade`, `competicoes/equipe-alunos`, `acesso/colaboradores`; `tests/browser/{e03-configurar-turmas-accessibility,e05-category-selection,e05-pdf-import-accessibility,e05-selection-accessibility,frontend-regression}.spec.cjs` e `tests/javascript/css-bundles.test.cjs`. O passe focado — `e03-configurar-turmas-accessibility`, `e03-admin-roster-accessibility`, os três specs E05, `aluno-portal`, `configuration-name-xss`, `frontend-regression` e `admin-lifecycle` — passou **40/40** (`E05-focused-browser.log`).
- **Execução completa:** `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual`, código 0: PHPUnit **366 testes/2.536 asserções**, PHPStan **235/235**, JavaScript **102/102**, integração **987/987**, Playwright **109/109** e visual **2/2**. Log: `test-results/ui-ux-audit-20260913/E05-all-IncludeVisual-final.log`. O ambiente completo foi removido pelo executor.
- **Inspeção visual:** capturei e revi `E05-turmas-mobile.png`, `E05-importacao-pdf-mobile.png`, `E05-selecao-categoria-dialogo.png`, `E05-adicionar-atletas-desktop.png` e `E05-senha-colaborador-modal.png`. Inputs, instrução do PDF, diálogo, rótulos longos e ação contextualizada ficaram legíveis; não foi necessário atualizar snapshots. A captura de alunos usa resposta sintética somente para exibir estados de seleção, porque a equipe real do fixture não tinha novos alunos elegíveis.
- **Pendências E05:** nenhuma. A preparação adicional das capturas também passou integração **987/987** (`E05-capture-prep.log`); o Compose de captura foi encerrado e removido sem tocar em dados fora do ambiente descartável.

### E06 — validado

- **Linha de base/reprodução:** a etapa começou após E05 validado. O primeiro red revelou preparos frágeis dos testes: login redireciona de verdade a `/edicoes` e um clique no trigger KVS fecha o painel antes que a busca esteja acessível. Os testes foram ajustados para usar a edição ativa pela API autenticada e para afirmar painel/campo antes da busca. A sequência red corrigida reproduziu 10 falhas e aprovou a visibilidade de ações em tablet. Falhas confirmadas: dias agenda são `div` sem teclado/nome/estado; filtros compactos não atualizam a tabela; KVS não mantém o painel aberto para seleção e a busca não aceita diacríticos; o cabeçalho do histórico está oculto e rótulos dependem de pseudo-elementos CSS; o vencedor depende de cor/troféu sem texto; o cartão de partida não abre com Enter. A tela exibida a 1024px é a composição compacta do SGI, então a falha do vencedor é válida. O cenário de ações tablet sem hover passou.
- **Evidências red:** `test-results/ui-ux-audit-20260913/E06-red-setup.log` (qualidade); `E06-red-browser.log` (tentativa inicial com erro de expectativa de redirecionamento e teste KVS sem guarda de abertura); `E06-red-agenda-retry.log` (quatro falhas agenda, incluindo botão ausente em ambas as composições; o quarto caso travou no mesmo seletor antes de incluir timeout curto); `E06-red-focused-final.log` (10 falhas/1 aprovação, retries desativados). Playwright gravou `error-context.md`, screenshot e trace por caso em `tests/browser/test-results/e06-*`.
- **Correções:** dias de calendário são controles nomeados com estado de hoje/seleção e contagem; calendário e filtros compactos têm alvos e anúncios acessíveis. A busca KVS ignora diacríticos, agrupa opções homônimas por categoria, mantém o filtro aberto durante digitação/seleção e oferece setas/Enter/Escape/Tab com foco restaurado. Histórico de jogos conserva associação semântica entre cabeçalhos e células; vencedor recebe indicação textual além da cor. Ações do chaveamento funcionam em tablet sem hover. O cartão do aluno abre por Enter/Espaço e o diálogo fecha por Escape com retorno de foco, sem controles aninhados.
- **Regressões green:** `tests/browser/e06-{agenda-accessibility,bracket-accessibility,student-game-details}.spec.cjs` passou **15/15** no MariaDB Compose oficial. O teste de teclado KVS foi repetido isoladamente (1/1). Uma captura `fullPage` interferia com geometria/foco do popup durante a própria regressão; evidência foi alterada para viewport (640×360) e asserções verificam que a busca/foco continuam intactos após capturar.
- **Execução integral:** `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual`, código 0. Resultado: PHPUnit **366 testes/2.536 asserções**, PHPStan **235/235**, JavaScript **102/102**, integração HTTP/banco/recuperação **987/987**, Playwright **124/124** (inclui offline, quatro perfis, raiz/subdiretório e E06), contrato visual **2/2**. O executor removeu o ambiente isolado ao final. A execução focada também passou com `E06_CAPTURE=1` e `--grep E06` (15/15).
- **Inspeção visual:** conferi `test-results/ui-ux-audit-20260913/E06-agenda-landscape-640x360.png`, `E06-kvs-landscape-open.png` e `E06-aluno-detalhes-mobile.png`. O calendário mantém controles e dias visíveis no início da grade; busca e opção KVS aparecem no viewport com foco visível; detalhe de jogo permanece legível em painel móvel. Nenhum snapshot visual do login precisou ser alterado.
- **Pendências:** nenhuma em E06. A antiga tentativa red e o ambiente descartável preparatório permanecem documentados; a execução green final usou um projeto/base MariaDB isolados e recém-criados.

### E07 — validado

- **Reprodução e regressão:** os cenários do placar, cronômetro e operação offline foram exercitados antes do fechamento. A revisão visual encontrou um defeito adicional a 320px: o badge verde de preparo ficava sobre o banner de sincronização e a barra expandida ocupava 390px de altura. A captura de referência antiga é apenas evidência do problema, sem uso como snapshot de aceite.
- **Correções:** botões de ponto descrevem equipe/ação; feedback separa gravação local, envio e confirmação; status distingue sem conexão, fila pendente, sincronização, revisão e sessão expirada; cronômetro tem anúncios discretos e overlay textual, sem anunciar cada segundo; bloqueio de tempo também desabilita a pontuação; estado do resultado é separado do estado de sincronização; ações de revisão permanecem disponíveis. A barra compacta mantém “Sincronizar agora” e “Ver pendências”, expande importação/exportação sob demanda e respeita foco/contraste. O badge de preparo some enquanto houver outro aviso e reaparece ao resolvê-lo.
- **Arquivos:** `resources/views/pages/competicoes/placar.php`; `resources/js/pages/competicoes/placar.js`; `resources/js/offline/{offline-core,mesario-offline}.js`; `resources/scss/shared.scss`; `resources/css/source/admin.css`; `tests/browser/{clock-persistence,mesario-offline,e07-offline-feedback}.spec.cjs`; `tests/javascript/{css-bundles,responsive-regression}.test.cjs`.
- **Validação:** build e checks passaram; `npm test` **102/102**; regressões JS responsivas e CSS **59/59**; Playwright focado do banner e casca offline **2/2**. A execução integral `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual` passou com código **0**: PHPUnit **366/2.536**, PHPStan **235/235**, JavaScript **102/102**, integração **987/987**, navegador **127/127** e visual **2/2**. Incluiu validação offline, quatro perfis, raiz/subdiretório, retrato/paisagem e desktop.
- **Inspeção visual:** `tests/browser/test-results/ui-ux-audit-20260913/E07-banner-offline-320.png`, gerada no teste focado com `E07_CAPTURE=1`, foi revisada. O banner ficou compacto (~166px), as duas ações principais continuam visíveis e o badge verde não encobre o aviso. Nenhum snapshot de contrato visual foi atualizado por E07.
- **Pendências:** nenhuma em E07.

### E08 — validado

- **Reprodução anterior:** a revisão do checkout encontrou `modo=view` trocado por `modo=create`, links com edição incorreta, ação móvel sem destino válido e ativação que enviava várias requisições e navegava sem confirmação HTTP/JSON. A pontuação não preservava modo/base path e sair com alterações não salvas não oferecia decisão.
- **Correções:** o resumo mostra edição/ano/status e etapas com links válidos que preservam `modo` e ID selecionado; ambos os layouts oferecem ações coerentes. Ativação exige confirmação, envia um POST à edição selecionada e só navega após `{success:true}`. Pontuação/modalidades preservam raiz ou `/SGI`; pontuação alterada permite cancelar, descartar ou salvar antes de continuar, e falhas mantêm os valores para nova tentativa.
- **Red e regressões focadas:** os oito cenários E08 falharam antes da correção nos contratos descritos. Depois das mudanças, `e08-edition-setup.spec.cjs` + `admin-lifecycle.spec.cjs` passou 14/14. A execução integral encontrou dois defeitos compartilhados: H1 duplicado no resumo a 390px e teste de foco que só procurava `<button>` numa ação de navegação `<a>`. O título móvel do conteúdo passou a H2 e o teste manteve a checagem de foco incluindo links; as duas regressões passaram 2/2 isoladamente.
- **Arquivos:** `resources/views/pages/eventos/configurar-resumo.php`, `resources/views/pages/eventos/configurar-pontuacao.php`, `resources/js/pages/eventos/{configurar-resumo,configurar-pontuacao,configurar-modalidades}.js`; `tests/browser/{e08-edition-setup,frontend-regression}.spec.cjs`.
- **Validação completa:** `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual`, código 0: PHPUnit 366/2.536, PHPStan 235/235, JavaScript 102/102, integração 987/987, Playwright 134 aprovados com um caso flaky E05, contrato visual 2/2. Os oito cenários E08 passaram dentro da suíte. Nenhum snapshot de contrato visual foi alterado.
- **Inspeção visual:** revisei resumo mobile/desktop, diálogo de ativação e confirmação de alterações não salvas após completar as transições Bootstrap. As ações mantêm hierarquia, contexto e alvos visíveis; texto de confirmação está legível. As referências antigas foram usadas só como evidência de antes, sem copiar para snapshots.
- **Estabilização do teste E05:** a revisão identificou uma espera `waitForResponse` sem prazo de 180s e ausência de asserções de prontidão antes do envio. O teste agora aguarda feedback do arquivo e lista carregada, usa timeout de 15s e confere a contagem de uploads após cada caso. Não foi encontrada evidência de defeito de produção; o caso de respostas falsas passou **3/3** em Compose descartável (`--repeat-each=3`). Essa mudança de harness remove a espera longa e torna uma ausência real de requisição identificável rapidamente.

### E09 — implementado; validação de navegador bloqueada

- **Reprodução anterior:** as listas tratavam HTTP 500/503, HTML 200 e JSON inválido como arrays vazios ou mantinham um spinner sem recuperação. A regressão de turmas reproduziu literalmente “Nenhuma turma cadastrada” depois de HTTP 500; jogos repetiu o mesmo comportamento com HTTP 503. O foco de arrecadação/ranking também encontrou perda de conteúdo confirmado quando uma consulta secundária falhava.
- **Mudança:** os renderizadores distinguem carregando, pronto, vazio válido e erro; erros expõem mensagem localizada com `role=alert`, preservam busca/seleção e oferecem “Tentar novamente”. Consultas secundárias mantêm o conteúdo principal e identificam dado desatualizado. O detalhe de membros em `aluno/jogos` libera o marcador de carregamento e, em uma nova tentativa após falha, verifica a conexão real antes de consultar novamente; se continuar sem rede, mantém o erro acionável.
- **Arquivos:** `resources/js/pages/eventos/{configurar-turmas,configurar-resumo,configurar-locais,configurar-arrecadacao}.js`, `resources/js/pages/{aluno/{jogos,ranking},resultados/ranking,competicoes/jogos,participantes/{turmas,turma-alunos},acesso/colaboradores,eventos/lista}.js`; specs `tests/browser/e09-*.spec.cjs` e ajustes de `e09-enrollment-recovery.spec.cjs`, `e09-student-roster-recovery.spec.cjs`.
- **Testes:** após as mudanças, o foco limpo E05/E09 chegou a **25/26**; o único caso restante era a segunda abertura do acordeão em soft-offline, levando ao ajuste final de reconexão. O teste de turmas passou com HTTP 500→retry e com `[]` vazio válido. O executor oficial sem navegador passou: `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -SkipBrowser -Keep` (código 0), PHPUnit **366/2.536**, PHPStan **235/235**, JS **103/103**, integração **987/987**.
- **Limitação:** depois do ajuste final, a tentativa de reexecutar o Playwright foi rejeitada pelo revisor automático por limite de uso. O código não é declarado validado sem essa repetição e sem o `all -IncludeVisual` posterior.

### E10 — implementado; validação visual pendente

- **Reprodução anterior:** Bootstrap recebia uma redefinição global de `.d-md-*` abaixo de 1200px, o que alterava utilitários fora do shell; o tema importava Inter do Google Fonts; offsets e regras de `.mobile-nav` não tinham consumidores; os blocos de impressão de ranking estavam duplicados em dois bundles; o login mantinha quatro imagens de banner/borda no DOM.
- **Mudança:** o breakpoint de 1200px continua, mas as regras compactas são escopadas a `body.sgi-app-shell`; a pilha de fontes é local (`system-ui`/`Segoe UI`/Roboto), sem URL externa. O token e seletores da barra móvel obsoleta foram removidos depois de conferir views e scripts; `#acoesInscricao` continua sticky no fluxo. A impressão fica em `shared.scss`, imprime uma variante do ranking, conserva título/lista e oculta navegação, filtros, botões e modal. Os banners do login são backgrounds locais do bloco visível, com nome acessível, reduzindo pedidos duplicados entre variantes.
- **Arquivos e testes:** `resources/scss/{_theme,bootstrap-theme,shared}.scss`, `resources/css/source/{admin,aluno-home,aluno-pages,login}.css`, heads, login e rankings; `tests/javascript/css-bundles.test.cjs`. `npm run build` (código 0), `node --test tests/javascript/css-bundles.test.cjs` (**53/53**), `npm run check` (**43/43**), `npm test` (**103/103**), `node --check tests/browser/accessibility.spec.cjs`, `git diff --check` e o executor `test-docker.ps1 -Database mariadb -SkipBrowser -Keep` (código 0: PHPUnit **366/2.536**, PHPStan **235/235**, integração **987/987**) passaram.
- **Pendência:** falta inspecionar no navegador as capturas do login, os pedidos de imagens e o preview de impressão. Nenhum snapshot foi alterado nesta etapa.

### E11 — implementado; execução completa pendente

- **Mudança:** foi criado `tests/browser/accessibility.spec.cjs` com três cenários observáveis: nomes/ajuda/envio por teclado no login, abertura do menu compacto com Escape e retorno do foco, e impressão do ranking com apenas uma variante e contexto visível. Os testes existentes de E03–E09 continuam cobrindo modais, filtros, uploads, offline, foco e reentrada.
- **Validação disponível:** todos os `tests/browser/*.spec.cjs` passaram em `node --check`, além do build, checks estáticos e `npm test` **103/103**. A suíte Playwright completa, a comparação visual adicional e a confirmação final dos quatro perfis/offline ainda dependem de uma execução de navegador autorizada.

## Auditoria final desta execução — 15/09/2026

- Releitura do plano confirmou que E00–E08 permanecem validados e que não há etapa funcional omitida. As únicas pendências reais continuam sendo as evidências de navegador/visual posteriores às últimas alterações de E09–E11; não foram convertidas em aprovação por inferência a partir de testes estáticos.
- O executor oficial isolado foi repetido depois das alterações finais: `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -SkipBrowser -Keep`, código **0**. Resultado: PHPUnit **366/366** (**2.536 asserções**), PHPStan **235/235**, lint PHP, build, `npm run check` **43/43**, `npm test` **103/103** e integração **987/987**.
- `node --test tests/javascript/css-bundles.test.cjs` passou **53/53**; todos os `tests/browser/*.spec.cjs`, incluindo `accessibility.spec.cjs` e os specs E09, passaram em `node --check`; `git diff --check` não encontrou erros (somente avisos normais de conversão LF/CRLF do Git).
- No servidor isolado mantido pelo executor, a página `/login`, os bundles `login.css`/`shared.css` e as quatro imagens locais do login responderam HTTP **200** com `Content-Type` correto. O HTML servido contém somente os dois blocos visuais nomeados por `role="img"`; o CSS compilado servido confirma fontes locais, shell escopado e a regra de impressão.
- Não foram introduzidas dependências, endpoints, migrações ou regras de negócio. Os assets continuam sendo gerados pelo build, e o ambiente Compose foi mantido por `-Keep` para investigação, sem tocar na base de trabalho.
- A execução Playwright/visual final permanece impedida pelo limite de uso do revisor automático registrado na etapa E09. Não é seguro marcar E09–E11 como `validado` sem essa evidência.

## Verificação adicional — 15/09/2026

- `powershell -ExecutionPolicy Bypass -File tools/test-local.ps1 -Suite quality` passou com PHPUnit **366/366** (**2.536 asserções**), PHPStan **235/235**, lint PHP, build, `npm run check` **43/43** e `npm test` **103/103**.
- Todos os **40** specs de navegador passaram em `node --check`; `npm --prefix tests/browser test -- --list` confirmou **162 testes** descobertos, incluindo E09 e E11. `git diff --check` não encontrou erros; os avisos exibidos são somente conversões normais de LF/CRLF do Git.
- A tentativa de concluir `powershell -ExecutionPolicy Bypass -File tools/test-docker.ps1 -Database mariadb -IncludeVisual` foi feita com autorização elevada, mas não iniciou: o Docker Desktop não expôs `dockerDesktopLinuxEngine` (`failed to connect to the docker API ... O sistema não pode encontrar o arquivo especificado`). Portanto, Playwright, integração isolada e contrato visual continuam não executados nesta verificação.

## Fechamento da validação — 15/09/2026

- Com o Docker disponível, a suíte oficial foi executada até a etapa de navegador. Qualidade passou; integração HTTP/banco/recuperação passou **987/987**; Playwright passou **160/160**, incluindo os quatro perfis, offline, raiz/subdiretório e os cenários E09/E11.
- Foram estabilizados somente asserts dos testes que dependiam de foco após Escape/resize, seletor ambíguo de impressão e nome parcial de categoria. Não houve alteração de regra de negócio para corrigir essas falhas.
- A comparação visual inicialmente detectou snapshots Linux defasados após a alteração intencional da tela de acesso. Os quatro snapshots Linux (`acesso-*-(desktop|mobile)-linux.png`) foram regenerados no container, inspecionados em estados normal/erro e confirmados sem atualização automática: contrato visual **2/2**.
- A verificação final de qualidade permanece: PHPUnit **366/366** (**2.536 asserções**), PHPStan **235/235**, `npm run check` **43/43**, `npm test` **103/103** e `git diff --check` sem erros. Os ambientes temporários do Compose foram encerrados e removidos.
- E09, E10 e E11 ficam encerrados como validados nesta execução; nenhuma pendência de teste UX/UI permanece.
