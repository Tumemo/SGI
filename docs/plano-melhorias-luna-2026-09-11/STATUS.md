# Estado do novo plano

Atualizado em 12/09/2026. Referência inicial: `f4c3f4902effde9a5e2e862d5481dd13909673f6`. O plano antigo T00–T29 permanece histórico concluído.

## Estado das tarefas

| Tarefa | Estado | Evidência |
| --- | --- | --- |
| N00 | Completa | Ambiente isolado identificado e baseline executado sem tocar a base de trabalho. PHP 8.2.12 do XAMPP, MariaDB 10.4.32 local. Docker inacessível por permissão nesta sessão. |
| N01 | Completa | EOL do CS Fixer alinhado ao `.editorconfig`; pós-condições de testes e ciclo de sessão corrigidos; cleanup do executor dividido em etapas independentes e coberto por teste com falha sintética. Qualidade e runner `all` aprovados. |
| N02 | Completa | C01 reproduzido antes da correção e coberto por regressão HTTP e fluxo Playwright offline. Escopo de edição do ponto agora vem do banco. |
| N03 | Completa | C02 reproduzido antes da correção: aluno lia edição B encerrada sem publicação e ID inexistente retornava resposta vazia. `RankingController` agora exige edição encerrada e `ranking_publicado_em`, usando o contrato `EdicaoConsulta`. |
| N04 | Completa | C03 confirmado e coberto: autoria de pontos sincronizados vem do operador autenticado; anulação preserva o autor original e registra o operador atual. Fila e body legado permanecem inalterados. |
| N05 | Completa | C04 reproduzido por POST direto; gênero/categoria agora são verificados pelo servidor com contexto bloqueado, e a semântica parcial do lote foi preservada. |
| N06 | Completa | C05 reproduzido em quatro expectativas HTTP antes da correção; retry agora reconhece modalidades elegíveis já inscritas uma vez, sem nova equipe/vaga. Integração e `all` aprovados. |
| N07 | Completa | C06 reproduzido em HTTP/importação/redistribuição, com regressões para 4xx/5xx, log detalhado, rollback e avisos. Integração 555/555 e perfil `all` aprovados. |
| N08 | Completa | C07 reproduzido antes da correção; regressões unitárias, HTTP, rollback e compatibilidade offline cobertas. Perfil `all` aprovado em MariaDB sintético. |
| N09 | Completa | C07 (limites e status) reproduzido em serviço e HTTP; valores inválidos agora são recusados antes do repositório. Limites aceitos e cadastro no navegador cobertos. |
| N10 | Completa | N10a e N10b completas: modalidades e turmas validam edição/categoria no estado final; transferências com vínculos são bloqueadas; ranking e administração compartilham o caso de uso de turma. |
| N11 | Completa | R01 reproduzido com processos/conexões distintos. Anulação e finalização agora serializam pelo jogo; validação vinculada usa leituras atuais sob trava. |
| N12 | Completa | R02 foi reproduzida com processos/conexões independentes: duas criações manuais, edição×criação e lote×criação podiam ocupar a mesma faixa. Trava compartilhada por local e revalidação atual dentro das transações; integração concorrente, navegador e `all` aprovados. |
| N13 | Completa | Validação final, cobertura do portal em raiz e `/SGI`, documentação alinhada e diff revisado. Alvos externos indisponíveis permanecem explicitamente pendentes. |

## Evidência desta execução

- A regressão de N02 foi executada antes da correção. Em 486 asserções, 482 passaram e quatro falharam: leitura de pontos da edição B, lista de atletas de B, tentativa de criar/anular ponto de B e a contagem da rota legada após o POST indevido. O cenário usou edição A ativa, jogo B iniciado, atleta ativo e elegível e ponto vinculado persistido; portanto, o defeito não foi mascarado por estado ou vínculo inválido.
- Após a correção, o runner `all` passou: PHPUnit 234 testes/2.194 asserções, PHP lint, PHPStan 223/223 sem erros, CS Fixer 0/293 ajustes, build 104 assets, checks JavaScript em 42 arquivos e testes JavaScript aprovados; integração HTTP/banco 491/491; Playwright 54 passaram e 1 foi ignorado pelo filtro/configuração existente.
- O teste Playwright `mesario-offline.spec.cjs` confirmou seleção obrigatória de atleta, registro offline, fila pendente e reentrada em jogo encerrado. O teste do executor simulou falha de qualidade seguida de falha do cliente SQL de remoção; confirmou aviso da falha, execução das etapas seguintes e preservação da falha/código originais.
- N00 registrou a reprodução do erro de sessão antes da correção: integração falhava após saída de texto porque `session.save_path` era alterado depois dos headers. Com diretório de sessão preparado pelo executor e `SessionManager` respeitando o caminho já ativo, o baseline de integração passou com 483/483; a execução final passou com 491/491.
- N03 foi executada primeiro em vermelho: integração reportou 498 asserções, 496 aprovadas e 2 falhas (`aluno não lê edição B encerrada enquanto o ranking não foi publicado` e `ID de edição ausente e inexistente não retornam dados do ranking`). Após exigir a publicação e preservar o bloqueio da edição ativa, integração passou isoladamente com 497/497. O fluxo de publicação usou a API administrativa existente e confirmou que a edição não pode ser lida antes de encerrada e publicada, nem quando reativada.
- Execução completa após N03: `tools/test-local.ps1 -Suite all -DatabaseBackend local -DatabasePort 63107 -DatabaseHost 127.0.0.1 -DatabaseUser root -DatabasePassword <credencial-sintética> -PhpPath C:\xampp\php\php.exe`. PHPUnit 234 testes/2.197 asserções, PHP lint, PHPStan 223 arquivos sem erros, CS Fixer 0/293 ajustes, build 104 assets, checks JavaScript em 42 arquivos, testes JavaScript, integração 497/497 e Playwright 54 passaram/1 ignorado pelo filtro existente. Artefatos: `test-results/local-20260912_084753_6bec97`.
- N04 foi reproduzida em vermelho antes da correção: o teste unitário enviou autor forjado, zero, nulo e ausente; o comando chegava com `[99, 0, 2, 2]` em vez de usar somente o operador autenticado. `ResultadoService` agora envia o operador em argumento separado até `PontoRepository`; a infraestrutura ignora `registrado_por` do body e exige operador positivo antes de criar/anular eventos. `MysqliPartidaGateway` também recebe ator explicitamente, repassado pelo controlador.
- Regressões N04: PHPUnit verificou operador explícito e recusa sem ator; integração confirmou rollback completo de ponto/placar quando o resultado diverge, gravação de autor verdadeiro para campos forjados/zero/nulos/ausentes e replay sem duplicação. Outro operador anulou ponto real, preservando `registrado_por` e preenchendo `anulado_por`; replay não descontou o placar novamente. Playwright criou duas jogadas offline, anulou uma, reconectou e confirmou duas ações (uma ativa, uma anulada).
- Execução completa após N04: `tools/test-local.ps1 -Suite all -DatabaseBackend local -DatabasePort 63107 -DatabaseHost 127.0.0.1 -DatabaseUser sgi_runner -DatabasePassword <credencial-sintética> -PhpPath C:\xampp\php\php.exe`. PHPUnit 236 testes/2.206 asserções; PHP lint; PHPStan 223 arquivos sem erros; CS Fixer 0/293; build 104 assets; checks JavaScript em 42 arquivos; testes JavaScript aprovados; integração 504/504; Playwright 54 passaram/1 ignorado pelo filtro existente. Artefatos: `test-results/local-20260912_092632_2e2c19`. Integração e navegador também passaram isoladamente em `test-results/local-20260912_091841_dc046e` e `test-results/local-20260912_091931_219e43`.
- N05 foi reproduzida antes da correção: um POST direto com aluno MASC e equipes da mesma turma colocou o aluno também na modalidade FEM (`insercoes: 2`, sem erros). A primeira integração vermelha registrou 533 asserções, 529 aprovadas e 4 falhas; além do vínculo FEM indevido, revelou que a fixture de lote enviava quatro IDs e violava o limite já contratado de três, então os casos foram separados/refinados antes de validar a correção. O teste unitário da regra também falhou inicialmente porque a regra de compatibilidade ainda não existia.
- N05 adicionou regra pura para gênero/categoria. O repositório agora lê gênero do usuário e categoria da turma sob as travas existentes, bloqueia modalidades em ordem numérica, revalida equipe/modalidade/edição/status e só inclui modalidades elegíveis no limite e na capacidade. Modalidades recusadas geram erro individual; as válidas do lote continuam, sem alterar CSRF, aceite de termos, API ou migrações. A regressão cria equipe correspondente para modalidade de gênero e categoria incompatíveis e remove seus usuários/equipes/modalidades sintéticos ao fim para não contaminar testes seguintes.
- A integração N05 passou isoladamente com 535/535, cobrindo MASC/MASC, FEM/FEM, MISTO, categoria correta/incorreta, equipe/modalidade/usuário inativos, outra turma/edição, aceite de termos revogado, CSRF, lote parcial e capacidade. `ConcurrentInvariantsTest` confirmou limite de três modalidades e disputa da última vaga sem ultrapassar capacidade.
- Na primeira execução isolada do navegador, a falta de limpeza das fixtures causou timeout em `occurrence-offline-edit.spec.cjs`; a regressão N05 foi ajustada para apagar apenas seus próprios registros sintéticos. Na execução final, o caso passou em 5 segundos.
- Execução completa após N05: `tools/test-local.ps1 -Suite all -DatabaseBackend local -DatabasePort 63107 -DatabaseHost 127.0.0.1 -DatabaseUser sgi_runner -DatabasePassword <credencial-sintética> -PhpPath C:\xampp\php\php.exe`. PHPUnit 237 testes/2.215 asserções; PHP lint; PHPStan 223 arquivos sem erros; CS Fixer 0/293; build 104 assets; checks JavaScript em 42 arquivos e testes JavaScript aprovados; integração 535/535; Playwright 54 passaram/1 ignorado pelo filtro/configuração existente. Artefatos: `test-results/local-20260912_100129_aba780`.
- N06 reconfirmou C05 no `MysqliInscricaoRepository`: vínculos já existentes eram removidos antes de contar e só eram reconhecidos no loop de novas modalidades; retry somente existente retornava `success=false` e `ja_existentes=0`. A regressão de integração falhou primeiro em quatro respostas: retry simples, IDs/equipes duplicados, lote existente+novo e modalidade existente junto a erro de categoria (536/540 passaram).
- N06 corrige a contagem a partir da interseção entre modalidades elegíveis solicitadas e modalidades já inscritas, deduplicando por ID de modalidade. O fluxo existente de limite, capacidade, equipes, envelope e parcialidade permanece; nenhuma vaga/equipe nova é disputada no retry já satisfeito.
- Integração após N06 passou com 540/540. O `all` final passou: PHPUnit 237 testes/2.215 asserções; lint PHP; PHPStan 223 arquivos sem erros; CS Fixer 0/293 ajustes; build com 104 assets; checks em 42 arquivos JavaScript; 95 testes JavaScript; integração 540/540; Playwright 54 passaram/1 ignorado pela configuração existente (55 cenários descobertos). Artefatos: `test-results/local-20260912_102139_ec3809`.
- Para o `all` N06 foi criada uma segunda instância MariaDB 10.4.32 isolada e limitada a loopback em `test-results/luna-n06-mariadb`, porta 63108, pois as credenciais do servidor descartável anterior não estavam disponíveis nesta execução. Nenhuma base de trabalho foi usada; migrações e CI não foram alteradas/executadas.
- N07 reconfirmou C06 nos fluxos HTTP de inscrição, importação PDF e redistribuição: validações conhecidas devem permanecer 4xx; falhas MySQL, parser e infraestrutura devem resultar em 5xx genérico, com detalhe no log. Antes da correção, a suíte unitária falhou porque `ImportacaoTurmaService` devolvia a mensagem interna do repositório; a integração falhou em cinco expectativas de vazamento/status para inscrição, importação e redistribuição.
- N07 separa recusas conhecidas de inscrição em `InscricaoRecusadaException`; erros genéricos agora seguem o envelope 500 seguro com log. O importador propaga falhas internas depois de rollback, mantendo avisos de linhas inválidas no resultado. A redistribuição distingue falta de capacidade (400 público) de falha SQL (500), reverte alterações parciais e não expõe caminho, SQL ou stack.
- Uma primeira integração após a correção aprovou 451 asserções, mas o worker concorrente encerrou com erro porque a exceção nova não era capturada como recusa de regra. O tipo específico foi alinhado à convenção `RuntimeException` dos workers; a integração completa subsequente passou com 555/555, incluindo a disputa do limite de três modalidades.
- O `all` final de N07 passou: PHPUnit 238 testes/2.221 asserções; lint PHP; PHPStan 224 arquivos sem erros; CS Fixer 0/294 ajustes; build 104 assets; checks em 42 arquivos JavaScript e 95 testes JavaScript aprovados; integração HTTP/banco 555/555; Playwright 54 passaram e 1 foi ignorado pela configuração existente. Artefatos: `test-results/local-20260912_110623_e845b8`. Os logs sintéticos `N07_IMPORT_SQL_MARKER` e `N07_REDIS_SQL_MARKER` ficaram em `server.stderr.log`; os corpos HTTP dos testes não os expuseram.
- N07 foi validado em MariaDB 10.4.32 descartável, restrito a loopback, porta 63108. MySQL 8.4, MariaDB 10.11, GitHub Actions e contrato visual não foram executados; nenhuma migração foi alterada.
- N08 reconfirmou C07: `ResultadoController` descartava linhas/eventos escalares com `array_filter`, e `ResultadoService` aceitava valores `is_numeric` e convertia gols/IDs com `(int)`. A regressão unitária foi executada antes da correção e falhou em 7 casos: gols fracionários `3.9`/`-0.5`, string decimal, overflow de `INT`, linha escalar, evento escalar e string inteira não normalizada. A integração HTTP pré-correção registrou 564 asserções, 561 aprovadas e 3 falhas para os casos de decimal e itens escalares; depois foram acrescentados casos para `pontos` raiz inválido e rollback.
- N08 normaliza somente inteiros ou strings inteiras válidas, respeita o limite do `INT` assinado e valida IDs de equipe sem coerção. Itens escalares e tipo inválido em `pontos` são recusados integralmente. A validação de zero/empate ocorre antes de qualquer gravação; divergência entre eventos vinculados e placar continua coberta por rollback. O teste confirma que 0–1 é válido, e strings de formulário como `03` seguem aceitas. Não houve mudança de migração, CSRF, autoria, fila ou formato legado de mutação.
- A regressão unitária final passou com 27 testes/59 asserções. O perfil `all` final passou: PHPUnit 253 testes/2.251 asserções; lint, PHPStan, CS Fixer, build e verificações JavaScript; integração HTTP/banco 566/566; Playwright 54 passaram e 1 foi ignorado pela seleção/configuração existente. Artefatos: `test-results/local-20260912_113733_1bae55`. O primeiro `all` após a mudança parou no PHPStan por anotação `list<mixed>` incompatível com a reindexação; após corrigir a anotação, o segundo parou apenas no EOL do formatador. Ambos foram corrigidos e a execução final completa passou.
- N08 usou MariaDB 10.4.32 descartável e limitado a loopback, porta 63112; nenhuma base de trabalho foi usada. MySQL 8.4, MariaDB 10.11, CI remoto e contrato visual separado não foram executados.
- N09 reconfirmou a segunda parte de C07: `max_inscrito_modalidade` usava `(int)` sem limite/validação; `max_equipes` convertia texto arbitrário, frações e negativos pequenos para zero/NULL ilimitado; `status_modalidade` era convertido para string e deixado para o ENUM do banco. A migração `001_initial_schema.sql` confirma colunas `INT(11)` e ENUM `'1'/'0'`.
- N09 foi reproduzida antes da correção: os testes unitários registraram 50 falhas em 75 casos e dois avisos de conversão de array em string; POST/PUT HTTP aceitaram entradas inválidas como HTTP 200 ou responderam 500 em casos de overflow/status, com valores coercidos ou alterados. A regressão cobre negativos, frações, texto decimal/arbitrário, booleanos, arrays, overflow e status não permitido em create e update parcial.
- `ModalidadeService` valida limites antes do repositório. `NULL`, vazio e zero seguem ilimitados; inteiros e strings apenas de algarismos entre 1 e 2.147.483.647 definem limite; negativos, frações, booleanos, arrays, texto e overflow retornam 400. Status aceita somente `0`/`1` como inteiro ou string, tanto em POST quanto PUT. O contrato ficou documentado em `docs/architecture.md`; testes de inscrição já existentes continuam cobrindo capacidade cheia e repetição.
- N09 adicionou verificação HTTP de que payloads inválidos não criam modalidade nem alteram a linha existente; as formas válidas (ilimitado, 1, INT máximo e os dois status) são exercitadas em PUT com restauração das fixtures. O Playwright existente de cadastro mantém criação por formulário com limites válidos (15 inscritos/2 equipes).
- A regressão unitária passou com 85 testes/120 asserções. O `all` final passou: PHPUnit 325 testes/2.343 asserções; lint, PHPStan, CS Fixer, build e verificações JavaScript; integração HTTP/banco 651/651; Playwright 54 passaram e 1 foi ignorado pela configuração existente. Artefatos: `test-results/local-20260912_120007_a5de62`.
- N09 foi validado em MariaDB 10.4.32 descartável e restrito a loopback, porta 63116. Nenhuma base de trabalho/migração foi alterada. MySQL 8.4, MariaDB 10.11, CI remoto e contrato visual separado permanecem não executados.
- N02 também passou isoladamente em integração (`test-results/local-20260912_082208_f120f1`) e navegador (`test-results/local-20260912_082303_5dfb8d`); execução anterior de `all`: `test-results/local-20260912_083206_99ff55`.
- Não foram executados MySQL 8.4, MariaDB 10.11, GitHub Actions nem contrato visual. Docker não estava acessível; N02 não alterou aparência. Nenhuma migração foi alterada.
- Para testes, foi iniciado um servidor MariaDB descartável sob `test-results/luna-20260912-mariadb`, porta local 63107. Ele é mantido durante esta implementação e será encerrado ao final; não é a base de desenvolvimento.
- N10a reconfirmou C08: o POST aceitava modalidade cuja categoria pertencia a outra edição; PUT parcial alterava categoria ou edição sem validar o par final; e a edição podia ser trocada mesmo com equipes vinculadas. A integração pré-correção falhou especificamente nos casos de POST incompatível, PUT parcial por categoria, PUT parcial por edição e transferência com equipe (esperado 400, observado 201/200 com persistência). O fixture inicial também provocou erro 500 por enviar status ENUM `0` via binding; o cenário foi corrigido para usar status ativo, sem alterar a produção, antes de fechar a reprodução.
- N10a introduziu `ModalidadeScopeRules` e passou a validar criação e mudança de escopo dentro da mesma transação que bloqueia edição, categoria e modalidade. PUT parcial é combinado com o escopo persistido; categoria inativa/inexistente ou de outra edição é recusada; transferência de edição é recusada se houver equipe, inscrição, jogo, crédito de pódio ou reserva de agenda. Não move descendentes nem altera leituras históricas. Testes unitários cobrem a regra; integração cobre POST/PUT parcial, atualização simultânea válida e preservação do escopo após recusa nos cinco tipos de vínculo.
- Regressão unitária final: `C:\xampp\php\php.exe vendor/bin/phpunit --filter 'ModalidadeScopeRulesTest|ModalidadeServiceTest'` — 84 testes e 106 asserções aprovados. Integração N10a: `tools/test-local.ps1 -Suite integration -DatabaseBackend local ...` — 676/676 asserções aprovadas em MariaDB 10.4.32 sintético.
- Validação de qualidade depois da mudança: perfil `quality` aprovado; `composer verify` com 330 testes/2.353 asserções, lint PHP, PHPStan sem erros e CS Fixer 0/296; build, `npm run check` e `npm test` (95 aprovados). A suíte completa terminou com código 0 usando `powershell -ExecutionPolicy Bypass -File tools/test-local.ps1 -Suite all -DatabaseBackend docker -Database mariadb -PhpPath C:\xampp\php\php.exe`: integração 676/676, Playwright 54 aprovados e 1 ignorado pela seleção/configuração existente. Artefatos: `test-results/local-20260912_125010_03ec72`. A execução completa foi desviada para o backend Docker após a instância MariaDB local do XAMPP falhar durante inicialização com assertion do InnoDB/erro de I/O; nenhuma base de trabalho foi usada. Não foram executados MySQL 8.4, CI remoto ou contrato visual separado.
- N10b confirmou que o PUT do ranking gravava diretamente em `turmas` e divergia do endpoint administrativo: categoria de outra edição e transferência de turma vinculada eram aceitas, e IDs de edição/categoria inválidos não eram recusados pela mesma regra. Antes da correção, a regressão integrada apresentou 14 falhas em 697 asserções; os casos observados incluíam alteração parcial de categoria/edição incompatível, transferência pelos dois caminhos e histórico que não deveria ser deslocado. O caso de uso do ranking agora delega ao serviço compartilhado de turma; as duas rotas chegam ao mesmo repositório e à mesma validação transacional.
- N10b valida a existência das edições e categorias e o pareamento ativo edição/categoria. A verificação e gravação são protegidas por transação e locks ordenados; mudança concorrente do escopo é detectada. Transferência de edição é recusada quando há usuários, equipes, arrecadações, créditos de pódio ou ocorrências vinculadas. Ajustes permitidos de nome e pontuação continuam aceitos; nenhum vínculo histórico é migrado ou reescrito. Não foi necessária migração.
- Regressão final de N10b: integração `698/698` aprovada. O perfil `quality` foi repetido depois da correção e encerrou com código 0: PHPUnit 334 testes/2.378 asserções, lint PHP, PHPStan, CS Fixer, build, `npm run check` e 95 testes JavaScript aprovados. Artefatos de qualidade: `test-results/local-20260912_132541_bb715a`.
- `all` final de N10: `powershell -ExecutionPolicy Bypass -File tools/test-local.ps1 -Suite all -DatabaseBackend docker -Database mariadb -PhpPath C:\xampp\php\php.exe`, código 0. Integração HTTP/banco `698/698`; Playwright `54 passaram, 1 ignorado` (55 cenários, o caso individual de ranking ignorado pela configuração existente). Artefatos: `test-results/local-20260912_132646_e048dd`. O banco foi MariaDB descartável criado pelo executor; não houve uso da base de trabalho. MySQL 8.4, CI remoto e contrato visual separado permanecem fora desta execução.

- N11 confirmou R01 antes de alterar produção. A regressão integrada vermelha somou 704 asserções (700 aprovadas, 4 falhas): uma anulação validada como `Iniciado` prosseguia após B concluir o jogo, deixando `Concluido` com placar 0–0 e o evento anulado; no sentido inverso, B mantinha snapshot anterior à anulação e rejeitava o placar 1–0 apesar de score e evento já estarem consistentes.
- Ordem de locks após a correção: A (anulação) faz a leitura inicial de ponto/status, trava `jogos` e compara o estado atual com o observado pelo serviço, depois trava `partidas` e `artilheiros`, atualiza placar/evento e confirma. B (resultado) resolve participantes, trava `jogos`, trava partidas em ordem de equipe e lê os eventos ativos com `FOR UPDATE` antes de concluir/avançar/créditos e confirmar. As leituras consistentes de pré-validação não autorizam gravação após mudança do estado.
- A regra de estado permitido continua no domínio/serviço (`PontoRules` e `PontoService`); a infraestrutura recebe o estado esperado e revalida depois do lock. A sincronização de retificações já concluídas continua aceita quando o próprio `ResultadoService` bloqueia o jogo para a correção; a integração de pódio/retificação passou sem mudar autoria, fila ou ID de mutação.
- Regressões concorrentes cobrem finalização vencendo a anulação, finalizador com snapshot anterior observando anulação já confirmada, duas anulações da mesma jogada e duas jogadas diferentes. Replays permanecem idempotentes. `AtomicMutationTest`, `PlacarAndArtilhariaTest` e a suíte offline verificam rollback sem confirmação parcial e manutenção das operações recusadas; nenhum deadlock real foi provocado nesta execução.
- O runner `integration` após N11 passou com `708/708` asserções em `test-results/local-20260912_141709_0daa0d`. O perfil `quality` passou com código 0: PHPUnit 335 testes/2.394 asserções, lint PHP, PHPStan 228 arquivos sem erros, CS Fixer 0/300, build 104 assets, checks em 42 arquivos JS e 95 testes JS. Artefatos: `test-results/local-20260912_142032_e7d663`.
- `all` final de N11 passou com código 0: integração `708/708`; Playwright `54 passaram, 1 ignorado` (55 cenários; teste individual de ranking ignorado pela configuração existente). Artefatos: `test-results/local-20260912_142152_7334bf`. Banco MariaDB 10.4.32 sintético e descartável; nenhuma base de trabalho ou migração alterada. Não foram executados MySQL 8.4, MariaDB 10.11, CI remoto ou contrato visual separado.
- N12 começou pela reprodução de R02 com processos e conexões independentes. Antes da correção, a regressão descobriu que duas criações manuais podiam passar pelo precheck e persistir jogos/partidas conflitantes; edição×criação e lote×criação também aceitavam a mesma faixa. O teste vermelho teve 6 falhas em 719 asserções. A cobertura usa barreiras determinísticas e também prova paralelismo entre locais, datas diferentes, limite exato de 10 minutos, rejeição de faixa inválida e rollback sem partidas órfãs.
- N12 usa trava das linhas de `locais` em ordem crescente e revalida jogos/reservas com leitura atual dentro da transação para criação, edição e confirmação de bloco. As operações de agenda em lote excluem seus próprios jogos ao revalidar o conjunto; não há trava global por edição. Nenhuma migração foi criada; CSRF, envelopes e IDs/idempotência foram preservados.
- Na primeira suíte de navegador após a correção, três fixtures paralelos reutilizaram o mesmo local e data e foram recusados pelo novo conflito legítimo. O helper passou a criar um local sintético por teste; a primeira tentativa excedeu o limite de 45 caracteres de `nome_local`, confirmado no schema e no log HTTP. O nome foi reduzido e a suíte repetida sem afrouxar assertions: 54 aprovados, 1 skip configurado. Os fluxos de cronômetro, mesário offline, responsividade, edição offline de ocorrência, chaveamento, placar e torneio passaram.
- `all` final de N12 encerrou com código 0 em `test-results/local-20260912_151941_0b3fef`: PHP 8.2.12; PHPUnit 335 testes/2.403 asserções; lint PHP; PHPStan 229 arquivos sem erros; CS Fixer 0/301 ajustes; build 104 assets; `npm run check` com 42 arquivos e 95 testes JavaScript; integração HTTP/banco 719/719; Playwright 54 passaram e 1 ignorado pela configuração existente (55 cenários). MariaDB 10.4.32 sintético, em loopback; base de trabalho preservada.
- N12 não alterou migrações nem dados de trabalho. No fechamento daquela tarefa, MySQL 8.4, MariaDB 10.11, PHP 8.4, CI remoto e contrato visual separado ainda não tinham sido executados; PHP 8.4 foi posteriormente validado em N13, e os demais alvos indisponíveis seguem como pendência.
- N13 encontrou e reproduziu um defeito de implantação em subdiretório: após inscrição válida em `/SGI/aluno/modalidades`, `modalidade.js` redirecionava para `/aluno/inicio`. A regressão Playwright falhou esperando `/SGI/aluno/inicio` e registrou navegação para `http://127.0.0.1:59965/aluno/inicio`. A fonte agora usa `APP_BASE + '/aluno/inicio'`; após `npm run build`, o fluxo passou com o prefixo preservado.
- `tests/browser/aluno-portal.spec.cjs` agora exercita, no runner existente, inscrição válida, recusa HTTP 400 de modalidade FEM incompatível com gênero/categoria, retry com `insercoes=0` e `ja_existentes=1`, ranking oculto antes da publicação e ranking visível após publicação. Os cenários de inscrição e publicação rodam com sessão/aluno no navegador; a administração publica uma edição sintética sem jogos e restaura a edição ativa em `finally`. A expectativa visual conta a categoria selecionada pelo ranking e confirma card visível, sem supor que a tela liste todas as sete turmas ao mesmo tempo.
- Checagem manual isolada de `/SGI`: `/SGI/api/v1/health` e recursos retornaram HTTP 200; `deployment-paths.spec.cjs` passou 1/1, `mesario-responsive.spec.cjs` 4/4 e a jornada completa `aluno-portal.spec.cjs` passou 4/4 em `http://127.0.0.1:59965/SGI/`. A jornada cobriu inscrição inelegível e retry, publicação do ranking e redirecionamento prefixado. O servidor próprio PID 25272 foi encerrado; logs e sessão sintética foram preservados em `test-results/luna-n13-subdir-repro`. Após o `all`, a instância MariaDB sintética da porta 65196 foi parada com shutdown normal; a base mantida `sgi_test_local_20260912155703e99654` foi removida após confirmar que pertencia a esta execução. Datadir e logs permanecem em `test-results/luna-20260912-n12-mariadb`.
- `all` após as mudanças finais passou com código 0 em `test-results/local-20260912_161103_8a724b`, PHP 8.2.12 e MariaDB 10.4.32 descartável: PHPUnit 335/2.403; lint PHP; PHPStan 229 arquivos sem erros; CS Fixer 0/301; build 104 assets; checks em 42 arquivos JS; testes JavaScript 95/95; integração 719/719; Playwright 54 aprovados/1 skip já configurado (55 cenários descobertos). O navegador incluiu criação/anulação de pontos online/offline, sincronização após reconexão, reentrada SPA, fila/identidade de sessão e torneios. Aparência/layout não mudou; contrato visual separado não foi necessário.
- Matriz local adicional: PHP 8.4.25 passou em `quality` (artefato `test-results/local-20260912_153354_3913e1`) e em integração/navegador com MariaDB 10.4.32 (719/719; 54/1 skip, artefato `test-results/local-20260912_154245_585ee3`). O PHP 8.4 instalado não tinha `mysqli` ativo; usei ini temporário isolado em `test-results/luna-php84-ini`, sem alterar configuração instalada ou arquivos do projeto. A validação final depois da correção de URL foi executada em PHP 8.2, incluindo `all` e a jornada `/SGI`. O perfil `quality` também foi repetido depois dessas últimas mudanças em PHP 8.4.25 com o ini temporário: 335 testes/2.403 asserções, lint, PHPStan 229 arquivos sem erros, CS Fixer 0/301, build 104 assets, checks em 42 arquivos e testes JavaScript 95/95; artefato `test-results/local-20260912_161958_a01ac3`. O CS Fixer em PHP 8.4 emitiu apenas aviso de que a versão mínima do projeto é 8.2.
- Docker não ficou disponível: `docker version` falhou por acesso negado a `C:\Users\ferreira-mr\.docker\config.json` e permissão negada ao pipe `npipe:////./pipe/docker_engine`. Portanto MySQL 8.4, MariaDB 10.11, GitHub Actions e o job visual Linux não foram executados. A configuração declarada em `.github/workflows/ci.yml` foi apenas confrontada, não tratada como execução.
- N13 alinhou `docs/architecture.md`, `docs/testing.md`, `docs/deployment.md` e os textos dos planos arquivados à política de trava por local, rotas canônicas, prefixo e matriz visual Linux. Não houve alteração de migração, lockfile, `.env`, segredo ou `public/assets` versionado. `git diff --check` e inspeção final do diff passaram; alertas LF→CRLF do Git foram apenas informativos.

## Próximo passo

Nenhuma tarefa de implementação resta no plano N00–N13. MySQL 8.4, MariaDB 10.11, GitHub Actions e contrato visual Linux seguem como validação externa pendente porque o Docker foi bloqueado neste host; executar esses alvos num ambiente habilitado antes de declarar cobertura dessa matriz. Não houve publicação nem push.
